---
phase: 03-bootstrap-com-stage-2-novo-projeto-md
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mentor/reference.md
  - mentor/novo-projeto.md
autonomous: true
requirements: [EST-03]
must_haves:
  truths:
    - "O template do PROGRESSO.md em reference.md tem um campo Capacidade: por marco, colado abaixo da User Story, em ambos os blocos de exemplo (marco 00 e marco 01)"
    - "O valor do campo Capacidade: usa o formato verbo-capacidade puro (ao terminar, voce consegue <verbo> <conceito>) sem nenhum jargao de framework"
    - "O Passo 5 de novo-projeto.md instrui o agente a gravar a frase de capacidade por marco no PROGRESSO.md, colada a User Story"
    - "O Passo 5 contem o guardrail literal de leveza (exatamente 1 frase por marco)"
    - "O Passo 5 reforca dimensionar o primeiro marco como o menor possivel"
  artifacts:
    - path: "mentor/reference.md"
      provides: "Campo Capacidade: na arvore de marcos do template PROGRESSO.md"
      contains: "**Capacidade:**"
    - path: "mentor/novo-projeto.md"
      provides: "Instrucao no Passo 5 para gravar a frase de capacidade + guardrail D-04 + reforco do primeiro marco menor"
      contains: "exatamente 1 frase"
  key_links:
    - from: "mentor/novo-projeto.md Passo 5"
      to: "mentor/reference.md template PROGRESSO.md campo Capacidade:"
      via: "instrucao de preenchimento aponta para o campo do template"
      pattern: "Capacidade"
---

<objective>
Tecer o PAR DE EDICAO ACOPLADA (D-02): adicionar o campo `**Capacidade:**` na arvore de
marcos do template `PROGRESSO.md` (dono: `mentor/reference.md`) E instruir o Passo 5 de
`mentor/novo-projeto.md` a preencher esse campo com a frase de capacidade por marco. Este
e o "Stage 2 propriamente dito" no nivel de marco. Inclui o guardrail de leveza D-04 e o
reforco D-03 (primeiro marco menor) no Passo 5.

Purpose: o campo sem a instrucao = template com campo vazio que o agente ignora; a
instrucao sem o campo = agente sem lugar canonico para gravar. As duas TEM de ir juntas.
A Fase 4 (`fecha-marco.md`) CONSOME exatamente a frase `**Capacidade:**` gravada aqui — o
mastery gate vai PROCURA-la no `PROGRESSO.md`.

Output: `mentor/reference.md` com campo `**Capacidade:**` em 2 blocos de marco;
`mentor/novo-projeto.md` Passo 5 com 3 costuras (gravar a frase + guardrail + primeiro
marco menor).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-bootstrap-com-stage-2-novo-projeto-md/03-CONTEXT.md
@.planning/phases/03-bootstrap-com-stage-2-novo-projeto-md/03-RESEARCH.md
@.planning/phases/03-bootstrap-com-stage-2-novo-projeto-md/03-PATTERNS.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Formato verbo-capacidade canonico da Fase 2 (reusar IDENTICO no nivel de marco). -->
<!-- Fonte VERIFICADA: mentor/reference.md:76, :85, :294 -->

Formato canonico da frase de capacidade (nivel PASSO, em reference.md):
```markdown
Objetivo (capacidade): ao terminar, voce consegue <verbo> <conceito>
```

Estado REAL do template PROGRESSO.md hoje (mentor/reference.md:223-239, VERIFICADO 2026-06-14):
```markdown
## Marcos

### 00 -- <slug> (Walking Skeleton)  <- ATUAL

**User Story:** Como [usuario], eu quero [capacidade basica], para que [valor minimo].
**Passos do caminho:** P01-P02 (ver CAMINHO.md)
**Entregavel:** <o que o aluno VE funcionando ao fechar o marco>

- [ ] <criterio de done>

### 01 -- <slug>

**User Story:** Como [usuario], eu quero [capacidade], para que [valor].
**Passos do caminho:** P03-P05
**Entregavel:** <concreto e observavel>

- [ ] <criterio de done>
```
NAO existe campo `**Capacidade:**`. Este plano adiciona em AMBOS os blocos.

Estado REAL do Passo 5 hoje (mentor/novo-projeto.md:68-81, VERIFICADO 2026-06-14):
```markdown
## Passo 5 — Passe 2: montagem dos marcos

So agora agrupe os passos em marcos:

- Cada marco = sequencia de passos **consecutivos** do caminho que, juntos, entregam
  algo que roda/renderiza (**fatia vertical**, nunca tema).
- Aplique a regra de granularidade de `mentor/reference.md` (sessao de ~30-90 min).
- Enquadre cada marco como **User Story**: "Como [usuario], eu quero [capacidade],
  para que [beneficio]". Historia grande demais -> `/spidr-split` ANTES de seguir.
- Traduza a barra de qualidade numa **Definition of Done** objetiva, distribuida
  pelos marcos.
- Rode a **verificacao de coesao** de `mentor/reference.md` (checklist mecanico)...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar campo Capacidade: no template PROGRESSO.md (reference.md) — Seam 4 / D-02</name>
  <files>mentor/reference.md</files>
  <read_first>
    - mentor/reference.md (LER as linhas 218-247 para ver o estado real dos 2 blocos de marco antes de editar; o offset pode ter mudado, confirme pela ancora textual `### 00 -- <slug> (Walking Skeleton)` e `### 01 -- <slug>`)
    - mentor/reference.md linhas 76, 85, 294 (formato verbo-capacidade canonico a reusar)
    - .planning/phases/03-bootstrap-com-stage-2-novo-projeto-md/03-PATTERNS.md (Seam 4, "PAR DE EDICAO ACOPLADA", anti-leak no campo)
    - .planning/codebase/CONVENTIONS.md (portugues sem acentos; campos em **negrito**)
  </read_first>
  <action>
    Editar `mentor/reference.md`, secao `## Marcos` do template PROGRESSO.md. Edicao 100%
    ADITIVA: inserir UMA linha `**Capacidade:**` LOGO ABAIXO da linha `**User Story:**` em
    AMBOS os blocos de marco. NAO reescrever User Story, Passos do caminho, Entregavel ou
    done — so inserir a nova linha entre User Story e Passos do caminho.

    Bloco marco 00 — DEPOIS da edicao deve ficar EXATAMENTE:
    ```markdown
    ### 00 -- <slug> (Walking Skeleton)  <- ATUAL

    **User Story:** Como [usuario], eu quero [capacidade basica], para que [valor minimo].
    **Capacidade:** ao terminar, voce consegue <verbo> <conceito>
    **Passos do caminho:** P01-P02 (ver CAMINHO.md)
    **Entregavel:** <o que o aluno VE funcionando ao fechar o marco>

    - [ ] <criterio de done>
    ```

    Bloco marco 01 — DEPOIS da edicao deve ficar EXATAMENTE:
    ```markdown
    ### 01 -- <slug>

    **User Story:** Como [usuario], eu quero [capacidade], para que [valor].
    **Capacidade:** ao terminar, voce consegue <verbo> <conceito>
    **Passos do caminho:** P03-P05
    **Entregavel:** <concreto e observavel>

    - [ ] <criterio de done>
    ```

    ANTI-LEAK (CONS-01) — OBRIGATORIO: o `PROGRESSO.md` e artefato lido pelo ALUNO. O valor
    placeholder do campo e SO o verbo de capacidade puro: literalmente
    `ao terminar, voce consegue <verbo> <conceito>`. PROIBIDO escrever "Bloom", "Stage 2",
    "maestria", "backward design" ou qualquer jargao de framework dentro deste bloco
    `markdown` do template. O rotulo "Capacidade" e neutro (o `CAMINHO.md` ja usa
    "Objetivo (capacidade)") — sem vazamento.

    NAO criar nova secao "## Stage 2"; NAO transformar o campo em lista/rubrica. E UMA linha
    por marco, identica em formato a `reference.md:76`.
  </action>
  <verify>
    <automated>grep -c "^\*\*Capacidade:\*\* ao terminar, voce consegue" mentor/reference.md  # deve retornar 2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Capacidade:" mentor/reference.md` retorna match (>= 2 ocorrencias na secao Marcos)
    - `grep -c "^\*\*Capacidade:\*\* ao terminar, voce consegue" mentor/reference.md` retorna exatamente `2` (um por bloco de marco)
    - `awk '/^## Marcos/{f=1;next} /^## /{f=0} f' mentor/reference.md | grep -n "Bloom\|Stage 2\|maestria\|backward design"` NAO retorna nenhum match (anti-leak verde: jargao proibido SOMENTE dentro do bloco `## Marcos`; ocorrencias pre-existentes em outras secoes de `reference.md` sao irrelevantes e nao devem falhar a task)
    - Cada linha `**Capacidade:**` aparece imediatamente apos a linha `**User Story:**` correspondente (verificavel por leitura dos 2 blocos)
    - As linhas `**User Story:**`, `**Passos do caminho:**`, `**Entregavel:**` e `- [ ] <criterio de done>` permanecem inalteradas em ambos os blocos (edicao aditiva, nenhum bullet reescrito)
  </acceptance_criteria>
  <done>O template PROGRESSO.md tem `**Capacidade:** ao terminar, voce consegue <verbo> <conceito>` colado abaixo da User Story nos 2 blocos de marco; zero jargao no bloco; demais campos intactos.</done>
</task>

<task type="auto">
  <name>Task 2: Instruir o Passo 5 a gravar a frase de capacidade + guardrail D-04 + primeiro marco menor (novo-projeto.md) — Seam 2 / D-01 marco + D-04 + D-03 reforco</name>
  <files>mentor/novo-projeto.md</files>
  <read_first>
    - mentor/novo-projeto.md (LER as linhas 68-81 para ver o Passo 5 real; ancore-se no texto `## Passo 5 — Passe 2: montagem dos marcos` e nos bullets existentes)
    - mentor/reference.md (confirmar que o campo `**Capacidade:**` JA foi adicionado na Task 1 — a instrucao do Passo 5 aponta para ele)
    - .planning/phases/03-bootstrap-com-stage-2-novo-projeto-md/03-PATTERNS.md (Seam 2; Pattern 3 dois registros dois publicos; Guardrail D-04 texto literal)
    - .planning/phases/03-bootstrap-com-stage-2-novo-projeto-md/03-CONTEXT.md (D-01 marco, D-03, D-04; exemplo de arvore de marcos)
    - .planning/codebase/CONVENTIONS.md (tom imperativo, segunda pessoa; sem acentos; backticks)
  </read_first>
  <action>
    Editar `mentor/novo-projeto.md`, dentro do `## Passo 5 — Passe 2: montagem dos marcos`.
    TRES costuras, todas ADITIVAS (nao reescrever bullets existentes):

    1. D-01 MARCO (Stage 2) — inserir um BULLET NOVO logo APOS o bullet da User Story
       (o bullet que hoje termina em "`/spidr-split` ANTES de seguir."). As duas nascem
       coladas. Texto EXATO a inserir:
       ```markdown
    - Junto da User Story, escreva no `PROGRESSO.md` a **frase de capacidade** daquele
      marco, no formato `ao terminar, voce consegue <verbo> <conceito>` (campo
      `**Capacidade:**` da arvore de marcos do template em `mentor/reference.md`). E a
      evidencia de maestria do marco (backward design: defina a capacidade-alvo antes de
      detalhar os passos). O que voce GRAVA no `PROGRESSO.md` e so o verbo de capacidade
      puro — sem nomes de framework (o aluno le esse arquivo).
       ```
       Observacao ao executor: a INSTRUCAO acima (lida pelo AGENTE) PODE nomear "backward
       design"; a FRASE RESULTANTE no `PROGRESSO.md` so tem "ao terminar, voce consegue...".
       Os dois registros sao distintos (Pattern 3 anti-leak). NAO transforme a interpretacao
       do ROADMAP "entre caminho e marcos" numa posicao fisica: e backward design CONCEITUAL
       (capacidade definida junto ao marco), gravada aqui no Passo 5 onde a User Story nasce.

    2. D-04 GUARDRAIL (leveza, teto rigido) — inserir, logo APOS o bullet do item 1, um
       bullet com o texto literal:
       ```markdown
    - **Teto rigido:** exatamente 1 frase de capacidade por marco. Se virou lista, rubrica
      ou sub-doc, esta errado — corte. Obrigatorio por marco (o gate de fechamento depende
      dessa frase existir), nunca opcional.
       ```

    3. D-03 REFORCO (ENG-02, primeiro marco menor) — inserir, junto da regra de
       granularidade (o bullet que hoje diz "Aplique a regra de granularidade de
       `mentor/reference.md` (sessao de ~30-90 min).") OU como bullet imediatamente apos
       ele, o texto:
       ```markdown
    - Dimensione o **primeiro marco** (Walking Skeleton) como o MENOR possivel: a primeira
      vitoria precisa vir rapido (ver Passo 8, "primeiro done leve").
       ```

    Mantenha o estilo de bullet imperativo ja usado nos demais itens do Passo 5. NAO criar
    secao "## Stage 2" nova. NAO mexer no bullet de Definition of Done nem no de verificacao
    de coesao. Portugues sem acentos; identificadores em backticks.
  </action>
  <verify>
    <automated>grep -q "exatamente 1 frase de capacidade por marco" mentor/novo-projeto.md && grep -q "frase de capacidade" mentor/novo-projeto.md && grep -q "primeiro marco" mentor/novo-projeto.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "exatamente 1 frase" mentor/novo-projeto.md` retorna match (guardrail D-04 presente)
    - `grep -n "frase de capacidade" mentor/novo-projeto.md` retorna match dentro do Passo 5 (instrucao D-01 marco presente)
    - `grep -n "ao terminar, voce consegue" mentor/novo-projeto.md` retorna match (formato canonico citado na instrucao)
    - `grep -n "primeiro marco" mentor/novo-projeto.md` retorna match (reforco D-03 presente)
    - `grep -n "backward design" mentor/novo-projeto.md` retorna match no Passo 5 (a INSTRUCAO ao agente nomeia o framework — permitido na prosa lida pelo agente)
    - Leitura: a instrucao manda gravar a frase no `PROGRESSO.md` colada a User Story, e diz explicitamente que o valor gravado e so o verbo puro sem jargao (anti-leak)
    - Nenhum bullet pre-existente do Passo 5 foi reescrito (edicao aditiva): User Story, Definition of Done e verificacao de coesao intactos
    - Nenhuma secao "## Stage 2" foi criada (`grep -n "## Stage 2" mentor/novo-projeto.md` NAO retorna match)
  </acceptance_criteria>
  <done>O Passo 5 instrui gravar a frase de capacidade por marco no `PROGRESSO.md` (formato verbo-capacidade puro, anti-leak explicitado), carrega o guardrail literal de 1 frase (D-04) e o reforco do primeiro marco menor (D-03); nenhum bullet existente reescrito.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nenhuma | Fase de edicao de documentacao markdown estatica em `mentor/`. Sem entrada nao-confiavel, sem execucao de codigo, sem rede, sem dados de usuario, sem autenticacao. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure (analogo) | Campo `**Capacidade:**` no template `PROGRESSO.md` (lido pelo aluno) | mitigate | Anti-leak CONS-01: o valor do campo e so o verbo de capacidade puro; jargao de framework ("Bloom", "Stage 2", "backward design", "maestria") proibido no bloco lido pelo aluno. Verificado por `grep` negativo na secao ## Marcos. NAO e ameaca de seguranca classica — e o unico analogo relevante neste dominio de doc estatico. |

**Nota:** Sem superficie de ataque relevante (edicao de doc estatico). ASVS V2-V6 nao se
aplicam. Nenhuma ameaca high/medium. O unico item acima e o controle anti-leak (vazamento
de jargao teorico ao aluno), tratado como analogo de disclosure por completude.
</threat_model>

<verification>
- `grep -c "^\*\*Capacidade:\*\* ao terminar, voce consegue" mentor/reference.md` == 2
- `grep -n "exatamente 1 frase" mentor/novo-projeto.md` retorna match
- `grep -n "frase de capacidade" mentor/novo-projeto.md` retorna match
- `grep -n "primeiro marco" mentor/novo-projeto.md` retorna match
- Anti-leak (auto-contido): `awk '/^## Marcos/{f=1;next} /^## /{f=0} f' mentor/reference.md | grep "Bloom\|Stage 2\|maestria\|backward design"` nao retorna nada
- (Opcional, se a Fase 2 ja rodou) Smoke-test anti-leak da Fase 2 (extrator de blocos cercados sobre `reference.md`) verde
- Diff NAO toca `.claude/` nem `AGENTS.md` (fonte-unica)
</verification>

<success_criteria>
- Template PROGRESSO.md em `reference.md` tem campo `**Capacidade:**` (formato verbo-capacidade) em ambos os blocos de marco, sem jargao
- Passo 5 de `novo-projeto.md` instrui gravar a frase por marco, carrega o guardrail D-04 (1 frase) e o reforco D-03 (primeiro marco menor)
- PAR ACOPLADO satisfeito: campo + instrucao existem juntos
- Edicao 100% aditiva; nenhum bullet/campo pre-existente reescrito; nenhuma secao "## Stage 2" criada
- Cobre EST-03 (Criterios 1 e 2 do ROADMAP) e prepara o contrato que a Fase 4 consome
</success_criteria>

<output>
After completion, create `.planning/phases/03-bootstrap-com-stage-2-novo-projeto-md/03-01-SUMMARY.md`
</output>
