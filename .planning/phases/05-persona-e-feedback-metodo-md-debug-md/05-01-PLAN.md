---
phase: 05-persona-e-feedback-metodo-md-debug-md
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/05-persona-e-feedback-metodo-md-debug-md/scripts/extract-fenced.sh
  - .planning/phases/05-persona-e-feedback-metodo-md-debug-md/scripts/check-phase5.sh
autonomous: true
requirements: [AVAL-05, AVAL-06, CONS-01]
nyquist_compliant: true
wave_0: true

must_haves:
  truths:
    - "Rodar check-phase5.sh produz uma saida PASS/FAIL deterministica para cada um dos V-01..V-18 e sai com codigo 0 quando tudo passa"
    - "Antes das edicoes (baseline), os checks POSITIVOS V-01..V-15 falham; V-16 (ANCHOR-RESOLVE), V-17 e V-18 (anti-leak baseline 0) passam"
    - "O anti-leak (V-17) roda SO sobre o conteudo de blocos cercados (via extract-fenced.sh), nunca sobre a prosa do doc inteiro"
  artifacts:
    - path: ".planning/phases/05-persona-e-feedback-metodo-md-debug-md/scripts/extract-fenced.sh"
      provides: "Filtro stdin->stdout que imprime so o conteudo dentro de blocos cercados ```"
      contains: "inside = !inside"
    - path: ".planning/phases/05-persona-e-feedback-metodo-md-debug-md/scripts/check-phase5.sh"
      provides: "Gate estatico PASS/FAIL que codifica V-01..V-18"
      contains: "fails"
  key_links:
    - from: "check-phase5.sh"
      to: "extract-fenced.sh"
      via: "sh \"$DIR/extract-fenced.sh\" \"$MET\" | rg -c \"$LEAK_PAT\""
      pattern: "extract-fenced\\.sh"
    - from: "check-phase5.sh V-16"
      to: "mentor/reference.md, mentor/tutor.md, mentor/fecha-marco.md"
      via: "rg -q do heading literal (nao slug reconstruido)"
      pattern: "Sintaxe nova de verdade"
---

<objective>
Clonar o harness de verificacao estatica da Fase 4 para a Fase 5: um extrator de blocos
cercados (`extract-fenced.sh`, clone byte-a-byte) e um runner PASS/FAIL (`check-phase5.sh`)
que codifica TODOS os checks V-01..V-18 do contrato de validacao. Este plano e Wave 0 — ele
DEVE existir e rodar antes dos dois planos de edicao (05-02, 05-03) para que cada edicao seja
verificavel por grep deterministico.

Purpose: Sem o harness, as edicoes de prosa nao teriam prova objetiva de que entregam
AVAL-05, AVAL-06, CONS-01 e o Criterio de Sucesso 4. O harness e o unico "codigo" da fase e
e um padrao ja provado em 3 fases (2, 4) — clonar, nao reinventar.
Output: dois scripts shell em `.planning/phases/05-.../scripts/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-persona-e-feedback-metodo-md-debug-md/05-RESEARCH.md
@.planning/phases/05-persona-e-feedback-metodo-md-debug-md/05-VALIDATION.md
@.planning/phases/05-persona-e-feedback-metodo-md-debug-md/05-PATTERNS.md

# Analogs a clonar (LER antes de escrever):
@.planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/scripts/extract-fenced.sh
@.planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/scripts/check-phase4.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clonar extract-fenced.sh byte-a-byte para o scripts dir da Fase 5</name>
  <read_first>
    - .planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/scripts/extract-fenced.sh (a fonte a clonar — script generico de 26 linhas)
    - .planning/phases/05-persona-e-feedback-metodo-md-debug-md/05-PATTERNS.md (secao "extract-fenced.sh (CREATE — clone verbatim)", linhas 24-64)
  </read_first>
  <files>.planning/phases/05-persona-e-feedback-metodo-md-debug-md/scripts/extract-fenced.sh</files>
  <action>
    Crie o diretorio `.planning/phases/05-persona-e-feedback-metodo-md-debug-md/scripts/` se
    nao existir. Copie o conteudo de
    `.planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/scripts/extract-fenced.sh`
    BYTE-A-BYTE para
    `.planning/phases/05-persona-e-feedback-metodo-md-debug-md/scripts/extract-fenced.sh`.
    NAO altere a logica awk (`/^[[:space:]]*```/ { inside = !inside; next } inside { print }`).
    O default de arquivo (`mentor/reference.md`) e inofensivo — `check-phase5.sh` sempre passa
    um arg explicito. Use `cp` ou leia-e-escreva o conteudo identico; nao reescreva a mao.
    Garanta que o arquivo continua executavel (`chmod +x` ou invoque via `sh`).
  </action>
  <verify>
    <automated>sh .planning/phases/05-persona-e-feedback-metodo-md-debug-md/scripts/extract-fenced.sh mentor/metodo.md | head -1 && diff .planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/scripts/extract-fenced.sh .planning/phases/05-persona-e-feedback-metodo-md-debug-md/scripts/extract-fenced.sh && echo IDENTICO</automated>
  </verify>
  <acceptance_criteria>
    - `diff` entre o extract-fenced.sh da Fase 4 e o da Fase 5 retorna vazio (clone byte-a-byte) e imprime `IDENTICO`.
    - Rodar `sh .../scripts/extract-fenced.sh mentor/metodo.md` imprime SO o conteudo dentro de blocos cercados (ex: a linha `Como [tipo de usuario]...` do bloco da User Story), sem as cercas ```.
    - `rg -q 'inside = !inside' .../scripts/extract-fenced.sh` casa (logica awk preservada).
  </acceptance_criteria>
  <done>O extrator existe no scripts dir da Fase 5, e identico ao da Fase 4, e isola corretamente blocos cercados.</done>
</task>

<task type="auto">
  <name>Task 2: Autorar check-phase5.sh com os scaffolds clonados + os 18 checks V-01..V-18</name>
  <read_first>
    - .planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/scripts/check-phase4.sh (scaffold a clonar: setup de vars, helpers count/check_min/check_zero/check_has, ANCHOR-RESOLVE, anti-leak, tail)
    - .planning/phases/05-persona-e-feedback-metodo-md-debug-md/05-RESEARCH.md (secao "Validation Architecture" tabela V-01..V-18 + secao "Code Examples" — corpos autoritativos dos checks)
    - .planning/phases/05-persona-e-feedback-metodo-md-debug-md/05-PATTERNS.md (secao "check-phase5.sh", linhas 68-198 — os blocos exatos a copiar)
    - .planning/phases/05-persona-e-feedback-metodo-md-debug-md/05-VALIDATION.md (Per-Task Verification Map — confirma cada V-row)
  </read_first>
  <files>.planning/phases/05-persona-e-feedback-metodo-md-debug-md/scripts/check-phase5.sh</files>
  <action>
    Crie `check-phase5.sh` clonando o SCAFFOLD de `check-phase4.sh` (mantenha verbatim o setup
    de `fails=0` e os tres helpers `count()`, `check_min()`, `check_zero()`, `check_has()`),
    trocando SO as vars de path para:
      `DIR=$(dirname "$0")`, `MET="mentor/metodo.md"`, `DBG="mentor/debug.md"`,
      `FUN="mentor/fundamentos.md"`, `REF="mentor/reference.md"`,
      `TUT="mentor/tutor.md"`, `FCH="mentor/fecha-marco.md"`.

    Codifique os 18 checks LITERALMENTE como abaixo (idiom `check_min "LABEL" "$(count 'PAT' FILE)" MIN`):

    # CONS-01 (metodo.md):
    check_min "V-01-link"    "$(count 'fundamentos\.md' "$MET")" 1
    check_min "V-02-corpo"   "$(count 'jargao|nunca .*ao aluno|nunca .*citad' "$MET")" 1
    check_min "V-03-fund03"  "$(count 'interno|nunca .*lido pelo aluno|injetad' "$MET")" 1
    check_min "V-04-espelho" "$(count 'jargao.*aluno|framework.*aluno' "$MET")" 1
    # AVAL-05 (metodo.md):
    check_min "V-05-autoexp" "$(count 'auto-explicacao|me explica por que|explicar.*antes de curar' "$MET")" 1
    check_min "V-06-guard"   "$(count 'exatamente 1|checklist ou rubrica|esta errado' "$MET")" 1
    check_min "V-07-front"   "$(count 'fecha-marco' "$MET")" 1
    # AVAL-06 (debug.md):
    check_min "V-08-lentes"  "$(count 'feed-up|feed-back|feed-forward' "$DBG")" 3
    check_min "V-09-pista"   "$(count 'PISTA.*feed-forward|feed-forward.*PISTA|PISTA.*sem .*resposta' "$DBG")" 1
    check_min "V-10-6passos" "$(count '^### [1-6]\.' "$DBG")" 6
    # Criterio 4 (metodo.md):
    check_min "V-11-grr"     "$(count 'reference\.md' "$MET")" 1
    check_min "V-12-retr"    "$(count 'tutor\.md|/tutor' "$MET")" 1
    check_min "V-13-gate"    "$(count 'fecha-marco\.md|/fecha-marco' "$MET")" 1
    # V-14: os 5 itens do ciclo preservados. Conte os itens numerados DENTRO da secao
    #   "Conduta por marco (ciclo)" (do heading ate o proximo "## "). Use awk para isolar a
    #   secao e contar linhas que comecam com "[1-5]." (== 5). Idiom sugerido:
    #     c14=$(awk '/^## Conduta por marco \(ciclo\)/{f=1;next} /^## /{f=0} f && /^[1-5]\./{n++} END{print n+0}' "$MET")
    #     check_min "V-14-5itens" "$c14" 5   # (preservacao: exatamente 5; >=5 aceita, mas a edicao e inline)
    # D-07 (metodo.md):
    check_min "V-15-ptr"     "$(count 'feedback tri-partido' "$MET")" 1

    # V-16 ANCHOR-RESOLVE (heading LITERAL, nunca slug reconstruido — A1/Pitfall 4):
    if rg -q '^### Sintaxe nova de verdade' "$REF" 2>/dev/null && \
       rg -q '^## Passo 1 — Recuperacao ativa' "$TUT" 2>/dev/null && \
       rg -q '^## Passo 1 — Mastery gate' "$FCH" 2>/dev/null; then
      printf 'PASS  %-16s as 3 ancoras D-09 resolvem\n' "V-16"
    else
      printf 'FAIL  %-16s ancora D-09 ausente\n' "V-16"; fails=$((fails+1))
    fi

    # V-17 ANTI-LEAK (so blocos cercados; LEAK_PAT estendido com jargao da Fase 5; FRASE
    #   quando o token solto e ambiguo — 'retrieval practice' nao 'retrieval'):
    LEAK_PAT='SDT|mastery|spacing|backward design|Bloom|GRR|Mayer|retrieval practice|testing effect|feed-up|feed-back|feed-forward|Hattie|formativ|auto-explicacao'
    leak_met=$(sh "$DIR/extract-fenced.sh" "$MET" | rg -c "$LEAK_PAT" 2>/dev/null || echo 0)
    leak_dbg=$(sh "$DIR/extract-fenced.sh" "$DBG" | rg -c "$LEAK_PAT" 2>/dev/null || echo 0)
    check_zero "V-17-leak-met" "$leak_met"
    check_zero "V-17-leak-dbg" "$leak_dbg"

    # V-18 defensivo (anti-drift P12): jargao novo NAO vaza para adaptadores. Conte sobre
    #   AGENTS.md e .claude/ e exija 0. Use um glob defensivo que nao quebre se .claude/ tiver
    #   subpastas; se rg nao achar arquivos, count -> 0 (ok). Idiom:
    #     leak_adapt=$(rg -c 'feed-up|feed-back|feed-forward|Hattie' AGENTS.md .claude/ 2>/dev/null | awk -F: '{s+=$NF} END{print s+0}')
    #     check_zero "V-18-adapt" "$leak_adapt"

    # Tail (clone verbatim):
    echo "== $fails fail(s) =="
    exit $fails

    Mantenha o estilo do scaffold (portugues sem acentos nos comentarios). NAO invente checks
    fora de V-01..V-18.
  </action>
  <verify>
    <automated>sh .planning/phases/05-persona-e-feedback-metodo-md-debug-md/scripts/check-phase5.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - O script roda sem erro de sintaxe shell e imprime uma linha PASS/FAIL para cada um de V-01..V-18 (mais V-17 dividido em leak-met/leak-dbg) e a cauda `== N fail(s) ==`.
    - BASELINE (antes das edicoes 05-02/05-03): os checks POSITIVOS V-01..V-15 FALHAM (esperado); V-16 PASSA (3 headings ja existem — confirmado: reference.md:406, tutor.md:37, fecha-marco.md:12); V-17 (leak-met e leak-dbg) PASSAM (baseline 0); V-18 PASSA (baseline 0). Ou seja, exit code != 0 no baseline por causa dos positivos — isso e o esperado e CORRETO para Wave 0.
    - `rg -q 'Sintaxe nova de verdade' check-phase5.sh` e `rg -q 'LEAK_PAT' check-phase5.sh` casam (ANCHOR-RESOLVE e anti-leak codificados).
    - `rg -q 'extract-fenced\.sh' check-phase5.sh` casa (V-17 consome o extrator do Task 1).
    - O LEAK_PAT inclui os termos novos da Fase 5: `feed-up`, `feed-back`, `feed-forward`, `Hattie`, `formativ`, `auto-explicacao` (grep desses literais no script).
  </acceptance_criteria>
  <done>check-phase5.sh existe, roda, codifica V-01..V-18, e no baseline so os positivos falham (V-16/V-17/V-18 passam).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Fase de documentacao/script estatico — sem runtime, rede, input nao-confiavel, auth ou cripto. Os scripts shell rodam SO localmente sobre arquivos versionados do repo (read-only grep/awk). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | (N/A) | scripts/extract-fenced.sh, scripts/check-phase5.sh | accept | Sem superficie de ataque: scripts de verificacao read-only que processam markdown local com `rg`/`awk`. Sem entrada externa, sem escrita de estado, sem rede. ASVS L1 nao se aplica (mesma conclusao das Fases 2 e 4). A unica preocupacao e editorial (anti-leak CONS-01 / anti-drift P12), ja coberta por V-17 e V-18. |
</threat_model>

<verification>
- `diff` extract-fenced.sh (Fase 4 vs Fase 5) == vazio.
- `sh scripts/check-phase5.sh` roda e imprime 18 checks + cauda.
- Baseline: positivos falham, V-16/V-17/V-18 passam.
</verification>

<success_criteria>
- Os dois scripts existem em `.planning/phases/05-.../scripts/`.
- `check-phase5.sh` codifica todos os V-01..V-18 (verificavel por grep dos labels).
- O anti-leak (V-17) consome `extract-fenced.sh` (so blocos cercados).
- Baseline coerente com a convencao das Fases 2/4 (positivos red, anti-leak/anchor green).
</success_criteria>

<output>
After completion, create `.planning/phases/05-persona-e-feedback-metodo-md-debug-md/05-01-SUMMARY.md`
</output>
