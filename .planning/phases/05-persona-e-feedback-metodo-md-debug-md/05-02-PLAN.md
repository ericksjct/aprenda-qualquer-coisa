---
phase: 05-persona-e-feedback-metodo-md-debug-md
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - mentor/metodo.md
autonomous: true
requirements: [CONS-01, AVAL-05]
nyquist_compliant: true

must_haves:
  truths:
    - "metodo.md declara como regra (corpo) que a fundamentacao guia o agente mas o jargao de framework NUNCA e citado ao aluno na sessao, com link para fundamentos.md como o porque, e essa mesma regra carrega o lembrete FUND-03 (doc interno, nunca lido pelo aluno)"
    - "A regra anti-leak tem um espelho curto na lista 'Anti-padroes (NUNCA faca)'"
    - "O gate de curadoria ganha um novo passo de 1 pergunta de auto-explicacao ANTES de abrir as 1-3 melhorias, com guardrail rigido 'exatamente 1 pergunta' e nota de fronteira apontando fecha-marco.md"
    - "O ciclo por marco nomeia inline GRR 3 fases (link reference.md), retrieval (link tutor.md) e mastery gate (link fecha-marco.md), preservando os 5 itens existentes"
    - "O resumo de forense em metodo.md continua um ponteiro curto que nomeia 'feedback tri-partido' + link /debug, sem copiar a definicao dos 3 termos"
    - "Nenhum jargao de framework aparece dentro de bloco cercado de metodo.md (anti-leak so atinge artefato do aluno; a prosa pode nomear frameworks)"
  artifacts:
    - path: "mentor/metodo.md"
      provides: "Persona com regra anti-leak (CONS-01), check formativo (AVAL-05), ciclo nomeado (C4) e ponteiro forense (D-07)"
      contains: "fundamentos.md"
  key_links:
    - from: "mentor/metodo.md (corpo, apos Regra de ouro)"
      to: "mentor/fundamentos.md"
      via: "primeira referencia de metodo.md a fundamentos.md (o porque teorico)"
      pattern: "fundamentos\\.md"
    - from: "mentor/metodo.md (Conduta por marco, passo 3)"
      to: "mentor/reference.md (Sintaxe nova de verdade / GRR)"
      via: "link nomeando GRR 3 fases"
      pattern: "reference\\.md"
    - from: "mentor/metodo.md (abertura do ciclo)"
      to: "mentor/tutor.md (Passo 1 — Recuperacao ativa)"
      via: "link nomeando retrieval"
      pattern: "tutor\\.md|/tutor"
    - from: "mentor/metodo.md (gate de curadoria / fechamento)"
      to: "mentor/fecha-marco.md (Passo 1 — Mastery gate)"
      via: "link nomeando mastery gate + nota de fronteira"
      pattern: "fecha-marco"
---

<objective>
Editar `mentor/metodo.md` (a persona/conduta, lida pelo AGENTE — nunca pelo aluno) para
tecer quatro fios travados em CONTEXT.md, todos por inserto ancorado em headings literais
existentes, SEM reestruturar o doc:
- CONS-01 (D-01/D-02): regra anti-leak no corpo (1a ponte a `fundamentos.md` + lembrete
  FUND-03) + espelho curto na lista "Anti-padroes (NUNCA faca)".
- AVAL-05 (D-03/D-04): novo passo de auto-explicacao no gate de curadoria + guardrail
  rigido "exatamente 1 pergunta" + nota de fronteira para `fecha-marco.md`.
- Criterio 4 (D-08/D-09): anotar inline 3 etapas do ciclo (GRR->reference.md,
  retrieval->tutor.md, gate->fecha-marco.md), preservando os 5 itens.
- D-07: o resumo de forense fica ponteiro curto nomeando "feedback tri-partido" + link
  `/debug` (sem copiar a definicao tri-partida que mora so em debug.md).

Purpose: entrega CONS-01, AVAL-05 e o Criterio de Sucesso 4 da Fase 5. metodo.md hoje NAO
referencia fundamentos.md em lugar nenhum — D-01 cria essa primeira ponte.
Output: `mentor/metodo.md` editado; check-phase5.sh V-01..V-07, V-11..V-15, V-17 verdes.

Depende do harness do plano 05-01 (Wave 0) para verificacao por grep. Roda em paralelo com
05-03 (arquivo disjunto: debug.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-persona-e-feedback-metodo-md-debug-md/05-CONTEXT.md
@.planning/phases/05-persona-e-feedback-metodo-md-debug-md/05-RESEARCH.md
@.planning/phases/05-persona-e-feedback-metodo-md-debug-md/05-PATTERNS.md

# Doc a editar + doc-dono cujo link e criado aqui (NUNCA redefinir, so linkar):
@mentor/metodo.md
@mentor/fundamentos.md

<interfaces>
<!-- Headings literais (ancoras de edicao) confirmados no estado atual do repo -->
metodo.md (headings de ancora):
  - "## Regra de ouro"                       (corpo: inserir a regra anti-leak D-01/D-02 logo apos esta secao)
  - "## Conduta por marco (ciclo)"           (5 itens numerados; passo 3 ja diz inverta para "eu faco -> voce faz")
  - "## Gate de curadoria — so depois de funcionar"  (3 passos; inserir o novo passo formativo entre o 2 e o 3)
  - "## Protocolo de forense (quando o aluno erra)"  (resumo 6 passos + linha "travado ha mais de 10 minutos" — D-07 mantem curto)
  - "## Fechamento de marco"                 (menciona /fecha-marco — ancora natural do nome "mastery gate")
  - "## Anti-padroes (NUNCA faca)"           (lista; espelho curto de D-01)

Docs-donos (LINK only — heading literal a citar; o ANCHOR-RESOLVE do harness grepa estes):
  - mentor/fundamentos.md       : preambulo de doc interno (FUND-03) + tabela "Frameworks supporting"
  - mentor/reference.md   :406  ### Sintaxe nova de verdade: "eu faco -> nos fazemos -> voce faz"   (dono da sintaxe GRR)
  - mentor/tutor.md       :37   ## Passo 1 — Recuperacao ativa (antes do recap)                     (dono do retrieval)
  - mentor/fecha-marco.md :12   ## Passo 1 — Mastery gate: verificacao do "done"                    (dono do mastery gate por marco)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CONS-01 — regra anti-leak no corpo (D-01/D-02) + espelho na lista de anti-padroes</name>
  <read_first>
    - mentor/metodo.md (integral — confirmar "## Regra de ouro" e "## Anti-padroes (NUNCA faca)")
    - mentor/fundamentos.md (preambulo de doc interno = FUND-03; nao redefinir, so confirmar que o link aponta para um doc interno)
    - .planning/phases/03-bootstrap-com-stage-2-novo-projeto-md/03-CONTEXT.md (nuance anti-leak: a PROSA pode nomear frameworks; o anti-leak governa o que o agente DIZ AO ALUNO)
    - .planning/phases/05-persona-e-feedback-metodo-md-debug-md/05-PATTERNS.md (Edit anchor 1 e 2; "Mirror-rule" pattern)
  </read_first>
  <files>mentor/metodo.md</files>
  <action>
    D-01 corpo (regra positiva, 1a ponte a fundamentos.md): Insira uma regra curta NOVA logo
    APOS a secao "## Regra de ouro" — ponto mais cedo onde o agente le. A regra declara: a
    fundamentacao guia a conduta do agente, ele APLICA as boas praticas, mas NUNCA cita o nome
    do framework ao aluno na sessao; o porque teorico (interno) esta em
    [fundamentos.md](fundamentos.md). D-02 (FUND-03 no mesmo lugar): a mesma regra carrega o
    lembrete de que fundamentos.md e doc INTERNO do agente, jamais lido pelo aluno nem injetado
    na sessao. Forma sugerida (texto e Claude's discretion; portugues SEM acentos):
      "A fundamentacao guia a sua conduta, nunca vira conteudo: voce APLICA as boas praticas,
      mas NUNCA cita o nome do framework ao aluno na sessao. O porque teorico (interno) esta
      em [fundamentos.md](fundamentos.md) — doc do agente, jamais lido pelo aluno nem injetado
      na sessao."
    Esta e a PRIMEIRA referencia de metodo.md a fundamentos.md (hoje nao existe nenhuma).

    D-01 espelho (lista): Na secao "## Anti-padroes (NUNCA faca)", adicione UM item imperativo
    curto, no mesmo estilo terso dos bullets atuais (ex: "- Preencher o TODO(human) pelo
    aluno."). Forma sugerida:
      "- Citar jargao de framework (Bloom, CLT, retrieval...) ao aluno na sessao — a teoria
        guia voce, nao e despejada nele."
    NAO duplique o porque/link no espelho — o espelho e so o eco imperativo curto.

    IMPORTANTE (anti-leak / Pitfall 3): esta regra e PROSA lida pelo agente — ela PODE e DEVE
    nomear frameworks e linkar fundamentos.md. NAO coloque nenhum desses exemplos de jargao
    dentro de um bloco cercado de tres crases (isso e o que vira artefato do aluno e o V-17
    grepa). Mantenha a regra e o espelho como prosa/bullets normais.
  </action>
  <verify>
    <automated>rg -n "fundamentos\.md" mentor/metodo.md && rg -n "jargao|nunca .*ao aluno|nunca .*citad" mentor/metodo.md && rg -n "interno|nunca .*lido pelo aluno|injetad" mentor/metodo.md</automated>
  </verify>
  <acceptance_criteria>
    - V-01: rg -c "fundamentos\.md" mentor/metodo.md >= 1 (1a ponte criada).
    - V-02: rg -c "jargao|nunca .*ao aluno|nunca .*citad" mentor/metodo.md >= 1 (regra anti-leak no corpo).
    - V-03: rg -c "interno|nunca .*lido pelo aluno|injetad" mentor/metodo.md >= 1 (FUND-03 no mesmo lugar).
    - V-04: rg -c "jargao.*aluno|framework.*aluno" mentor/metodo.md >= 1 (espelho na lista de anti-padroes).
    - V-17 (parcial): sh scripts/extract-fenced.sh mentor/metodo.md | rg -c "LEAK_PAT" continua 0 — o jargao da regra/espelho esta na PROSA, nao em bloco cercado.
    - A regra positiva e o espelho citam o jargao SO como prosa; nenhum bloco cercado novo contendo nomes de framework foi adicionado.
  </acceptance_criteria>
  <done>metodo.md tem a regra anti-leak no corpo (com link fundamentos.md + FUND-03) e o espelho curto na lista; V-01..V-04 verdes e V-17 ainda 0.</done>
</task>

<task type="auto">
  <name>Task 2: AVAL-05 — novo passo de auto-explicacao no gate de curadoria + guardrail + fronteira (D-03/D-04)</name>
  <read_first>
    - mentor/metodo.md (secao "## Gate de curadoria — so depois de funcionar": 3 passos numerados + "Ordem sagrada")
    - mentor/fecha-marco.md (Passo 1 — Mastery gate, linha 12 — alvo do link da nota de fronteira; e o estilo de guardrail "Mantenha leve... nao vire quiz nem checklist")
    - .planning/phases/05-persona-e-feedback-metodo-md-debug-md/05-PATTERNS.md (Edit anchor 3; "Rigid leveza guardrail" pattern)
  </read_first>
  <files>mentor/metodo.md</files>
  <action>
    D-03 (novo passo formativo): Na secao "## Gate de curadoria — so depois de funcionar",
    insira um NOVO passo ENTRE o passo 2 atual ("Quando o codigo funciona E passa no criterio
    de done...") e o passo 3 atual (curadoria / 1-3 melhorias) — ou seja, ANTES de abrir as
    melhorias. Renumere os passos seguintes conforme necessario (o passo 3 atual vira 4, etc.).
    O novo passo: quando o codigo funciona, o agente faz EXATAMENTE 1 pergunta de
    auto-explicacao ("me explica por que isso funciona") antes de curar; so segue para a
    curadoria se o aluno consegue explicar; se nao, e sinal de lacuna -> volta ao conceito.
    Use a frase literal "me explica por que isso funciona" e o termo "auto-explicacao" (o V-05
    grepa por isso).

    D-04 (guardrail rigido + nota de fronteira): Logo apos o novo passo (ou ao final da secao
    do gate de curadoria), escreva, em prosa, os dois literais:
      - Guardrail: "check formativo = exatamente 1 pergunta de auto-explicacao; se virou
        checklist ou rubrica, esta errado." (V-06 grepa "exatamente 1|checklist ou rubrica|esta errado")
      - Fronteira: "o gate de DOMINIO por marco (sintese/transferencia) mora em
        [fecha-marco.md](fecha-marco.md) — aqui e o micro-check do gate de curadoria por passo,
        nao o mastery gate por marco." (V-07 grepa "fecha-marco")
    NAO transforme o check em rubrica/lista de criterios (Pitfall 2). Exatamente 1 pergunta.
    Tudo isto e prosa lida pelo agente; nao colocar em bloco cercado.
  </action>
  <verify>
    <automated>rg -n "auto-explicacao|me explica por que|explicar.*antes de curar" mentor/metodo.md && rg -n "exatamente 1|checklist ou rubrica|esta errado" mentor/metodo.md && rg -n "fecha-marco" mentor/metodo.md</automated>
  </verify>
  <acceptance_criteria>
    - V-05: rg -c "auto-explicacao|me explica por que|explicar.*antes de curar" mentor/metodo.md >= 1.
    - V-06: rg -c "exatamente 1|checklist ou rubrica|esta errado" mentor/metodo.md >= 1 (guardrail literal).
    - V-07: rg -c "fecha-marco" mentor/metodo.md >= 1 (nota de fronteira aponta o dono do mastery gate por marco).
    - O novo passo aparece ANTES do passo de curadoria (1-3 melhorias) na secao "Gate de curadoria".
    - NAO ha a palavra "rubrica" ou "criterios" como INSTRUCAO (so como guardrail negativo); exatamente 1 pergunta (Pitfall 2).
  </acceptance_criteria>
  <done>O gate de curadoria tem o novo passo de auto-explicacao + guardrail rigido + nota de fronteira; V-05..V-07 verdes.</done>
</task>

<task type="auto">
  <name>Task 3: Criterio 4 — ciclo nomeado inline (D-08/D-09) + D-07 ponteiro forense curto</name>
  <read_first>
    - mentor/metodo.md (secao "## Conduta por marco (ciclo)" — 5 itens, passo 3 diz inverta para "eu faco -> voce faz"; secao "## Protocolo de forense"; secao "## Fechamento de marco")
    - mentor/reference.md (heading "### Sintaxe nova de verdade" linha 406 — dono do GRR; NAO redefinir a sintaxe, so linkar)
    - mentor/tutor.md (heading "## Passo 1 — Recuperacao ativa" linha 37 — dono do retrieval)
    - mentor/fecha-marco.md (heading "## Passo 1 — Mastery gate" linha 12 — dono do gate)
    - .planning/phases/05-persona-e-feedback-metodo-md-debug-md/05-PATTERNS.md (Edit anchor 4 e 5; nota critica sobre slug GRR fragil — A1)
  </read_first>
  <files>mentor/metodo.md</files>
  <action>
    D-08/D-09 (ciclo nomeado inline — EXATAMENTE 3 nomeacoes, sem bloco de visao geral):
    Na secao "## Conduta por marco (ciclo)", anote INLINE, nos pontos onde cada etapa JA
    ocorre, preservando os 5 itens numerados (V-14 conta == 5; nao adicione nem remova itens,
    so anote dentro deles):
      1. retrieval — na abertura do ciclo (item 1, "Conceito em DOIS canais, sempre antes da
         tentativa", ou a referencia a recuperacao antes da tentativa): nomeie "retrieval
         (recuperacao ativa)" e linke [tutor.md](tutor.md) (dono, heading "Passo 1 —
         Recuperacao ativa"). V-12 grepa "tutor.md|/tutor".
      2. GRR 3 fases — no passo 3, onde ja diz inverta para "eu faco -> voce faz": nomeie
         "GRR 3 fases (eu faco -> nos fazemos -> voce faz)" e linke [reference.md](reference.md)
         (dono da sintaxe GRR). V-11 grepa "reference.md".
      3. mastery gate — amarre ao fechamento (a secao "## Fechamento de marco" ja menciona
         /fecha-marco, OU no item do ciclo que aponta o fechamento): nomeie "mastery gate" e
         linke [fecha-marco.md](fecha-marco.md). V-13 grepa "fecha-marco\.md|/fecha-marco".
         (Esse link ja pode estar satisfeito pela nota de fronteira do Task 2 — confirme que o
         ciclo/fechamento tambem nomeia o gate.)
    ESCOPO FECHADO: NAO nomeie backward design nem "primeiro done leve" (rejeitado D-09 — nao
    transformar o ciclo em indice). Exatamente 3 nomeacoes com link.
    Sobre o slug GRR: linke ao doc/heading reference.md; o slug exato (com dois-pontos, aspas e
    setas) e fragil (A1) — sera confirmado por 1 clique manual no gate da fase. Nao tente
    reconstruir o slug aqui; um link a reference.md (com ou sem ancora de secao) satisfaz V-11.

    D-07 (ponteiro forense curto): Na secao "## Protocolo de forense (quando o aluno erra)",
    mantenha o resumo CURTO; adicione NO MAXIMO 1 linha nomeando "feedback tri-partido" + link
    /debug (ex: "Esse protocolo e um ciclo de feedback tri-partido — veja `/debug` para a forma
    completa."). NAO copie a definicao dos 3 termos (feed-up/feed-back/feed-forward) — ela mora
    SO em debug.md (fonte-unica; Pitfall 1). V-15 grepa "feedback tri-partido". Garanta que ha
    tambem "/debug" ou "debug.md" na secao. Mantenha tudo como prosa (sem bloco cercado novo
    com jargao).
  </action>
  <verify>
    <automated>rg -n "reference\.md" mentor/metodo.md && rg -n "tutor\.md|/tutor" mentor/metodo.md && rg -n "fecha-marco\.md|/fecha-marco" mentor/metodo.md && rg -n "feedback tri-partido" mentor/metodo.md && rg -n "/debug|debug\.md" mentor/metodo.md && awk '/^## Conduta por marco/{f=1;next} /^## /{f=0} f && /^[1-5]\./{n++} END{print "ciclo_itens="n+0}' mentor/metodo.md</automated>
  </verify>
  <acceptance_criteria>
    - V-11: rg -c "reference\.md" mentor/metodo.md >= 1 (ciclo linka GRR ao dono).
    - V-12: rg -c "tutor\.md|/tutor" mentor/metodo.md >= 1 (ciclo linka retrieval).
    - V-13: rg -c "fecha-marco\.md|/fecha-marco" mentor/metodo.md >= 1 (ciclo/fechamento linka mastery gate).
    - V-14: a contagem awk de itens "[1-5]." dentro da secao "Conduta por marco (ciclo)" == 5 (itens preservados; edicao inline, nao bloco novo).
    - V-15: rg -c "feedback tri-partido" mentor/metodo.md >= 1 E rg -c "/debug|debug\.md" mentor/metodo.md >= 1 (ponteiro curto).
    - SEM definicao dos 3 termos (feed-up/feed-back/feed-forward) em metodo.md: rg -c "feed-up|feed-back|feed-forward" mentor/metodo.md == 0 (Pitfall 1 — a definicao mora so em debug.md).
    - Exatamente 3 nomeacoes com link no ciclo (GRR, retrieval, gate); backward design / primeiro done leve NAO nomeados.
  </acceptance_criteria>
  <done>O ciclo nomeia inline GRR/retrieval/gate (3 links, 5 itens preservados) e o resumo de forense vira ponteiro curto "feedback tri-partido" + /debug; V-11..V-15 verdes, sem vazar a definicao tri-partida.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Fase de documentacao — edicao de prosa markdown lida pelo agente. Sem runtime, rede, input nao-confiavel, auth ou cripto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | (N/A) | mentor/metodo.md | accept | Sem superficie de ataque (mesma conclusao das Fases 2 e 4). A unica preocupacao e editorial: (a) anti-leak CONS-01 — mitigado por V-17 (extract-fenced.sh + grep == 0 em blocos cercados); (b) anti-drift / duplicacao da definicao forense — mitigado por D-07 (ponteiro curto) e pelo acceptance criteria que exige rg "feed-up|feed-back|feed-forward" == 0 em metodo.md (definicao mora so em debug.md). |
</threat_model>

<verification>
- Apos cada task, rodar o `rg` do criterio tocado (verify de cada task).
- Apos o plano: `sh .planning/phases/05-persona-e-feedback-metodo-md-debug-md/scripts/check-phase5.sh` — V-01..V-07, V-11..V-15 devem ficar verdes; V-17 (leak-met) deve permanecer 0.
- ANCHOR-RESOLVE (V-16) ja passa (headings dos donos existem); confirmar que continua verde.
- Gate da fase (humano): 1 clique no link GRR no renderer para confirmar o slug fragil (A1).
</verification>

<success_criteria>
- metodo.md entrega CONS-01 (regra no corpo + espelho + FUND-03 + 1a ponte a fundamentos.md).
- metodo.md entrega AVAL-05 (passo de auto-explicacao + guardrail rigido + fronteira fecha-marco).
- metodo.md entrega o Criterio 4 (ciclo nomeia GRR/retrieval/gate por link, 5 itens preservados).
- D-07 mantido: ponteiro curto "feedback tri-partido" + /debug, sem copiar a definicao.
- Anti-leak V-17 (metodo.md) permanece 0; os 5 itens do ciclo (V-14) permanecem 5.
</success_criteria>

<output>
After completion, create `.planning/phases/05-persona-e-feedback-metodo-md-debug-md/05-02-SUMMARY.md`
</output>
