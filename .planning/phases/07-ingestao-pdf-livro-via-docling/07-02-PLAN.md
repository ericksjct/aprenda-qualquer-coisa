---
phase: 07-ingestao-pdf-livro-via-docling
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - scripts/check-consistencia.sh
  - .claude/skills/converte-livro/SKILL.md
  - AGENTS.md
  - README.md
  - mentor/metodo.md
  - mentor/novo-projeto.md
autonomous: true
requirements: [D-02, D-06, D-13, D-14, D-15]

must_haves:
  truths:
    - "The mentor, at bootstrap, asks 'tem livro-base? (PDF ou .md)' and branches: .md -> paste into livro/ (zero deps); PDF -> surface the exact venv command OR fire /converte-livro"
    - "Before firing the script the mentor pre-warns: vai demorar, nao consome tokens (roda local), mostra progresso pagina N de M"
    - "The mentor never runs docling silently; it surfaces the command/skill and consumes livro/ once populated"
    - "A thin /converte-livro skill exists that POINTS to mentor/novo-projeto.md (no duplicated method logic)"
    - "AGENTS.md registers scripts/converte_livro.py as an opt-in tool and lists /converte-livro among native skills"
    - "check-consistencia.sh whitelists /converte-livro in NONCMD so the V-15 set-equality stays green; README.md + mentor/metodo.md also list /converte-livro; the harness exits 0 AFTER these edits"
    - "The /converte-livro -> mentor/novo-projeto.md routing is recorded as a DOCUMENTED exception to the /comando -> mentor/<comando>.md convention (no mentor/converte-livro.md is required)"
  artifacts:
    - path: "scripts/check-consistencia.sh"
      provides: "Harness reconciled: NONCMD whitelists converte-livro (V-15 stays green; V-12 set-fechado unchanged)"
      contains: "converte-livro"
    - path: ".claude/skills/converte-livro/SKILL.md"
      provides: "Thin adapter pointing to mentor/ (D-13) + documented routing exception"
      contains: "mentor/novo-projeto.md"
    - path: "AGENTS.md"
      provides: "Tool + skill registration for the converter"
      contains: "converte_livro.py"
    - path: "README.md"
      provides: "/converte-livro listed among Claude Code adapters/skills (V-15 consistency)"
      contains: "converte-livro"
    - path: "mentor/metodo.md"
      provides: "/converte-livro noted in the routing convention (V-15 consistency)"
      contains: "converte-livro"
    - path: "mentor/novo-projeto.md"
      provides: "Optional livro-base bootstrap step (D-02/D-15) + D-14 pre-warning + esqueleto livro/"
      contains: "tem livro-base"
  key_links:
    - from: ".claude/skills/converte-livro/SKILL.md"
      to: "mentor/novo-projeto.md"
      via: "thin pointer body"
      pattern: "mentor/novo-projeto.md"
    - from: "mentor/novo-projeto.md livro-base step"
      to: "scripts/converte_livro.py (venv command)"
      via: "fenced text command surfaced by mentor"
      pattern: "converte_livro.py"
    - from: "mentor/novo-projeto.md Passo 7 esqueleto"
      to: "livro/"
      via: "skeleton bullet alongside referencias/"
      pattern: "livro/"
    - from: "AGENTS.md /converte-livro registration"
      to: "scripts/check-consistencia.sh NONCMD whitelist"
      via: "harness reconciliation BEFORE asserting green"
      pattern: "NONCMD"
---

<objective>
Weave the invocation layer: a thin `/converte-livro` skill (D-13), the AGENTS.md tool/skill
registration, and the OPTIONAL livro-base bootstrap step in `mentor/novo-projeto.md` carrying
the D-15 PDF-vs-.md branch and the D-14 pre-warning. The mentor surfaces the command / fires
the skill — it NEVER runs docling silently — then consumes `livro/`.

Because adding `/converte-livro` to AGENTS.md/README.md/mentor/metodo.md trips the harness'
V-15 set-equality (a CLOSED command set), this plan FIRST reconciles the harness contract
(`scripts/check-consistencia.sh` NONCMD whitelist) so `sh scripts/check-consistencia.sh`
stays green AFTER the registration edits.

Purpose: Give the agent a deterministic, script-prescribed path to populate `livro/` while
keeping method logic single-sourced in `mentor/` (D-02). This is the orchestration half of the
phase; it cites the FIXED CLI signature from Plan 01.

Output: `.claude/skills/converte-livro/SKILL.md` (new); edits to `scripts/check-consistencia.sh`,
`AGENTS.md`, `README.md`, `mentor/metodo.md`, and `mentor/novo-projeto.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ingestao-pdf-livro-via-docling/07-CONTEXT.md
@.planning/phases/07-ingestao-pdf-livro-via-docling/07-PATTERNS.md
@.planning/phases/07-ingestao-pdf-livro-via-docling/07-01-SUMMARY.md
@.claude/skills/tutor/SKILL.md
@scripts/check-consistencia.sh
@AGENTS.md
@README.md
@mentor/metodo.md
@mentor/novo-projeto.md

<interfaces>
<!-- The FIXED CLI from Plan 01 (07-01-SUMMARY.md is authoritative; values below are the planned -->
<!-- defaults — if the SUMMARY differs, the SUMMARY wins). -->

```text
# Surface this exact shape (venv-prefixed; Pitfall 3 — explicit venv, never global python):
python -m venv .venv-pdf
.\.venv-pdf\Scripts\activate
pip install -r requirements-pdf.txt
python scripts/converte_livro.py <pdf> --slug <slug>
# output: .projetos/<slug>/livro/ , one .md per chapter with <!-- page: N --> anchors
```

<!-- The harness contract that MUST be reconciled before /converte-livro is registered. -->
<!-- V-15 set-equality (check-consistencia.sh lines 49-66): for AGENTS.md/README.md/mentor/metodo.md, -->
<!-- the POSITIVE side requires the 5 CANON commands; the NEGATIVE side fails on any `/name` token -->
<!-- (backtick-delimited) NOT in NONCMD. Current NONCMD (line 52): -->
<!--   NONCMD='novo-projeto|tutor|fecha-marco|debug|spidr-split|config|clear|comando' -->
<!-- V-12 (lines 28-32) loops a HARDCODED set {novo-projeto tutor fecha-marco debug spidr-split} -->
<!-- requiring mentor/<cmd>.md — converte-livro is NOT in that loop, so NO mentor/converte-livro.md -->
<!-- is required: /converte-livro intentionally routes to mentor/novo-projeto.md (documented exception). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the harness contract — whitelist /converte-livro in NONCMD (BEFORE any registration edit)</name>
  <read_first>
    - scripts/check-consistencia.sh (lines 28-32 V-12 hardcoded set-fechado; lines 49-66 V-15 set-equality with CANON line 49 + NONCMD line 52; lines 34-39 V-13 backtick-path resolution; line 72 V-16 accents -g '!reference.md')
    - 07-CONTEXT.md (D-13 thin-skill + D-02 single-source — why converte-livro routes to novo-projeto.md, not an eponymous doc)
    - AGENTS.md lines 25-26 (the existing /comando -> mentor/<comando>.md convention this exception deviates from)
  </read_first>
  <files>scripts/check-consistencia.sh</files>
  <action>
    Edit scripts/check-consistencia.sh to admit `/converte-livro` as a KNOWN command that routes to
    mentor/novo-projeto.md (NOT a canonical procedure with its own mentor doc). Do this FIRST in the
    plan so every later harness assertion in this plan runs against the reconciled contract.

    1) On line 52, add `converte-livro` to the NONCMD alternation (the V-15 negative-side whitelist).
       Change:
         NONCMD='novo-projeto|tutor|fecha-marco|debug|spidr-split|config|clear|comando'
       to:
         NONCMD='novo-projeto|tutor|fecha-marco|debug|spidr-split|converte-livro|config|clear|comando'
       This stops the V-15 negative side (lines 59-60) from flagging `/converte-livro` as a `/name`
       FORA-DO-SET. Do NOT add it to CANON (line 49): CANON is the 5-command set whose members must
       appear in all three files AND each requires a mentor/<cmd>.md via V-12 — converte-livro has no
       eponymous mentor doc, so keeping it OUT of CANON is deliberate.

    2) Do NOT touch the V-12 loop (lines 29-32): its set is hardcoded `for cmd in novo-projeto tutor
       fecha-marco debug spidr-split` and must stay 5 — adding converte-livro there would (wrongly)
       require a mentor/converte-livro.md. Add a one-line ASCII comment near line 52 documenting the
       exception, e.g.:
         # converte-livro: comando real que roteia para mentor/novo-projeto.md (passo livro-base);
         #   NAO tem mentor/converte-livro.md proprio (excecao documentada a convencao /comando).

    3) Confirm V-13 (lines 34-39) still passes: the new backtick paths this plan introduces
       (`mentor/novo-projeto.md`, `scripts/converte_livro.py`, `requirements-pdf.txt`) all resolve —
       converte_livro.py + requirements-pdf.txt are created by Plan 01 (Wave 1), so they exist on disk
       by the time this Wave-2 plan runs. No SKIP entry needed.

    Keep the script ASCII (it is a conduct/infra doc; no accented chars).
  </action>
  <verify>
    <automated>sh scripts/check-consistencia.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-consistencia.sh` NONCMD line contains `converte-livro` (grep: `NONCMD=` line includes `converte-livro`).
    - `scripts/check-consistencia.sh` V-12 loop (line 29) still lists exactly `novo-projeto tutor fecha-marco debug spidr-split` (converte-livro NOT added to V-12 — no eponymous mentor doc required).
    - `scripts/check-consistencia.sh` contains an ASCII comment documenting the converte-livro -> novo-projeto.md routing exception.
    - `scripts/check-consistencia.sh` contains ZERO accented characters (grep for `[áàâãéêíóôõúç]` returns no match).
    - `sh scripts/check-consistencia.sh` exits 0 (harness still green BEFORE the registration edits; whitelist is a no-op until /converte-livro appears).
  </acceptance_criteria>
  <done>NONCMD whitelists converte-livro, V-12 set-fechado untouched, the routing exception is documented in-script, and the harness still exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Thin /converte-livro skill adapter (with documented routing exception)</name>
  <read_first>
    - .claude/skills/tutor/SKILL.md (lines 1-13 — exact thin-adapter template: YAML name+description, then a 2-3 line body pointing to mentor/<comando>.md + the /comando convention)
    - 07-PATTERNS.md section ".claude/skills/converte-livro/SKILL.md" (D-15 nuance: method logic lives in mentor/novo-projeto.md; skill only points; ASCII description)
    - 07-01-SUMMARY.md (the final CLI signature — do NOT restate docling steps in the skill; only point)
  </read_first>
  <files>.claude/skills/converte-livro/SKILL.md</files>
  <action>
    Create .claude/skills/converte-livro/SKILL.md mirroring tutor/SKILL.md exactly. ASCII, no accents.

    YAML frontmatter:
      name: converte-livro
      description: <ASCII trigger surface>. Use quando o aluno tem um livro-base em PDF e quer
        converte-lo para markdown em .projetos/<slug>/livro/, ou quando o bootstrap pergunta
        "tem livro-base?". Aponta para o procedimento canonico no mentor.

    Body (thin pointer — NO docling logic duplicated). CRITICAL: state the routing exception
    EXPLICITLY in the body so it is self-documenting (this is the pattern-compliance fix —
    /converte-livro intentionally routes to mentor/novo-projeto.md, NOT an eponymous mentor doc):
      "# Converte Livro (adaptador Claude Code)
       Leia `mentor/novo-projeto.md` (passo do livro-base, 'Passo 3b') na raiz deste repositorio
       e siga o procedimento exatamente: pergunte se o livro e PDF ou .md, pre-avise que a conversao
       demora / nao consome tokens (roda local) / mostra progresso, e so consuma `livro/` depois.
       Excecao de convencao: ao contrario das demais skills (que apontam para `mentor/<comando>.md`),
       esta aponta para `mentor/novo-projeto.md` — o livro-base e um PASSO do bootstrap, nao um
       procedimento proprio; nao existe `mentor/converte-livro.md`."

    Do NOT explain docling, env guards, or the CLI flags here — those live in mentor/novo-projeto.md
    (D-02 single-source) and scripts/converte_livro.py.

    Also record in 07-02-SUMMARY.md (at plan close) that /converte-livro deliberately routes to
    mentor/novo-projeto.md (the livro-base bootstrap step) rather than an eponymous mentor doc.
  </action>
  <verify>
    <automated>sh scripts/check-consistencia.sh</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/skills/converte-livro/SKILL.md` contains `name: converte-livro` and `mentor/novo-projeto.md`.
    - The file contains the phrase `nao consome tokens` (D-14 pre-warning echoed in the pointer).
    - The file contains an explicit routing-exception sentence (grep: `Excecao de convencao` and `nao existe` + `converte-livro.md`).
    - The file does NOT contain `docling`, `AcceleratorOptions`, or `iterate_items` (no method/engine duplication).
    - The file contains ZERO accented characters (ASCII internal doc): a grep for `[áàâãéêíóôõúç]` returns no match.
    - `sh scripts/check-consistencia.sh` exits 0 (the skill adds no `/name` token to a V-15 file; Task 1 already whitelisted converte-livro).
  </acceptance_criteria>
  <done>A thin /converte-livro skill exists, points to mentor/novo-projeto.md, documents the routing exception, duplicates no logic, and is ASCII.</done>
</task>

<task type="auto">
  <name>Task 3: Weave the optional livro-base bootstrap step into mentor/novo-projeto.md (D-15 + D-14 + esqueleto)</name>
  <read_first>
    - mentor/novo-projeto.md (Passo 3 lines 34-50 — the EXACT skeleton to mirror: heading -> framing sentence -> fenced text command -> rationale bullets -> graceful-skip bullet; AND Passo 7 esqueleto lines 104-117 listing referencias/)
    - 07-PATTERNS.md section "mentor/novo-projeto.md (EDIT)" (the anatomy to reuse + D-15 content + venv requirement)
    - 07-01-SUMMARY.md (final CLI signature + venv prefix to surface verbatim)
    - mentor/reference.md:22-43 (Idioma e acentuacao — this doc is ASCII)
  </read_first>
  <files>mentor/novo-projeto.md</files>
  <action>
    Add a NEW optional step "Passo 3b — Livro-base (opcional)" immediately AFTER Passo 3
    (Referencia externa), reusing Passo 3's exact skeleton. ASCII, no accents (internal doc).
    markdownlint-clean (MD022 blank line around headings, MD031 around fences, MD032 around lists,
    MD040 fenced lang tag, MD047 trailing newline).

    Content:
      - Heading `## Passo 3b — Livro-base (opcional)`.
      - Framing: the mentor PERGUNTA ao aluno: "tem livro-base? (PDF ou `.md`)". Then branch:
        * Se `.md`: instrua o aluno a COLAR o arquivo em `.projetos/<slug>/livro/` — caminho sem
          nenhuma dependencia nova (D-06). Nada de docling.
        * Se PDF: o mentor mostra a linha de comando EXATA (ou dispara `/converte-livro`), com o
          PRE-AVISO obrigatorio (D-14): "vai demorar, nao consome tokens (roda local), e mostra
          o progresso (pagina N de M)". Inclua tambem que a 1a execucao baixa modelos (~2GB).
      - A fenced `text` block with the EXACT venv-prefixed command from 07-01-SUMMARY.md:
        ```text
        python -m venv .venv-pdf
        .\.venv-pdf\Scripts\activate
        pip install -r requirements-pdf.txt
        python scripts/converte_livro.py <pdf> --slug <slug>
        ```
        (Pitfall 3: explicit venv activation, Python 3.10+; never the ambiguous global python.)
      - Rationale bullets: a saida (markdown do livro, um arquivo por capitulo com ancoras
        `<!-- page: N -->`) fica em `.projetos/<slug>/livro/`; e a literatura-base que o tutor
        consulta depois (D-01, detalhado em `mentor/tutor.md`).
      - CRITICAL closing rule (D-15): "O mentor NUNCA roda docling silenciosamente: ele mostra o
        comando ou dispara a skill com o pre-aviso, e so segue quando `livro/` estiver populado."
      - Graceful-skip bullet (mirror Passo 3 line 50): "Se o aluno nao tem livro-base, ou o venv
        nao esta acessivel, siga sem ele — o livro e opcional; o contrato e apenas markdown em
        `livro/` quando existir."

    In Passo 7 (esqueleto, ~lines 104-117), add a skeleton bullet alongside `referencias/`:
      "- `livro/` (se o Passo 3b converteu/colou o livro-base do aluno)."
  </action>
  <verify>
    <automated>sh scripts/check-consistencia.sh</automated>
  </verify>
  <acceptance_criteria>
    - `mentor/novo-projeto.md` contains the literal `tem livro-base`.
    - `mentor/novo-projeto.md` contains `nao consome tokens` and `pagina N de M` (D-14 pre-warning).
    - `mentor/novo-projeto.md` contains `converte_livro.py` and `.venv-pdf` (exact venv-prefixed command).
    - `mentor/novo-projeto.md` contains `NUNCA roda docling silenciosamente` (D-15 rule).
    - `mentor/novo-projeto.md` Passo 7 esqueleto contains a `livro/` bullet.
    - `mentor/novo-projeto.md` contains ZERO accented characters (grep for `[áàâãéêíóôõúç]` returns no match — internal ASCII doc).
    - `sh scripts/check-consistencia.sh` exits 0 (V-16 accents + adapter-drift checks pass; converte-livro already whitelisted by Task 1).
  </acceptance_criteria>
  <done>The optional livro-base step exists with the D-15 branch, D-14 pre-warning, exact venv command, and the livro/ esqueleto bullet; consistency harness green.</done>
</task>

<task type="auto">
  <name>Task 4: Register converter + skill in AGENTS.md, README.md, mentor/metodo.md (V-15 set-consistent)</name>
  <read_first>
    - AGENTS.md (## Ferramentas lines 50-54 registering roadmap_fetch.py; ## Nota por ferramenta -> Claude Code bullet lines 57-60 listing native skills; lines 25-26 the /comando convention)
    - README.md (skills/adapters references ~lines 59-62 day-to-day table, ~line 118 "Adaptadores Claude Code" row, ~lines 173-176 Layout skills tree)
    - mentor/metodo.md (lines 4-6 routing convention header that lists the /comando pattern)
    - 07-PATTERNS.md section "AGENTS.md (EDIT)" (mirror roadmap_fetch.py registration; add /converte-livro to skill list; note opt-in venv / OPTIONAL)
    - 07-01-SUMMARY.md (the command form)
  </read_first>
  <files>AGENTS.md, README.md, mentor/metodo.md</files>
  <action>
    Edit all three V-15 files together so the command set stays consistent across them (the harness
    checks set-equality over AGENTS.md/README.md/mentor/metodo.md). ASCII, no accents. markdownlint-clean.
    Task 1 already whitelisted `converte-livro` in NONCMD, so adding `/converte-livro` here keeps the
    harness green.

    AGENTS.md:
    1) In `## Ferramentas`, add a bullet AFTER the roadmap_fetch.py bullet, same style:
       "- `python scripts/converte_livro.py <pdf> --slug <slug>` — converte um PDF de livro-base
         para markdown por capitulo (com ancoras `<!-- page: N -->`) em `.projetos/<slug>/livro/`,
         via docling. OPCIONAL e isolado: requer o venv de `requirements-pdf.txt` (o core do toolkit
         segue stdlib-only). Usado no bootstrap (`mentor/novo-projeto.md`, Passo 3b). NUNCA rode
         em silencio — pre-avise (demora / 0 tokens / progresso) e consuma `livro/` depois."
    2) In `## Nota por ferramenta` -> Claude Code bullet (line 58), add `/converte-livro` to the
       native-skills list so it reads: `/novo-projeto`, `/tutor`, `/fecha-marco`, `/debug`,
       `/spidr-split`, `/converte-livro`.

    README.md (keep the prose accented as it already is — README is student-facing and NOT in the
    V-16 accent scope, which only covers mentor/ except reference.md; but it IS in the V-15
    set-equality and V-13 backtick-path scope, so the `/converte-livro` token must appear):
    3) In the "Adaptadores Claude Code" row of the "Como funciona por dentro" table (~line 118),
       add `/converte-livro` to the skills list so the set matches AGENTS.md.
    4) In the Layout skills tree (~lines 173-176), add a tree line for the new skill:
       `    ├── converte-livro/SKILL.md  # /converte-livro (opcional; aponta para novo-projeto.md)`
       Keep the existing box-drawing/comment style; ensure the last skill entry uses the corner glyph.

    mentor/metodo.md (ASCII — this IS in the V-16 accent scope; no accents):
    5) In the routing-convention header (lines 4-6), note `/converte-livro` as a command that routes
       to `mentor/novo-projeto.md` (the documented exception to /comando -> mentor/<comando>.md), e.g.
       append a sentence: "Excecao: `/converte-livro` aponta para `mentor/novo-projeto.md` (passo
       livro-base), pois nao e um procedimento proprio." This satisfies the V-15 positive presence of
       converte-livro in mentor/metodo.md without implying a mentor/converte-livro.md.
  </action>
  <verify>
    <automated>sh scripts/check-consistencia.sh</automated>
  </verify>
  <acceptance_criteria>
    - `AGENTS.md` contains `converte_livro.py` in the `## Ferramentas` block AND `/converte-livro` in the Claude Code native-skills bullet AND `requirements-pdf.txt` AND `OPCIONAL`.
    - `AGENTS.md` contains ZERO accented characters (grep for `[áàâãéêíóôõúç]` returns no match).
    - `README.md` contains `/converte-livro` (V-15 set-consistent with AGENTS.md) and a `converte-livro/SKILL.md` tree line.
    - `mentor/metodo.md` contains `converte-livro` and the routing-exception sentence; `mentor/metodo.md` contains ZERO accented characters (grep returns no match).
    - `sh scripts/check-consistencia.sh` exits 0 — confirm AFTER all edits: V-15 set-equality (5 CANON present + 0 fora-do-set, since converte-livro is now in NONCMD), V-13 backtick paths resolve, V-16 accents clean.
  </acceptance_criteria>
  <done>AGENTS.md/README.md/mentor/metodo.md all register the converter/skill consistently; harness green with /converte-livro whitelisted.</done>
</task>

</tasks>

<verification>
- `sh scripts/check-consistencia.sh` exits 0 (the repo's permanent anti-drift/anti-accent harness, with /converte-livro whitelisted in NONCMD and present in all three V-15 files).
- `grep -c "tem livro-base" mentor/novo-projeto.md` >= 1 (D-15 question present).
- `grep -c "nao consome tokens" mentor/novo-projeto.md .claude/skills/converte-livro/SKILL.md` confirms the D-14 pre-warning in both pointer surfaces.
- `grep -c "converte-livro" scripts/check-consistencia.sh AGENTS.md README.md mentor/metodo.md` >= 1 in each (set-consistent registration).
- No method/engine logic duplicated into the skill or AGENTS.md (single-source D-02).
</verification>

<success_criteria>
- Harness reconciled FIRST: NONCMD whitelists converte-livro; V-12 set-fechado untouched; routing exception documented in-script.
- Thin /converte-livro skill points to mentor/novo-projeto.md (no duplicated logic), documents the routing exception, ASCII.
- novo-projeto.md has the optional livro-base step: D-15 PDF-vs-.md branch, D-14 pre-warning, exact venv command, D-06 paste path, "never run docling silently" rule, livro/ esqueleto bullet.
- AGENTS.md/README.md/mentor/metodo.md register the converter/skill set-consistently.
- check-consistencia.sh exits 0 AFTER all edits; internal conduct docs stay ASCII.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ingestao-pdf-livro-via-docling/07-02-SUMMARY.md`.
The SUMMARY MUST note that /converte-livro deliberately routes to mentor/novo-projeto.md (livro-base
bootstrap step) rather than an eponymous mentor doc, and that NONCMD in check-consistencia.sh was
extended to whitelist it (V-15 reconciliation).
</output>
