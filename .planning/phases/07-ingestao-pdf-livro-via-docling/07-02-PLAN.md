---
phase: 07-ingestao-pdf-livro-via-docling
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - .claude/skills/converte-livro/SKILL.md
  - AGENTS.md
  - mentor/novo-projeto.md
autonomous: true
requirements: [D-02, D-06, D-13, D-14, D-15]

must_haves:
  truths:
    - "The mentor, at bootstrap, asks 'tem livro-base? (PDF ou .md)' and branches: .md -> paste into livro/ (zero deps); PDF -> surface the exact venv command OR fire /converte-livro"
    - "Before firing the script the mentor pre-warns: vai demorar, nao consome tokens (roda local), mostra progresso pagina N de M"
    - "The mentor never runs docling silently; it surfaces the command/skill and consumes livro/ once populated"
    - "A thin /converte-livro skill exists that POINTS to mentor/novo-projeto.md (no duplicated method logic)"
    - "AGENTS.md registers scripts/converte_livro.py as an opt-in tool and lists /converte-livro among native skills"
  artifacts:
    - path: ".claude/skills/converte-livro/SKILL.md"
      provides: "Thin adapter pointing to mentor/ (D-13)"
      contains: "mentor/novo-projeto.md"
    - path: "AGENTS.md"
      provides: "Tool + skill registration for the converter"
      contains: "converte_livro.py"
    - path: "mentor/novo-projeto.md"
      provides: "Optional livro-base bootstrap step (D-02/D-15) + D-14 pre-warning + esqueleto livro/"
      contains: "tem livro-base"
  key_links:
    - from: ".claude/skills/converte-livro/SKILL.md"
      to: "mentor/novo-projeto.md"
      via: "thin pointer body"
      pattern: "mentor/novo-projeto.md"
    - from: "mentor/novo-projeto.md livro-base step"
      to: "scripts/converte_livro.py (venv command)"
      via: "fenced text command surfaced by mentor"
      pattern: "converte_livro.py"
    - from: "mentor/novo-projeto.md Passo 7 esqueleto"
      to: "livro/"
      via: "skeleton bullet alongside referencias/"
      pattern: "livro/"
---

<objective>
Weave the invocation layer: a thin `/converte-livro` skill (D-13), the AGENTS.md tool/skill
registration, and the OPTIONAL livro-base bootstrap step in `mentor/novo-projeto.md` carrying
the D-15 PDF-vs-.md branch and the D-14 pre-warning. The mentor surfaces the command / fires
the skill — it NEVER runs docling silently — then consumes `livro/`.

Purpose: Give the agent a deterministic, script-prescribed path to populate `livro/` while
keeping method logic single-sourced in `mentor/` (D-02). This is the orchestration half of the
phase; it cites the FIXED CLI signature from Plan 01.

Output: `.claude/skills/converte-livro/SKILL.md` (new), edits to `AGENTS.md` and
`mentor/novo-projeto.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ingestao-pdf-livro-via-docling/07-CONTEXT.md
@.planning/phases/07-ingestao-pdf-livro-via-docling/07-PATTERNS.md
@.planning/phases/07-ingestao-pdf-livro-via-docling/07-01-SUMMARY.md
@.claude/skills/tutor/SKILL.md
@mentor/novo-projeto.md
@AGENTS.md

<interfaces>
<!-- The FIXED CLI from Plan 01 (07-01-SUMMARY.md is authoritative; values below are the planned -->
<!-- defaults — if the SUMMARY differs, the SUMMARY wins). -->

```text
# Surface this exact shape (venv-prefixed; Pitfall 3 — explicit venv, never global python):
python -m venv .venv-pdf
.\.venv-pdf\Scripts\activate
pip install -r requirements-pdf.txt
python scripts/converte_livro.py <pdf> --slug <slug>
# output: .projetos/<slug>/livro/ , one .md per chapter with <!-- page: N --> anchors
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thin /converte-livro skill adapter</name>
  <read_first>
    - .claude/skills/tutor/SKILL.md (lines 1-13 — exact thin-adapter template: YAML name+description, then a 2-3 line body pointing to mentor/<comando>.md + the /comando convention)
    - 07-PATTERNS.md section ".claude/skills/converte-livro/SKILL.md" (D-15 nuance: method logic lives in mentor/novo-projeto.md; skill only points; ASCII description)
    - 07-01-SUMMARY.md (the final CLI signature — do NOT restate docling steps in the skill; only point)
  </read_first>
  <files>.claude/skills/converte-livro/SKILL.md</files>
  <action>
    Create .claude/skills/converte-livro/SKILL.md mirroring tutor/SKILL.md exactly. ASCII, no accents.

    YAML frontmatter:
      name: converte-livro
      description: <ASCII trigger surface>. Use quando o aluno tem um livro-base em PDF e quer
        converte-lo para markdown em .projetos/<slug>/livro/, ou quando o bootstrap pergunta
        "tem livro-base?". Aponta para o procedimento canonico no mentor.

    Body (2-3 lines, thin pointer — NO docling logic duplicated):
      "# Converte Livro (adaptador Claude Code)
       Leia `mentor/novo-projeto.md` (passo do livro-base) na raiz deste repositorio e siga o
       procedimento exatamente: pergunte se o livro e PDF ou .md, pre-avise que a conversao
       demora / nao consome tokens (roda local) / mostra progresso, e so consuma `livro/` depois.
       Convencao: quando o documento citar um `/comando`, e a skill correspondente (conteudo
       canonico em `mentor/<comando>.md`)."

    Do NOT explain docling, env guards, or the CLI flags here — those live in mentor/novo-projeto.md
    (D-02 single-source) and scripts/converte_livro.py.
  </action>
  <verify>
    <automated>sh scripts/check-consistencia.sh</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/skills/converte-livro/SKILL.md` contains `name: converte-livro` and `mentor/novo-projeto.md`.
    - The file contains the phrase `nao consome tokens` (D-14 pre-warning echoed in the pointer).
    - The file does NOT contain `docling`, `AcceleratorOptions`, or `iterate_items` (no method/engine duplication).
    - The file contains ZERO accented characters (ASCII internal doc): a grep for `[áàâãéêíóôõúç]` returns no match.
  </acceptance_criteria>
  <done>A thin /converte-livro skill exists, points to mentor/, duplicates no logic, and is ASCII.</done>
</task>

<task type="auto">
  <name>Task 2: Weave the optional livro-base bootstrap step into mentor/novo-projeto.md (D-15 + D-14 + esqueleto)</name>
  <read_first>
    - mentor/novo-projeto.md (Passo 3 lines 34-50 — the EXACT skeleton to mirror: heading -> framing sentence -> fenced text command -> rationale bullets -> graceful-skip bullet; AND Passo 7 esqueleto lines 104-117 listing referencias/)
    - 07-PATTERNS.md section "mentor/novo-projeto.md (EDIT)" (the anatomy to reuse + D-15 content + venv requirement)
    - 07-01-SUMMARY.md (final CLI signature + venv prefix to surface verbatim)
    - mentor/reference.md:22-43 (Idioma e acentuacao — this doc is ASCII)
  </read_first>
  <files>mentor/novo-projeto.md</files>
  <action>
    Add a NEW optional step "Passo 3b — Livro-base (opcional)" immediately AFTER Passo 3
    (Referencia externa), reusing Passo 3's exact skeleton. ASCII, no accents (internal doc).
    markdownlint-clean (MD022 blank line around headings, MD031 around fences, MD032 around lists,
    MD040 fenced lang tag, MD047 trailing newline).

    Content:
      - Heading `## Passo 3b — Livro-base (opcional)`.
      - Framing: the mentor PERGUNTA ao aluno: "tem livro-base? (PDF ou `.md`)". Then branch:
        * Se `.md`: instrua o aluno a COLAR o arquivo em `.projetos/<slug>/livro/` — caminho sem
          nenhuma dependencia nova (D-06). Nada de docling.
        * Se PDF: o mentor mostra a linha de comando EXATA (ou dispara `/converte-livro`), com o
          PRE-AVISO obrigatorio (D-14): "vai demorar, nao consome tokens (roda local), e mostra
          o progresso (pagina N de M)". Inclua tambem que a 1a execucao baixa modelos (~2GB).
      - A fenced `text` block with the EXACT venv-prefixed command from 07-01-SUMMARY.md:
        ```text
        python -m venv .venv-pdf
        .\.venv-pdf\Scripts\activate
        pip install -r requirements-pdf.txt
        python scripts/converte_livro.py <pdf> --slug <slug>
        ```
        (Pitfall 3: explicit venv activation, Python 3.10+; never the ambiguous global python.)
      - Rationale bullets: a saida (markdown do livro, um arquivo por capitulo com ancoras
        `<!-- page: N -->`) fica em `.projetos/<slug>/livro/`; e a literatura-base que o tutor
        consulta depois (D-01, detalhado em `mentor/tutor.md`).
      - CRITICAL closing rule (D-15): "O mentor NUNCA roda docling silenciosamente: ele mostra o
        comando ou dispara a skill com o pre-aviso, e so segue quando `livro/` estiver populado."
      - Graceful-skip bullet (mirror Passo 3 line 50): "Se o aluno nao tem livro-base, ou o venv
        nao esta acessivel, siga sem ele — o livro e opcional; o contrato e apenas markdown em
        `livro/` quando existir."

    In Passo 7 (esqueleto, ~lines 104-117), add a skeleton bullet alongside `referencias/`:
      "- `livro/` (se o Passo 3b converteu/colou o livro-base do aluno)."
  </action>
  <verify>
    <automated>sh scripts/check-consistencia.sh</automated>
  </verify>
  <acceptance_criteria>
    - `mentor/novo-projeto.md` contains the literal `tem livro-base`.
    - `mentor/novo-projeto.md` contains `nao consome tokens` and `pagina N de M` (D-14 pre-warning).
    - `mentor/novo-projeto.md` contains `converte_livro.py` and `.venv-pdf` (exact venv-prefixed command).
    - `mentor/novo-projeto.md` contains `NUNCA roda docling silenciosamente` (D-15 rule).
    - `mentor/novo-projeto.md` Passo 7 esqueleto contains a `livro/` bullet.
    - `mentor/novo-projeto.md` contains ZERO accented characters (grep for `[áàâãéêíóôõúç]` returns no match — internal ASCII doc).
    - `sh scripts/check-consistencia.sh` exits 0 (V-16 accents + adapter-drift checks pass).
  </acceptance_criteria>
  <done>The optional livro-base step exists with the D-15 branch, D-14 pre-warning, exact venv command, and the livro/ esqueleto bullet; consistency harness green.</done>
</task>

<task type="auto">
  <name>Task 3: Register converter + skill in AGENTS.md</name>
  <read_first>
    - AGENTS.md (## Ferramentas lines 50-54 registering roadmap_fetch.py; ## Nota por ferramenta -> Claude Code bullet lines 57-60 listing native skills)
    - 07-PATTERNS.md section "AGENTS.md (EDIT)" (mirror roadmap_fetch.py registration; add /converte-livro to skill list; note opt-in venv / OPTIONAL)
    - 07-01-SUMMARY.md (the command form)
  </read_first>
  <files>AGENTS.md</files>
  <action>
    Edit AGENTS.md. ASCII, no accents. markdownlint-clean.

    1) In `## Ferramentas`, add a bullet AFTER the roadmap_fetch.py bullet, same style:
       "- `python scripts/converte_livro.py <pdf> --slug <slug>` — converte um PDF de livro-base
         para markdown por capitulo (com ancoras `<!-- page: N -->`) em `.projetos/<slug>/livro/`,
         via docling. OPCIONAL e isolado: requer o venv de `requirements-pdf.txt` (o core do toolkit
         segue stdlib-only). Usado no bootstrap (`mentor/novo-projeto.md`, Passo 3b). NUNCA rode
         em silencio — pre-avise (demora / 0 tokens / progresso) e consuma `livro/` depois."

    2) In `## Nota por ferramenta` -> Claude Code bullet (line 58), add `/converte-livro` to the
       native-skills list so it reads: `/novo-projeto`, `/tutor`, `/fecha-marco`, `/debug`,
       `/spidr-split`, `/converte-livro`.
  </action>
  <verify>
    <automated>sh scripts/check-consistencia.sh</automated>
  </verify>
  <acceptance_criteria>
    - `AGENTS.md` contains `converte_livro.py` in the `## Ferramentas` block.
    - `AGENTS.md` contains `/converte-livro` in the Claude Code native-skills bullet.
    - `AGENTS.md` contains `requirements-pdf.txt` and the word `OPCIONAL` (opt-in noted).
    - `AGENTS.md` contains ZERO accented characters (grep for `[áàâãéêíóôõúç]` returns no match).
    - `sh scripts/check-consistencia.sh` exits 0 (V-14/V-15 adapter set-equality + V-16 accents pass).
  </acceptance_criteria>
  <done>AGENTS.md registers the converter as an opt-in tool and lists /converte-livro; harness green.</done>
</task>

</tasks>

<verification>
- `sh scripts/check-consistencia.sh` exits 0 (the repo's permanent anti-drift/anti-accent harness, including the new adapter set-equality once /converte-livro is added everywhere).
- `grep -c "tem livro-base" mentor/novo-projeto.md` >= 1 (D-15 question present).
- `grep -c "nao consome tokens" mentor/novo-projeto.md .claude/skills/converte-livro/SKILL.md` confirms the D-14 pre-warning in both pointer surfaces.
- No method/engine logic duplicated into the skill or AGENTS.md (single-source D-02).
</verification>

<success_criteria>
- Thin /converte-livro skill points to mentor/ (no duplicated logic), ASCII.
- novo-projeto.md has the optional livro-base step: D-15 PDF-vs-.md branch, D-14 pre-warning, exact venv command, D-06 paste path, "never run docling silently" rule, livro/ esqueleto bullet.
- AGENTS.md registers the converter (opt-in) and the skill.
- check-consistencia.sh exits 0; all four internal docs stay ASCII.
</success_criteria>

<output>
After completion, create `.planning/phases/07-ingestao-pdf-livro-via-docling/07-02-SUMMARY.md`.
</output>
