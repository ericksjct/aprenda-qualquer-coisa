---
phase: 07-ingestao-pdf-livro-via-docling
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - mentor/reference.md
  - mentor/tutor.md
autonomous: true
requirements: [D-01, D-03, D-04, D-08, D-10]

must_haves:
  truths:
    - "reference.md layout shows livro/ as a NEW sibling of referencias/, with the <!-- page: N --> anchor noted (D-08)"
    - "reference.md documents the divergence record format in APRENDIZADO.md: livro diz X / pratica diz Y / escolha do mentor, dated (D-04)"
    - "tutor.md Passo 0 reads livro/*.md as a baseline source when it exists (D-01)"
    - "tutor.md Passo 3 cites 've a pagina X do livro' using the <!-- page: N --> anchor (D-10)"
    - "The divergence protocol (mentor decides alone) ALWAYS ships with the dated APRENDIZADO.md record — D-03 never appears without D-04"
  artifacts:
    - path: "mentor/reference.md"
      provides: "livro/ in layout (D-08) + divergence record format in APRENDIZADO template (D-04)"
      contains: "livro/"
    - path: "mentor/tutor.md"
      provides: "consult livro/ baseline (D-01) + page reference (D-10) + divergence protocol pointer (D-03/D-04)"
      contains: "livro/"
  key_links:
    - from: "mentor/reference.md layout tree"
      to: "livro/ sibling of referencias/"
      via: "ASCII tree bullet with anchor note"
      pattern: "livro/"
    - from: "mentor/tutor.md Passo 0"
      to: ".projetos/<slug>/livro/*.md"
      via: "ordered read-list bullet"
      pattern: "livro/"
    - from: "mentor/tutor.md Passo 3"
      to: "<!-- page: N --> anchor"
      via: "'ve a pagina X' page reference"
      pattern: "pagina"
    - from: "divergence protocol (D-03)"
      to: "APRENDIZADO.md dated record (D-04)"
      via: "always-together rule"
      pattern: "APRENDIZADO"
---

<objective>
Weave the consumption layer into the method: `mentor/reference.md` gains `livro/` in the repo
layout (D-08) and the divergence-record format in the APRENDIZADO.md template (D-04);
`mentor/tutor.md` consults `livro/` as the theoretical baseline (D-01), cites the exact page via
the `<!-- page: N -->` anchor (D-10), and references the divergence protocol where the mentor
decides alone (D-03) — ALWAYS paired with the dated APRENDIZADO.md record (D-04, never D-03 alone).

Purpose: This makes the converted book actually USED — as literatura-base the mentor respects,
with page-precise multi-midia references, and a transparent, auditable divergence trail.

Output: edits to `mentor/reference.md` and `mentor/tutor.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-ingestao-pdf-livro-via-docling/07-CONTEXT.md
@.planning/phases/07-ingestao-pdf-livro-via-docling/07-PATTERNS.md
@.planning/phases/07-ingestao-pdf-livro-via-docling/07-01-SUMMARY.md
@mentor/reference.md
@mentor/tutor.md

<interfaces>
<!-- The anchor format the tutor cites — confirm against 07-01-SUMMARY.md (it wins if it differs). -->

```text
<!-- page: N -->   # HTML comment at the start of each chapter file in .projetos/<slug>/livro/
                   # N = real docling page number (prov[0].page_no); enables "ve a pagina X"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: reference.md — add livro/ to layout (D-08) + divergence record in APRENDIZADO template (D-04)</name>
  <read_first>
    - mentor/reference.md (Layout do repo do aluno block lines 221-238 — the ASCII tree to edit, comment style; Template APRENDIZADO.md lines 298-318, esp. "## Decisoes arquiteturais" dated-bullet lines 311-313; Idioma e acentuacao lines 22-43)
    - 07-PATTERNS.md section "mentor/reference.md (EDIT)" (where to place livro/, the anchor note, reuse Decisoes arquiteturais dated-entry pattern for divergence)
    - 07-01-SUMMARY.md (confirm the anchor format <!-- page: N -->)
  </read_first>
  <files>mentor/reference.md</files>
  <action>
    Edit mentor/reference.md. The surrounding PROSE of reference.md is ASCII; the TEXT inside the
    student-facing fenced TEMPLATES (APRENDIZADO.md) is intentionally ACCENTED Portuguese
    (reference.md:40-43) — match the existing accented template text when editing inside the fence.
    markdownlint-clean.

    1) In the "Layout do repo do aluno" tree (lines 227-238), add `livro/` as a NEW sibling of
       `referencias/` (distinct from it — D-08 separates book-base from roadmap output). Mirror the
       existing `|--` comment style. Place it right after the `referencias/` line:
       ```text
       |-- referencias/          # roadmaps de referencia (scripts/roadmap_fetch.py)
       |-- livro/                # livro-base do aluno em markdown (docling OU colado): baseline teorica
       |   `-- 0x-<capitulo>.md   #   um arquivo por capitulo, com ancoras <!-- page: N -->
       ```
       Keep the tree's box-drawing characters consistent with the rest of the block (ASCII `|`-- style).

    2) In the "## Template -- APRENDIZADO.md" fenced block, EXTEND the "## Decisoes arquiteturais"
       section (lines 311-313) with the divergence-record format (D-04). Reuse the dated-bullet
       style `- AAAA-MM-DD -- ...` with ACCENTED Portuguese (student artifact). Add, inside the
       fence, a sub-pattern documenting the three-way record:
       ```markdown
       ## Decisões arquiteturais

       - AAAA-MM-DD -- <decisão tomada> (contexto: <por que escolhemos isso>)
       - AAAA-MM-DD -- divergência livro x prática: o livro diz <X> / a prática atual diz <Y> /
         escolha do mentor: <Z> (critério: <recência/consenso/...>)
       ```
       In the PROSE near the template (ASCII), add one sentence: every divergence between the book
       and the mentor's research is recorded here as a dated entry — this is the transparency
       mechanism that pairs with the mentor's autonomous decision (cross-reference D-03/D-04 in tutor.md).

    3) Confirm (do not rewrite) that the "Idioma e acentuacao" section already covers livro/ content
       ("qualquer outro texto que o ALUNO le" — accented, no ASCII normalization). Add `livro/` to the
       student-facing accented list (line ~30-31, alongside CAMINHO.md/PROGRESSO.md/APRENDIZADO.md)
       so the accent rule for the converted book is explicit.
  </action>
  <verify>
    <automated>sh scripts/check-consistencia.sh</automated>
  </verify>
  <acceptance_criteria>
    - `mentor/reference.md` layout tree contains a `livro/` line as a sibling of `referencias/`.
    - `mentor/reference.md` contains the literal `<!-- page: N -->` in the layout comment.
    - `mentor/reference.md` APRENDIZADO template contains the divergence record line with `o livro diz` and `escolha do mentor`.
    - `mentor/reference.md` "Idioma e acentuacao" student-facing list includes `livro/`.
    - `sh scripts/check-consistencia.sh` exits 0 (V-16 only flags accents OUTSIDE the intentionally-accented student templates — confirm the new accented text sits INSIDE a fenced template block, consistent with existing APRENDIZADO entries).
  </acceptance_criteria>
  <done>livro/ is in the layout with the anchor note; the APRENDIZADO template documents the dated divergence record; the accent rule covers livro/; harness green.</done>
</task>

<task type="auto">
  <name>Task 2: tutor.md — consult livro/ baseline (D-01), page reference (D-10), divergence protocol D-03+D-04 together</name>
  <read_first>
    - mentor/tutor.md (Passo 0 ordered reads lines 12-35; Passo 3 step 1 "Conceito em dois canais" lines 65-74)
    - 07-PATTERNS.md section "mentor/tutor.md (EDIT)" (add livro/ to Passo 0 read-list; weave page-ref into Passo 3 step 1 as a refinement, not a new top-level step; D-03/D-04 ALWAYS together; reuse APRENDIZADO.md artifact)
    - 07-01-SUMMARY.md (anchor format the tutor cites)
    - mentor/reference.md (the divergence record format Task 1 documents — tutor.md POINTS to it, does not redefine it)
  </read_first>
  <files>mentor/tutor.md</files>
  <action>
    Edit mentor/tutor.md. ASCII, no accents (internal doc). markdownlint-clean.

    1) Passo 0 (ordered read-list, lines 24-29): add a bullet — read `livro/*.md` when it exists,
       in the existing bulleted style:
       "- `livro/*.md` (se existir) — o livro-base do aluno convertido (ou colado): literatura-base
         que voce RESPEITA e consulta como baseline teorica (D-01). Cada arquivo abre com uma ancora
         `<!-- page: N -->` para voce citar a pagina exata."
       Place it after the `APRENDIZADO.md` read bullet so it sits among the context reads.

    2) Passo 3 step 1 "Conceito em dois canais" (lines 69-74): add a refinement sentence (NOT a new
       top-level step) — when the concept is covered by the book, the tutor can cite the exact page:
       "Quando o conceito do passo aparece no `livro/`, aponte o aluno para a pagina exata usando a
        ancora `<!-- page: N -->` daquele trecho ('ve a pagina X do livro') — reforco multi-midia,
        ancorado na literatura-base do aluno (D-10)."

    3) Add the divergence protocol (D-03) in Passo 3 step 1, ALWAYS paired with the dated record
       (D-04 — never D-03 alone). Write a short sub-paragraph:
       "Se a sua pesquisa/pratica atual diverge do que o livro ensina, VOCE decide sozinho qual
        seguir (criterio proprio: recencia, consenso) — nao transfira a escolha ao aluno. Mas toda
        divergencia vira uma entrada DATADA em `APRENDIZADO.md` (secao Decisoes arquiteturais,
        formato em `mentor/reference.md`): o livro diz X / a pratica diz Y / a sua escolha e Z.
        Esse registro e o que mantem a decisao auditavel e didatica — nunca decida em silencio
        sem registrar."
       This sub-paragraph MUST contain BOTH the "decide sozinho" rule and the "entrada datada em
       APRENDIZADO.md" record in the same place (D-03 + D-04 together).
  </action>
  <verify>
    <automated>sh scripts/check-consistencia.sh</automated>
  </verify>
  <acceptance_criteria>
    - `mentor/tutor.md` Passo 0 read-list contains a `livro/*.md` bullet mentioning `baseline` and `<!-- page: N -->`.
    - `mentor/tutor.md` contains the page-reference phrasing `ve a pagina X do livro`.
    - `mentor/tutor.md` contains BOTH `decide sozinho` (D-03) AND `entrada` + `APRENDIZADO.md` (D-04) within the divergence sub-paragraph — grep confirms both strings present (D-03 never ships alone).
    - `mentor/tutor.md` contains `mentor/reference.md` as the pointer to the divergence record format (single-source; tutor does not redefine it).
    - `mentor/tutor.md` contains ZERO accented characters (grep for `[áàâãéêíóôõúç]` returns no match).
    - `sh scripts/check-consistencia.sh` exits 0.
  </acceptance_criteria>
  <done>tutor.md consults livro/ as baseline, cites exact pages via the anchor, and carries the divergence protocol with its mandatory dated APRENDIZADO.md record (D-03+D-04 together); harness green.</done>
</task>

</tasks>

<verification>
- `sh scripts/check-consistencia.sh` exits 0 (anti-drift / accent harness over mentor/ edits).
- `grep -c "livro/" mentor/reference.md mentor/tutor.md` >= 1 in each (D-01/D-08 wiring present).
- `grep -c "ve a pagina X" mentor/tutor.md` >= 1 (D-10 page reference).
- The divergence sub-paragraph in tutor.md contains BOTH "decide sozinho" and "APRENDIZADO.md" (D-03 + D-04 never split).
</verification>

<success_criteria>
- reference.md: livro/ in layout (D-08) with anchor note; APRENDIZADO template documents the dated divergence record (D-04); accent rule covers livro/.
- tutor.md: reads livro/ as baseline (D-01); cites exact page via <!-- page: N --> (D-10); divergence protocol (D-03) always paired with dated APRENDIZADO.md record (D-04).
- check-consistencia.sh exits 0; both docs stay ASCII (template-internal accented text excepted, inside fences).
</success_criteria>

<output>
After completion, create `.planning/phases/07-ingestao-pdf-livro-via-docling/07-03-SUMMARY.md`.
</output>
