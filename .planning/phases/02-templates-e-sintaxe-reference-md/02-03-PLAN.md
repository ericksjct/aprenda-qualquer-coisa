---
phase: 02-templates-e-sintaxe-reference-md
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - mentor/reference.md
autonomous: true
requirements: [EST-02, EST-04, CARGA-01, CARGA-02, CARGA-03]
must_haves:
  truths:
    - "O corpo da secao 'eu faco -> nos fazemos -> voce faz' entrega as 3 fases (hoje so 2); 'nos fazemos' e etapa condicional real"
    - "O 'eu faco' inclui o worked example analogo (exemplo resolvido em instancia diferente) antes do TODO solo, para substrato baixo"
    - "O fading por substrato esta nomeado (expertise-reversal: suporte recua conforme a maestria sobe) na zona de Sondagem/Granularidade"
    - "As 'Regras da aula' aplicam os 3 principios de Mayer que transferem para texto puro, com o limite honesto '3 de 12'"
    - "Backward design, GRR/worked-example, expertise-reversal e Mayer estao nomeados na PROSA e linkam fundamentos.md por ancoras REAIS"
    - "Os hyperlinks resolvem para cabecalhos que existem em fundamentos.md (nenhum link quebrado)"
    - "Nenhum jargao de framework vazou para dentro de bloco cercado (CONS-01)"
  artifacts:
    - path: "mentor/reference.md"
      provides: "GRR 3 fases + fading + worked example + Mayer + citacoes por hyperlink"
      contains: "nos fazemos"
  key_links:
    - from: "prosa de reference.md"
      to: "mentor/fundamentos.md (ancoras de secao reais)"
      via: "hyperlink relativo [nome do framework](fundamentos.md#ancora)"
      pattern: "\\]\\(fundamentos\\.md#"
---

<objective>
Tecer 2 fios na PROSA de `mentor/reference.md` e citar a teoria por hyperlink (fonte-unica):
1. GRR de 3 fases (EST-04, D-02): REESCREVER o corpo da secao "### Sintaxe nova de verdade:
   'eu faco -> nos fazemos -> voce faz'" — hoje o titulo promete 3 fases mas o corpo so
   entrega 2. Tornar "nos fazemos" uma etapa CONDICIONAL real, e fundir o worked example
   analogo (CARGA-02) no "eu faco".
2. Carga cognitiva: nomear o FADING por substrato (CARGA-01, expertise-reversal) na zona de
   Sondagem/Granularidade; adicionar a regra de Mayer com limite honesto na "Regras da aula"
   (CARGA-03).
E SEMPRE citar `fundamentos.md` por hyperlink na prosa (EST-02), nunca reescrevendo teoria.

Purpose: Completar a sintaxe GRR (we-do deixa de ser implicito), formalizar a calibragem por
substrato (expertise-reversal) e a apresentacao por carga cognitiva (Mayer), aterrissando as
citacoes que `fundamentos.md` marcou "(Fase 2, pendente)".
Output: `mentor/reference.md` editado em Z1/Z2 (fading), Z6 (GRR 3 fases + worked example),
Z4-prosa (Mayer), com >= 2 hyperlinks para `fundamentos.md` resolvendo.

NOTA CRITICA (D-04, corrigido pela pesquisa): NAO existe ancora de framework em
`fundamentos.md` (os nomes vivem em CELULAS de tabela). As unicas ancoras reais sao as 8
de SECAO. Link para framework => apontar a SECAO que contem a celula, com o NOME do
framework no TEXTO do link. As 2 ancoras usadas aqui (ja verificadas existindo):
- `#frameworks-foundational-load-bearing` (contem backward design, GRR/worked-example, expertise-reversal)
- `#mayer-so-3-de-12-principios-em-texto-puro` (ressalva Mayer)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-templates-e-sintaxe-reference-md/02-CONTEXT.md
@.planning/phases/02-templates-e-sintaxe-reference-md/02-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@mentor/reference.md
@mentor/fundamentos.md

<interfaces>
<!-- Ancoras REAIS de fundamentos.md (verificadas existindo como heading): -->
## Frameworks foundational (load-bearing)   -> #frameworks-foundational-load-bearing
### Mayer: so 3 de 12 principios em texto puro -> #mayer-so-3-de-12-principios-em-texto-puro
<!-- NAO existe #backward-design, #grr, #expertise-reversal — apontariam para nada. -->

<!-- Verificador anti-leak do plano 02-01: -->
sh .planning/phases/02-templates-e-sintaxe-reference-md/scripts/extract-fenced.sh mentor/reference.md | rg -c "Bloom|backward design|GRR|Mayer|expertise-reversal|constructive alignment|cognitive load"  # DEVE ser 0
</interfaces>

<anti_leak_boundary>
As edicoes deste plano sao majoritariamente em PROSA (FORA dos blocos cercados) — la PODE
nomear framework + linkar. RISCO BAIXO. EXCECAO de atencao:
- A reescrita do corpo da Z6 (L382-395) e prosa de agente — pode nomear GRR e linkar.
- A regra Mayer (D-04, CARGA-03) vai na PROSA "Regras da aula" (`:318-330`), NUNCA dentro do
  bloco ```` ```markdown ```` da aula (`:272-316`). Ponto de risco #3 da pesquisa.
- O fading (Z1/Z2) e prosa de Sondagem/Granularidade — pode nomear expertise-reversal.
Mesmo assim, rodar o grep negativo anti-leak apos cada commit (a reescrita nao deve empurrar
jargao para dentro de nenhum bloco cercado).
</anti_leak_boundary>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever a Z6 — GRR 3 fases real + worked example no "eu faco" (EST-04, CARGA-02) + link GRR (EST-02)</name>
  <read_first>
    - mentor/reference.md (Z6: `### Sintaxe nova de verdade: "eu faco -> nos fazemos -> voce faz"` L382-395 — titulo 3 fases, corpo L388-389 so 2; "nos fazemos" ausente)
    - mentor/fundamentos.md (celula GRR/worked-example L27 vive na secao `## Frameworks foundational (load-bearing)` L19 — ancora `#frameworks-foundational-load-bearing`; confirmar com `rg -n "^#" mentor/fundamentos.md`)
    - .planning/phases/02-templates-e-sintaxe-reference-md/02-CONTEXT.md (D-02 L50-61: estrutura das 3 fases e o gatilho condicional; D-03 L63-74: worked example funde no "eu faco")
    - .planning/phases/02-templates-e-sintaxe-reference-md/02-RESEARCH.md (Pitfall 3 L280-284: NAO criar secao nova, reescrever o corpo; Mapa decisao->ancora L196-209)
  </read_first>
  <files>mentor/reference.md</files>
  <action>
REESCREVER o CORPO da secao `### Sintaxe nova de verdade: "eu faco -> nos fazemos -> voce
faz"` (L384-395), mantendo o TITULO. NAO criar secao nova (Pitfall 3). O corpo atual
(L388-389) entrega so "eu faco -> voce faz"; preencher a etapa "nos fazemos" e torna-la
CONDICIONAL. Estrutura exata a escrever, em prosa de agente (fora de bloco cercado):

As 3 fases, nomeadas e em ordem:
- **eu faco:** o agente MOSTRA um exemplo resolvido completo, explicando cada pedaco, numa
  instancia DIFERENTE da do `TODO(human)` (= worked example analogo, CARGA-02). Para
  substrato zero-absoluto no conceito dominante, isso e OBRIGATORIO antes do solo.
- **nos fazemos:** pratica conjunta guiada — o agente puxa cada micro-decisao, o aluno
  responde, o agente confirma — numa instancia AINDA diferente da do `TODO` solo. (Esta e
  a etapa que faltava no corpo.)
- **voce faz:** o `TODO(human)` solo, o aluno aplica numa variacao.

Gatilho CONDICIONAL do "nos fazemos" (D-02): ligar quando o substrato no conceito dominante
e zero-absoluto/iniciante OU quando o salto exemplo->solo e grande. PULAR a etapa "nos
fazemos" em intermediario/avancado (onde vira atrito — conecta com o fading da Task 2).

Citacao (EST-02, prosa): nomear GRR/liberacao gradual e linkar a secao foundational. Ex:
`A [liberacao gradual de responsabilidade](fundamentos.md#frameworks-foundational-load-bearing)
formaliza tres fases; o exemplo resolvido na fase "eu faco" e um worked example (instancia
diferente).`
Usar a ancora REAL `#frameworks-foundational-load-bearing` (a ancora `#grr` NAO existe).

Preservar o paragrafo final sobre a PISTA (L393-395) — ele continua valido. So reescrever
o miolo de L384-391 para incluir as 3 fases + worked example + gatilho.

CONVENCOES: portugues sem acentos; `TODO(human)` e `fundamentos.md` em backticks; manter o
estilo de **negrito** para as 3 fases (espelha o uso de negrito do doc).
  </action>
  <verify>
    <automated>rg -c "nos fazemos" mentor/reference.md && rg -n "\]\(fundamentos\.md#frameworks-foundational-load-bearing\)" mentor/reference.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "nos fazemos" mentor/reference.md` retorna >= 2 (titulo + corpo; antes era so o titulo).
    - O corpo nomeia as 3 fases: `rg -q "eu faco" mentor/reference.md` E `rg -q "nos fazemos" mentor/reference.md` E `rg -q "voce faz" mentor/reference.md` no CORPO (inspecao L384-395).
    - Gatilho condicional presente: `rg -q "zero-absoluto|iniciante" mentor/reference.md` na zona Z6 e mencao a "salto" exemplo->solo (inspecao).
    - Worked example no "eu faco": `rg -c "instancia diferente|exemplo resolvido" mentor/reference.md` >= 1.
    - Link GRR resolve: `rg -q "\]\(fundamentos\.md#frameworks-foundational-load-bearing\)" mentor/reference.md` retorna 0 (encontrou) E `rg -q "^## Frameworks foundational \(load-bearing\)" mentor/fundamentos.md` retorna 0 (a ancora-alvo existe).
    - NAO criou secao nova duplicada: `rg -c "Sintaxe nova de verdade" mentor/reference.md` == 1.
    - ANTI-LEAK (CONS-01): `sh .planning/phases/02-templates-e-sintaxe-reference-md/scripts/extract-fenced.sh mentor/reference.md | rg -c "Bloom|backward design|GRR|Mayer|expertise-reversal|constructive alignment|cognitive load"` retorna `0`.
    - Sem acentos: `rg -c "[áàâãéêíóôõúç]" mentor/reference.md` retorna 0.
  </acceptance_criteria>
  <done>Corpo da Z6 com 3 fases reais; "nos fazemos" condicional; worked example no "eu faco"; link GRR resolve; anti-leak verde.</done>
</task>

<task type="auto">
  <name>Task 2: Fading por substrato (CARGA-01) em Z1/Z2 + link expertise-reversal e backward design (EST-02)</name>
  <read_first>
    - mentor/reference.md (Z1: `## Sondagem de substrato` L3-32, classificacao 4-niveis L17-22 com "mais buraco, menos pista" L22; Z2: `## Regra de granularidade` L34-44, "Granularidade ~ 1/nivel" L43-44)
    - mentor/fundamentos.md (celula Worked-Example+Expertise-Reversal L27 e Backward Design L24, ambas na secao foundational L19; ancora `#frameworks-foundational-load-bearing`)
    - .planning/phases/02-templates-e-sintaxe-reference-md/02-CONTEXT.md (D-03 fading L63-74: regra de calibragem nomeada; D-01 "porque" backward design via link)
    - .planning/phases/02-templates-e-sintaxe-reference-md/02-RESEARCH.md (CARGA-01 hooks L110; Mapa decisao->ancora L196-209)
  </read_first>
  <files>mentor/reference.md</files>
  <action>
Duas insercoes de PROSA (fora de bloco cercado — risco anti-leak baixo).

(a) FADING NOMEADO (CARGA-01, D-03) — na zona de Sondagem/Granularidade. Apos a tabela de
classificacao por nivel (Z1, L17-22) ou nas "Regras" da Sondagem (L24-32), adicionar uma
regra que NOMEIA o principio expertise-reversal e formaliza o fading (hoje so implicito em
"mais buraco, menos pista"). Texto de agente, ex:

`O andaime RECUA conforme a maestria do assunto sobe — e o [expertise-reversal
effect](fundamentos.md#frameworks-foundational-load-bearing): suporte demais ATRAPALHA o
avancado (vira ruido), enquanto o iniciante precisa do exemplo resolvido. Calibre a
densidade de andaime+pista pelo substrato DO ASSUNTO: zero-absoluto/iniciante -> exemplo
resolvido + pergunta-guia + pista detalhada; intermediario -> pergunta-guia + scaffold
padrao; avancado -> mais buraco, menos pista (recue o andaime).`

Pode ser tabela substrato -> densidade de andaime (alinhada ao estilo de tabelas do doc) OU
lista; escolher o que fica mais mecanico para o agente ler. Reusar/consolidar a calibragem
ja parcial em L17-22 sem duplicar.

(b) Citacao backward design (EST-02, D-01 "porque") — em UMA das zonas onde a ordem
capacidade-primeiro aparece. O lugar natural e a prosa "Regras do caminho" (`:86-94`) ou o
topo de `## Regra de granularidade`. Adicionar 1 linha de prosa nomeando backward design e
linkando:

`A ordem capacidade-primeiro, artefato-como-evidencia segue [backward
design](fundamentos.md#frameworks-foundational-load-bearing): defina o resultado desejado
(a capacidade) antes da atividade.`

Usar a ancora REAL `#frameworks-foundational-load-bearing` nas duas. NAO inventar
`#backward-design` nem `#expertise-reversal` (nao existem).

CONVENCOES: portugues sem acentos; `fundamentos.md` e identificadores em backticks.
  </action>
  <verify>
    <automated>rg -c "expertise-reversal|suporte demais|recua" mentor/reference.md && rg -c "\]\(fundamentos\.md#" mentor/reference.md</automated>
  </verify>
  <acceptance_criteria>
    - Fading nomeado: `rg -c "expertise-reversal|suporte demais|recua" mentor/reference.md` >= 1 na zona Z1/Z2.
    - Backward design citado: `rg -q "backward design" mentor/reference.md` retorna 0 (encontrou) na prosa.
    - Total de hyperlinks para fundamentos.md (acumulado com Task 1) >= 2: `rg -c "\]\(fundamentos\.md#" mentor/reference.md` >= 2 (EST-02, SC3).
    - Cada ancora usada existe em fundamentos.md: para cada captura de `rg -o "fundamentos\.md#[a-z0-9-]+" mentor/reference.md`, o slug corresponde a um heading de `rg -n "^#" mentor/fundamentos.md` (cross-check da tabela de ancoras da pesquisa; `#frameworks-foundational-load-bearing` confirmado).
    - ANTI-LEAK (CONS-01): `sh .planning/phases/02-templates-e-sintaxe-reference-md/scripts/extract-fenced.sh mentor/reference.md | rg -c "Bloom|backward design|GRR|Mayer|expertise-reversal|constructive alignment|cognitive load"` retorna `0` (os termos novos estao na PROSA, fora dos blocos).
    - Sem acentos: `rg -c "[áàâãéêíóôõúç]" mentor/reference.md` retorna 0.
  </acceptance_criteria>
  <done>Fading nomeado (expertise-reversal) em Z1/Z2; backward design citado; >= 2 links resolvendo; anti-leak verde.</done>
</task>

<task type="auto">
  <name>Task 3: Regra de Mayer com limite honesto + link (CARGA-03, EST-02) em "Regras da aula"</name>
  <read_first>
    - mentor/reference.md (Z4-prosa: `Regras da aula` L318-330 — PROSA fora do bloco cercado da aula; o bloco da aula e L272-316, NAO tocar)
    - mentor/fundamentos.md (ressalva Mayer L70-75: texto-fonte "so 3 de 12 ... coerencia/sinalizacao/segmentacao"; cabecalho `### Mayer: so 3 de 12 principios em texto puro` L70 -> ancora `#mayer-so-3-de-12-principios-em-texto-puro`)
    - .planning/phases/02-templates-e-sintaxe-reference-md/02-CONTEXT.md (D-04 CARGA-03 L82-85: regra de agente, limite honesto, hyperlink)
    - .planning/phases/02-templates-e-sintaxe-reference-md/02-RESEARCH.md (Code Examples L323-331: o formato exato; Pitfall 4 L286-290: nao inflar o claim; Anti-Theory-Leak L244-246 ponto de risco #3)
  </read_first>
  <files>mentor/reference.md</files>
  <action>
Adicionar uma REGRA DE AGENTE na PROSA "Regras da aula" (L318-330), sobre COMO escrever a
aula em texto puro. NUNCA dentro do bloco ```` ```markdown ```` da aula (L272-316) — ponto
de risco #3. Inserir um novo item de lista (mantendo o estilo de **negrito** dos itens
existentes), com os 3 principios de Mayer que transferem para texto puro + linguagem simples
+ o LIMITE honesto "3 de 12" + hyperlink para a ressalva. Texto exato:

```markdown
- **Apresentacao (texto puro):** aplique os 3 principios de Mayer que transferem para
  texto — coerencia (corte o superfluo), sinalizacao (destaque o essencial), segmentacao
  (um passo atomico por vez) — e linguagem simples/legivel. Os outros 9 tratam de
  audio/video e nao se aplicam: ver [so 3 de 12 principios transferem](fundamentos.md#mayer-so-3-de-12-principios-em-texto-puro).
```

CRITICO (Pitfall 4): NAO escrever "aplicamos os principios de Mayer" sem o limite. O numero
"3 de 12" e os 3 nomes (coerencia, sinalizacao, segmentacao) sao obrigatorios. Nao inflar.

Usar a ancora REAL `#mayer-so-3-de-12-principios-em-texto-puro` (esta e a UNICA ancora de
framework-especifico que existe em `fundamentos.md`, pois a ressalva tem cabecalho proprio).

CONVENCOES: portugues sem acentos; `fundamentos.md` em backticks.
  </action>
  <verify>
    <automated>rg -n "3 de 12" mentor/reference.md && rg -n "coerencia" mentor/reference.md && rg -n "\]\(fundamentos\.md#mayer-so-3-de-12-principios-em-texto-puro\)" mentor/reference.md</automated>
  </verify>
  <acceptance_criteria>
    - Limite honesto presente: `rg -c "3 de 12" mentor/reference.md` >= 1 (Pitfall 4).
    - Os 3 principios nomeados: `rg -q "coerencia" mentor/reference.md` E `rg -q "sinalizacao" mentor/reference.md` E `rg -q "segmentacao" mentor/reference.md`.
    - Link Mayer resolve: `rg -q "\]\(fundamentos\.md#mayer-so-3-de-12-principios-em-texto-puro\)" mentor/reference.md` retorna 0 (encontrou) E `rg -q "^### Mayer: so 3 de 12 principios em texto puro" mentor/fundamentos.md` retorna 0 (ancora-alvo existe).
    - A regra esta na PROSA "Regras da aula", NAO dentro do bloco da aula: a string "3 de 12" NAO aparece na saida do extrator de blocos cercados: `sh .planning/phases/02-templates-e-sintaxe-reference-md/scripts/extract-fenced.sh mentor/reference.md | rg -c "3 de 12"` retorna `0`.
    - ANTI-LEAK (CONS-01): `sh .../extract-fenced.sh mentor/reference.md | rg -c "Bloom|backward design|GRR|Mayer|expertise-reversal|constructive alignment|cognitive load"` retorna `0` ("Mayer" esta na prosa, nao no bloco).
    - Sem acentos: `rg -c "[áàâãéêíóôõúç]" mentor/reference.md` retorna 0.
  </acceptance_criteria>
  <done>Regra Mayer com 3 principios + limite "3 de 12" + link resolvendo na prosa "Regras da aula"; jargao fora dos blocos; anti-leak verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — markdown doc edit, no executable surface. `mentor/reference.md` e um doc de metodo
pedagogico: sem autenticacao, sem input de usuario em runtime, sem cripto, sem rede.
Confirmado por 02-RESEARCH.md "Security Domain" (L436-439).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | N/A | edicao de prosa + hyperlinks em markdown | accept | ASVS V1-V14 inaplicaveis — doc estatico, sem superficie de ataque. O risco de correcao relevante (link quebrado, anti-theory-leak CONS-01) e tratado como criterio de aceitacao por task, nao como ameaca de seguranca. |
</threat_model>

<verification>
- `rg -c "nos fazemos" mentor/reference.md` >= 2 (GRR 3 fases real).
- `rg -c "\]\(fundamentos\.md#" mentor/reference.md` >= 2 (EST-02) e cada ancora existe em fundamentos.md.
- `rg -c "3 de 12" mentor/reference.md` >= 1; "3 de 12" NAO aparece dentro de bloco cercado.
- Smoke-test do plano 02-01 todo-verde: EST-02/EST-04/CARGA-01/CARGA-02/CARGA-03 + ANCHOR-RESOLVE + ANTI-LEAK em PASS.
- Manual (02-VALIDATION.md): resolver >= 1 hyperlink no renderizador-alvo.
</verification>

<success_criteria>
- EST-04: corpo da Z6 com 3 fases reais; "nos fazemos" condicional.
- CARGA-02: worked example analogo (instancia diferente) no "eu faco".
- CARGA-01: fading nomeado (expertise-reversal) em Z1/Z2.
- CARGA-03: 3 principios de Mayer + limite "3 de 12" + link, em "Regras da aula".
- EST-02: backward design e GRR nomeados na prosa, linkando fundamentos.md por ancoras reais.
- CONS-01: 0 jargao dentro de blocos cercados; links resolvem (0 quebrados).
</success_criteria>

<output>
After completion, create `.planning/phases/02-templates-e-sintaxe-reference-md/02-03-SUMMARY.md`
</output>
