---
phase: 02-templates-e-sintaxe-reference-md
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - mentor/reference.md
autonomous: true
requirements: [EST-01]
must_haves:
  truths:
    - "Cada passo do template CAMINHO.md tem uma linha 'Objetivo (capacidade):' com verbo, ACIMA de 'Entregavel:'"
    - "O template de aula '## Objetivo do passo' espelha capacidade + artefato (nao so o entregavel)"
    - "O campo 'DONE:' do scaffold esta reenquadrado como evidencia observavel de capacidade"
    - "Nenhum jargao de framework (Bloom, backward design, etc.) aparece DENTRO de qualquer bloco cercado (CONS-01)"
  artifacts:
    - path: "mentor/reference.md"
      provides: "Template CAMINHO + aula + scaffold com o fio do verbo de capacidade"
      contains: "Objetivo (capacidade):"
  key_links:
    - from: "template CAMINHO.md (bloco markdown)"
      to: "template aula '## Objetivo do passo'"
      via: "capacidade declarada no CAMINHO espelhada na aula e cobrada no DONE do scaffold (alinhamento construtivo)"
      pattern: "Objetivo \\(capacidade\\):"
---

<objective>
Tecer o fio do VERBO DE CAPACIDADE (EST-01, D-01) nos tres templates que viram artefato do
aluno: o `CAMINHO.md`, a aula e o scaffold. A capacidade vem PRIMEIRO (norte), o artefato e
a EVIDENCIA. Todas as 3 edicoes acontecem DENTRO de blocos cercados (```` ```markdown ````
e ```` ```text ````) que viram doc do aluno — logo a fronteira anti-theory-leak (CONS-01) e
o risco dominante: SO o verbo concreto, ZERO nome de framework.

Purpose: Estabelecer a FORMA do campo de capacidade que a Fase 3 (`novo-projeto.md` Stage 2)
vai consumir. Backward design explicito: capacidade-primeiro, artefato-como-evidencia.
Output: `mentor/reference.md` editado em 3 zonas (Z3 CAMINHO, Z4 aula, Z5 scaffold).

IMPORTANTE: o "porque" teorico (backward design / constructive alignment) NAO entra aqui —
ele vai na PROSA via hyperlink no plano 02-03 (D-04). Este plano so insere os campos de
capacidade em linguagem pura.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-templates-e-sintaxe-reference-md/02-CONTEXT.md
@.planning/phases/02-templates-e-sintaxe-reference-md/02-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@mentor/reference.md

<interfaces>
<!-- Verificador anti-leak criado no plano 02-01 (Wave 1). Usar em CADA commit. -->
Extrator de blocos cercados:
  sh .planning/phases/02-templates-e-sintaxe-reference-md/scripts/extract-fenced.sh mentor/reference.md
Grep negativo anti-leak (CONS-01), DEVE retornar 0:
  sh .../extract-fenced.sh mentor/reference.md | rg -c "Bloom|backward design|GRR|Mayer|expertise-reversal|constructive alignment|cognitive load"
</interfaces>

<anti_leak_boundary>
Estas 3 edicoes ficam DENTRO de blocos cercados (artefato do aluno) — risco ALTO:
- Z3: dentro do bloco ```` ```markdown ```` do CAMINHO (`reference.md:51-84`)
- Z4: dentro do bloco ```` ```markdown ```` da aula, secao `## Objetivo do passo` (`:280-283`)
- Z5: dentro do bloco ```` ```text ```` do scaffold, campo `DONE:` (`:347`)
REGRA: so o verbo de capacidade puro ("explicar", "construir", "depurar", "conectar").
NUNCA escrever "Bloom", "objetivo cognitivo", "backward design", "constructive alignment".
</anti_leak_boundary>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Linha "Objetivo (capacidade):" em cada passo do CAMINHO (Z3)</name>
  <read_first>
    - mentor/reference.md (Z3: `## Template — CAMINHO.md` L46-94; passos P01 L64-71 e P02 L72-78; cada passo tem `Entregavel:` que e o ancoradouro)
    - .planning/phases/02-templates-e-sintaxe-reference-md/02-CONTEXT.md (D-01, L37-49: forma exata da linha)
    - .planning/phases/02-templates-e-sintaxe-reference-md/02-RESEARCH.md (Code Examples L301-313: o formato verificado; Anti-Theory-Leak L240-243 ponto de risco #1)
  </read_first>
  <files>mentor/reference.md</files>
  <action>
Localizar o bloco ```` ```markdown ```` do template `CAMINHO.md` (secao `## Template —
CAMINHO.md`, "## Passos"). Em CADA passo (P01 e P02), inserir uma linha NOVA exatamente
ACIMA da linha `- Entregavel:`, com a forma exata de D-01:

`- Objetivo (capacidade): ao terminar, voce consegue <verbo> <conceito>`

Resultado esperado para P01 (a linha Objetivo entra entre `Substrato exigido:` e
`Entregavel:`):

```markdown
### P01 — <titulo curto>

- Conceito dominante (novo): c01-<slug>
- Pressupoe: (nenhum — entrada coberta pelo substrato)
- Substrato exigido: <assunto>: zero-absoluto ok ("eu faco -> voce faz")
- Objetivo (capacidade): ao terminar, voce consegue <verbo> <conceito>
- Entregavel: <o que o aluno ESCREVE> + <o que ele VE funcionando ao terminar>
- Arquivos: <paths em projeto/>
```

Fazer o mesmo em P02 (inserir a linha `- Objetivo (capacidade): ...` acima de
`- Entregavel: ...`). Manter o placeholder `<verbo> <conceito>` (e template, nao instancia
real) — `<verbo>` indica um verbo concreto de acao, NUNCA a palavra "Bloom".

ANTI-LEAK (ponto de risco #1, CONS-01): a linha entra DENTRO do bloco cercado. Proibido
qualquer jargao. So `<verbo>` como placeholder de verbo concreto.

CONVENCOES: portugues sem acentos; identificadores em backticks; manter a ordem dos campos
do passo (so inserir entre `Substrato exigido:` e `Entregavel:`).
  </action>
  <verify>
    <automated>rg -n "Objetivo \(capacidade\): ao terminar, voce consegue" mentor/reference.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "Objetivo \(capacidade\): ao terminar, voce consegue" mentor/reference.md` retorna >= 2 (P01 e P02).
    - A linha Objetivo aparece ANTES de `Entregavel:` em cada passo: inspecao confirma ordem (`Substrato exigido:` -> `Objetivo (capacidade):` -> `Entregavel:`).
    - ANTI-LEAK (CONS-01): `sh .planning/phases/02-templates-e-sintaxe-reference-md/scripts/extract-fenced.sh mentor/reference.md | rg -c "Bloom|backward design|GRR|Mayer|expertise-reversal|constructive alignment|cognitive load"` retorna `0`.
    - Sem acentos nas linhas tocadas: `rg -c "[áàâãéêíóôõúç]" mentor/reference.md` retorna 0.
  </acceptance_criteria>
  <done>Linha `Objetivo (capacidade):` presente em P01 e P02 acima de `Entregavel:`; anti-leak verde.</done>
</task>

<task type="auto">
  <name>Task 2: Espelhar capacidade+artefato na aula (Z4) e reenquadrar DONE do scaffold (Z5)</name>
  <read_first>
    - mentor/reference.md (Z4: `## Objetivo do passo` L280-283, hoje so espelha entregavel; Z5: bloco ```` ```text ```` do scaffold `## Formato do scaffold` L332-353, campo `DONE:` em L347)
    - .planning/phases/02-templates-e-sintaxe-reference-md/02-CONTEXT.md (D-01 L41-49: aula espelha capacidade+artefato; DONE reenquadrado como "evidencia observavel de que a capacidade foi adquirida")
    - .planning/phases/02-templates-e-sintaxe-reference-md/02-RESEARCH.md (Anti-Theory-Leak L244-246 ponto de risco #2; State of the Art L335-342)
    - .planning/codebase/CONVENTIONS.md (ordem fixa dos campos do scaffold META..PISTA L27-29: NUNCA reordenar, so reenquadrar o texto de DONE)
  </read_first>
  <files>mentor/reference.md</files>
  <action>
Duas edicoes, ambas DENTRO de blocos cercados (artefato do aluno).

(a) Z4 — secao `## Objetivo do passo` do template de aula (dentro do bloco ```` ```markdown ````
da aula). Hoje o corpo (L282-283) so descreve o entregavel:
`<o que vai ficar pronto ao final de P0x — espelha o "Entregavel" do CAMINHO.md, concreto e
observavel, em 1-2 linhas>`.
Reescrever para espelhar CAPACIDADE + ARTEFATO (alinhamento com a linha nova do CAMINHO),
em linguagem simples, SEM jargao:

```markdown
## Objetivo do passo

<capacidade: ao terminar, voce consegue <verbo> <conceito>; e o artefato que prova isso:
o que vai ficar pronto, concreto e observavel — espelha o "Objetivo (capacidade)" e o
"Entregavel" do CAMINHO.md, em 1-2 linhas>
```

(b) Z5 — campo `DONE:` do bloco ```` ```text ```` do scaffold (L347). Hoje:
`DONE: <como o aluno sabe que terminou>`.
Reenquadrar como evidencia observavel de CAPACIDADE (combate proxy-completion), SEM citar
"constructive alignment" nem outro framework:

`DONE: <evidencia observavel de que voce JA CONSEGUE <a capacidade do passo>: o que roda/aparece quando voce a demonstra, nao so "o codigo compila">`

NAO reordenar os campos do scaffold (ordem META..PISTA e contrato inviolavel,
`CONVENTIONS.md`). So trocar o TEXTO do campo `DONE:`.

ANTI-LEAK (ponto de risco #2, CONS-01): ambas as edicoes ficam DENTRO de bloco cercado.
Proibido jargao. So linguagem de capacidade observavel em palavras simples.

CONVENCOES: portugues sem acentos; identificadores em backticks.
  </action>
  <verify>
    <automated>rg -n "^## Objetivo do passo" mentor/reference.md && rg -n "DONE: <evidencia observavel" mentor/reference.md</automated>
  </verify>
  <acceptance_criteria>
    - `## Objetivo do passo` agora menciona capacidade: `rg -A2 "^## Objetivo do passo" mentor/reference.md | rg -q "capacidade|consegue"` retorna 0 (encontrou).
    - O campo DONE foi reenquadrado: `rg -c "DONE: <evidencia observavel de que voce JA CONSEGUE" mentor/reference.md` retorna 1.
    - Ordem dos campos do scaffold preservada: `META`, `PORQUE`, `PRESSUPOE`, `ARQUIVOS`, `EXEMPLO-DE-RESULTADO`, `DONE`, `PERGUNTA-GUIA`, `TODO(human)`, `PISTA` ainda aparecem nessa sequencia (inspecao do bloco L337-353).
    - ANTI-LEAK (CONS-01): `sh .planning/phases/02-templates-e-sintaxe-reference-md/scripts/extract-fenced.sh mentor/reference.md | rg -c "Bloom|backward design|GRR|Mayer|expertise-reversal|constructive alignment|cognitive load"` retorna `0`.
    - Sem acentos: `rg -c "[áàâãéêíóôõúç]" mentor/reference.md` retorna 0.
  </acceptance_criteria>
  <done>Aula espelha capacidade+artefato; DONE reenquadrado como evidencia de capacidade; ordem do scaffold intacta; anti-leak verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — markdown doc edit, no executable surface. Esta fase edita um doc de metodo pedagogico
em markdown (`mentor/reference.md`): sem autenticacao, sem input de usuario em runtime, sem
cripto, sem rede. Confirmado por 02-RESEARCH.md "Security Domain" (L436-439).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | N/A | edicao de template markdown | accept | ASVS V1-V14 inaplicaveis — doc estatico, sem superficie de ataque. O unico "risco de correcao" relevante e o anti-theory-leak (CONS-01), tratado como criterio de aceitacao em cada task, nao como ameaca de seguranca. |
</threat_model>

<verification>
- `rg -c "Objetivo \(capacidade\): ao terminar, voce consegue" mentor/reference.md` >= 2.
- `## Objetivo do passo` menciona capacidade; `DONE:` reenquadrado como evidencia de capacidade.
- Anti-leak verde apos CADA task: extrator + grep negativo = 0.
- Smoke-test do plano 02-01: checagens EST-01a/EST-01b/ANTI-LEAK em PASS.
</verification>

<success_criteria>
- EST-01 satisfeito: capacidade explicita no CAMINHO (acima do entregavel), espelhada na aula, cobrada no DONE.
- CONS-01 preservado: 0 jargao dentro de blocos cercados.
- Convencoes mantidas: sem acentos, identificadores em backticks, ordem do scaffold intacta.
</success_criteria>

<output>
After completion, create `.planning/phases/02-templates-e-sintaxe-reference-md/02-02-SUMMARY.md`
</output>
