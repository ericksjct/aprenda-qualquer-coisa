---
phase: 07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - scripts/consulta_livro.py
  - tests/test_consulta_livro.py
autonomous: true
requirements: [D-07, D-09, D-10, D-14]

must_haves:
  truths:
    - "consulta_livro.py \"<query>\" --slug <slug> [-k N] le o .index/ e devolve top-k por DADOS (trecho + pagina + latex + page_image_path)"
    - "A busca e hibrida: KNN denso (sqlite-vec vec0) + match esparso (FTS5/BM25) fundidos por RRF"
    - "A query e vetorizada com bge-m3 local (custo = so embeddar a query; 0 token pago)"
    - "Ha preflight de sqlite-vec + FTS5 com mensagem instruindo pysqlite3-binary se faltar"
    - "Toda query SQL usa bind params; sanitize_slug confina o acesso a .projetos/<slug>/livro/.index/"
  artifacts:
    - path: "scripts/consulta_livro.py"
      provides: "CLI de consulta hibrida (molde roadmap_fetch.py); rrf; sqlite_preflight"
      contains: "def rrf"
    - path: "tests/test_consulta_livro.py"
      provides: "Testes GREEN: rrf, sqlite_preflight, cli_shape (sem modelo)"
      exports: ["test_rrf", "test_sqlite_preflight", "test_cli_shape"]
  key_links:
    - from: "scripts/consulta_livro.py"
      to: ".projetos/<slug>/livro/.index/vectors.db"
      via: "KNN vec0 MATCH + FTS5 MATCH, mesmo schema do plano 03"
      pattern: "vec_chunks|fts_chunks"
    - from: "rrf(dense_ranked, sparse_ranked)"
      to: "top-k fundido"
      via: "Reciprocal Rank Fusion k=60"
      pattern: "1\\s*/\\s*\\(k\\s*\\+\\s*rank\\)"
---

<objective>
Wave 3: criar `scripts/consulta_livro.py` (NOVO), o estagio de CONSULTA. CLI no molde de
`roadmap_fetch.py` (D-09): `consulta_livro.py "<query>" --slug <slug> [-k N]`. Vetoriza a query
com bge-m3 local, faz busca hibrida denso(sqlite-vec vec0)+esparso(FTS5/BM25) sobre o `.index/`
construido no plano 03, funde por RRF e devolve top-k como DADOS por linha (trecho + pagina +
latex + page_image_path) — D-10. O tutor multimodal le esses dados (e a imagem-de-pagina quando
o LaTeX for duvidoso).

Esta wave torna VERDES os testes RED `test_rrf`/`test_sqlite_preflight`/`test_cli_shape` (plano 01).

Purpose: e a interface que o metodo (plano 05) costura no tutor; fecha a vertical ingestao->consulta.
Output: `consulta_livro.py` + testes de logica GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid/07.1-CONTEXT.md
@.planning/phases/07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid/07.1-RESEARCH.md
@.planning/phases/07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid/07.1-PATTERNS.md
@scripts/roadmap_fetch.py
@scripts/converte_livro.py
@tests/test_consulta_livro.py
@tests/test_converte_livro.py

<interfaces>
<!-- Molde de CLI (roadmap_fetch.py linhas 142-164; --slug required como converte_livro.py:294) -->
```python
def main() -> None:
    parser = argparse.ArgumentParser(description=__doc__.splitlines()[0])
    parser.add_argument("query", help="pergunta/conceito a buscar no livro")
    parser.add_argument("--slug", required=True, help="slug do projeto (kebab-case)")
    parser.add_argument("-k", "--top-k", type=int, default=5, help="quantos trechos retornar")
    args = parser.parse_args()
    slug = sanitize_slug(args.slug)   # reusar/importar de converte_livro (path-safety)
```

<!-- RRF (RESEARCH Pattern 4) — assinatura EXATA que o teste do plano 01 espera -->
```python
def rrf(dense_ranked, sparse_ranked, k=60):
    scores = {}
    for rank, cid in enumerate(dense_ranked):  scores[cid] = scores.get(cid,0)+1/(k+rank)
    for rank, cid in enumerate(sparse_ranked): scores[cid] = scores.get(cid,0)+1/(k+rank)
    return sorted(scores, key=scores.get, reverse=True)
```

<!-- Schema lido (DDL do plano 03) -->
vec_chunks USING vec0(embedding float[1024]); fts_chunks USING fts5(text, content='')
corpus.jsonl: {"id","text","page","chapter","concept_hint","latex"[],"page_image_path","parent_id"}

<!-- Saida por DADOS (D-10): JSON por linha -->
[{"trecho": str, "pagina": int, "latex": [str], "page_image_path": str}]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: consulta_livro.py — CLI + rrf + sqlite_preflight (logica pura, testes GREEN)</name>
  <read_first>
    - scripts/roadmap_fetch.py (docstring 1-16; main 142-164; padrao "le, imprime, sai")
    - scripts/converte_livro.py (sanitize_slug 50-62; env guards 41-45 — reusar/espelhar)
    - tests/test_consulta_livro.py (testes RED rrf/sqlite_preflight/cli_shape do plano 01)
    - .planning/phases/07.1-.../07.1-RESEARCH.md (Pattern 4 rrf; Pattern 6 CLI; Pitfall 5 preflight; Security)
    - .planning/phases/07.1-.../07.1-PATTERNS.md (secao consulta_livro.py: shape de argparse; bind params)
  </read_first>
  <files>scripts/consulta_livro.py, tests/test_consulta_livro.py</files>
  <behavior>
    - rrf(dense_ranked, sparse_ranked, k=60): exatamente o interface-block; `rrf([1,2,3],[3,1,2])` poe 1 e 3 acima de 2.
    - sqlite_preflight(): testa enable_load_extension + FTS5; retorna True nesta maquina; em falta, mensagem instruindo `pip install pysqlite3-binary` (Pitfall 5).
    - parser CLI: aceita `query --slug s -k 3`; `sanitize_slug` rejeita `..`/separadores; default top-k=5.
    - O modulo importa SEM FlagEmbedding (import lazy dentro da funcao de busca).
  </behavior>
  <action>
    1. Cabecalho identico ao padrao do repo: shebang, docstring de modulo com bloco `Uso:`
       (exemplos `python scripts/consulta_livro.py "taxa continua" --slug mat-financeira -k 5` +
       nota "roda LOCAL, so embeda a query, nao consome token pago"). Reproduzir as 5 linhas de
       blindagem de memoria (OMP/OPENBLAS/MKL/VECLIB/NUMEXPR = "1") ANTES de qualquer import
       pesado (espelho de converte_livro.py:41-45).
    2. Reusar `sanitize_slug` importando de `scripts.converte_livro` (mesma path-safety).
    3. Implementar `rrf(...)` exatamente como o interface-block (RESEARCH Pattern 4).
    4. Implementar `sqlite_preflight()` (Pitfall 5): conectar `:memory:`, `enable_load_extension(True)`,
       `import sqlite_vec; sqlite_vec.load(db)`, `CREATE VIRTUAL TABLE t USING fts5(x)`; em
       exception, `sys.exit("sqlite-vec/FTS5 indisponivel; rode: pip install pysqlite3-binary")`.
    5. `main()` no molde do interface-block; resolver
       `index_dir = Path(".projetos")/slug/"livro"/".index"`; se `vectors.db` ausente, sair
       amigavel (espelho `fetch_roadmap` 35-40 e `converte_livro` 322).
    6. Tornar VERDES `test_rrf`, `test_sqlite_preflight`, `test_cli_shape` (plano 01); ajustar so
       o minimo no teste se a assinatura final divergir (sem afrouxar asserções).
  </action>
  <verify>
    <automated>.venv-pdf\Scripts\python -m pytest tests/test_consulta_livro.py -q -k "rrf or sqlite_preflight or cli_shape" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def rrf" scripts/consulta_livro.py` == 1 e contem `1/(k+rank)` (ou `1 / (k + rank)`)
    - `grep -c "def sqlite_preflight" scripts/consulta_livro.py` == 1 e menciona `pysqlite3-binary`
    - `grep -c "OMP_NUM_THREADS" scripts/consulta_livro.py` == 1 (blindagem espelhada)
    - `grep -c "from scripts.converte_livro import sanitize_slug" scripts/consulta_livro.py` >= 1 (ou import equivalente)
    - `pytest tests/test_consulta_livro.py -k "rrf or sqlite_preflight or cli_shape"` => PASS (GREEN)
    - modulo importa sem FlagEmbedding: `python -c "import scripts.consulta_livro"` na suite stdlib OK
  </acceptance_criteria>
  <done>rrf, sqlite_preflight e o parser CLI existem e passam; blindagem e path-safety espelhadas; testes do plano 01 GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Busca hibrida (vec0 KNN + FTS5) fundida por RRF + saida por DADOS (D-10)</name>
  <read_first>
    - scripts/consulta_livro.py (Task 1: rrf, sqlite_preflight, main scaffold)
    - scripts/converte_livro.py (open_index_db/DDL do plano 03 — mesmo schema)
    - .planning/phases/07.1-.../07.1-RESEARCH.md (Pattern 4 busca hibrida + RRF; Pattern 6; Security: bind params)
    - tests/test_consulta_livro.py (test_cli_shape — saida JSON por linha)
  </read_first>
  <files>scripts/consulta_livro.py, tests/test_consulta_livro.py</files>
  <action>
    1. `embed_query(text)`: import LAZY `from FlagEmbedding import BGEM3FlagModel`; carregar
       `BGEM3FlagModel("BAAI/bge-m3", use_fp16=False)`; `model.encode([text], return_dense=True)`
       -> vetor 1024. (Custo = so embeddar 1 frase — D-10.)
    2. `search(index_dir, query, top_k)`: abrir `vectors.db` com a mesma carga de sqlite-vec do
       plano 03 (`enable_load_extension(True); sqlite_vec.load(db); enable_load_extension(False)`).
       - KNN denso: `SELECT rowid FROM vec_chunks WHERE embedding MATCH ? ORDER BY distance LIMIT ?`
         (passar o vetor serializado via `sqlite_vec.serialize_float32` e bind params).
       - Esparso: `SELECT rowid FROM fts_chunks WHERE fts_chunks MATCH ? LIMIT ?` (query como bind param).
       - fundir as duas listas de rowid por `rrf(dense_ranked, sparse_ranked)`; pegar top_k.
       - hidratar cada rowid lendo a linha correspondente do `corpus.jsonl` (provenance completo).
    3. Saida por DADOS (D-10): imprimir, por linha, JSON
       `{"trecho","pagina","latex","page_image_path"}` para cada hit (NAO prosa). Reusar
       `json.dumps(..., ensure_ascii=False)`.
    4. SQL: SEMPRE bind params `?` (nunca f-string com a query do usuario — Security/Injection).
    5. Estender `test_cli_shape` (ou adicionar `test_search_output_shape`) mockando `embed_query`
       e a conexao para asseverar que a saida e JSON por linha com as 4 chaves (sem baixar modelo).
  </action>
  <verify>
    <automated>.venv-pdf\Scripts\python -m pytest tests/test_consulta_livro.py -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def search" scripts/consulta_livro.py` == 1 e `grep -c "def embed_query" scripts/consulta_livro.py` == 1
    - `grep -c "embedding MATCH" scripts/consulta_livro.py` >= 1 (KNN denso)
    - `grep -c "fts_chunks MATCH" scripts/consulta_livro.py` >= 1 (esparso)
    - `grep -c "use_fp16=False" scripts/consulta_livro.py` == 1
    - nenhuma query SQL com f-string de input: `grep -n "execute(f" scripts/consulta_livro.py` => 0
    - a saida e JSON por linha com chaves `trecho`/`pagina`/`latex`/`page_image_path` (teste assevera)
    - suite `tests/test_consulta_livro.py` toda GREEN (logica pura, sem modelo)
  </acceptance_criteria>
  <done>busca hibrida vec0+FTS5 fundida por RRF; saida por dados com provenance; SQL por bind params; suite GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| query do usuario -> SQL (vec0/FTS5) | input nao confiavel entra na busca |
| --slug -> path do .index/ | componente de path do usuario |
| consulta -> filesystem (read) | le `.projetos/<slug>/livro/.index/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-12 | Injection | query -> FTS5/vec0 MATCH | mitigate | bind params `?` em todo execute; query do usuario nunca interpolada via f-string |
| T-07.1-13 | Tampering | `--slug` (path traversal) | mitigate | `sanitize_slug` (whitelist `^[a-z0-9-]+$`, rejeita `..`/sep) antes de resolver index_dir |
| T-07.1-14 | Tampering | `enable_load_extension` ligado | mitigate | ligar so para carregar sqlite-vec; desligar logo apos (Pattern 4) |
| T-07.1-15 | DoS | `-k` enorme / query gigante | accept | uso local single-user; `-k` int do argparse; custo de embeddar 1 query e baixo |
</threat_model>

<verification>
- `pytest tests/test_consulta_livro.py` toda GREEN (rrf, sqlite_preflight, cli_shape, search output).
- `search` usa `embedding MATCH` (denso) + `fts_chunks MATCH` (esparso) + `rrf`; saida por dados.
- SQL por bind params; sanitize_slug aplicado; blindagem espelhada.
- modulo importavel sem FlagEmbedding (import lazy).
</verification>

<success_criteria>
- CLI `consulta_livro.py "<query>" --slug <slug> [-k N]` devolve top-k por DADOS (D-09/D-10).
- Busca hibrida denso+esparso fundida por RRF (D-07); query embeddada local (0 token pago).
- Preflight sqlite-vec+FTS5; path-safety e bind params (Security).
</success_criteria>

<output>
Apos completar, crie `.planning/phases/07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid/07.1-04-SUMMARY.md`.
</output>
