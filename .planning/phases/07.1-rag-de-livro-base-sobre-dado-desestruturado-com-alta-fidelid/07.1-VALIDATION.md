---
phase: 07.1
slug: rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-23
---

# Phase 07.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from RESEARCH.md `## Validation Architecture`. The planner fills the
> Per-Task Verification Map; the golden set (D-17) is the phase gate.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (existing suite em `tests/`) |
| **Config file** | `tests/conftest.py` (fixtures existentes; ver `tests/fixtures/sample.pdf`) |
| **Quick run command** | `.venv-pdf\Scripts\python -m pytest tests/ -q` (logic tests sem deps pesadas) |
| **Full suite command** | `.venv-pdf\Scripts\python -m pytest tests/ -v` (inclui golden smoke + golden set D-17) |
| **Estimated runtime** | ~TBD (planner mede; golden set sobre PDF real pode ser lento — marcar `@pytest.mark.slow`) |

---

## Sampling Rate

- **After every task commit:** Run quick run command
- **After every plan wave:** Run full suite command
- **Before `/gsd-verify-work`:** Full suite must be green + golden set (D-17) acima do limiar
- **Max feedback latency:** TBD (planner define; manter testes de logica rapidos e isolar os lentos)

---

## Per-Task Verification Map

> Planner preenche durante o planejamento. Toda task com `<automated>` verify
> OU dependencia de Wave 0.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | D-XX | — | N/A | unit | `TBD` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Recriar `.venv-pdf` com `py -3.12` (BLOCKING — MinerU nao instala em 3.14) antes de qualquer teste com deps de RAG
- [ ] `tests/` — stubs para a logica deterministica (chunking, boundary-cut por histograma, provenance, fusao RRF) sem baixar modelos
- [ ] Golden set (D-17) como teste-gate sobre `.projetos/mat-financeira/` — marcado `slow`, roda na full suite

*Logica deterministica (chunking, provenance, parsing de content_list.json, fusao de score) deve ser testavel SEM baixar modelos — usar fixtures.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Sanidade do tutor numa amostra do golden set ("a pagina N realmente contem o citado?") | D-17 | Julgamento semantico/visual nao automatizavel | Numa sessao real, consultar 2-3 conceitos e conferir pagina+formula contra o PDF |
| Legibilidade da imagem-rede da equacao quando o LaTeX vier duvidoso (D-02) | D-02/D-17 | Avaliacao visual da imagem recortada | Abrir `page_image_path` retornado e confirmar a formula legivel |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (incl. venv-3.12 recreation)
- [ ] No watch-mode flags
- [ ] Feedback latency acceptable (logic tests fast; golden set isolated as slow)
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
