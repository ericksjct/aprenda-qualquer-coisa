---
phase: 07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid
plan: 03
type: execute
wave: 2
depends_on: [02]
files_modified:
  - scripts/converte_livro.py
  - tests/test_converte_livro.py
autonomous: true
requirements: [D-06, D-07, D-08, D-14, D-15]

must_haves:
  truths:
    - "A lista de chunks (build_chunks, plano 02) e vetorizada com bge-m3 em CPU (use_fp16=False) e gravada num indice consultavel"
    - "O indice mora em .projetos/<slug>/livro/.index/ com corpus.jsonl + vectors.db (vec0 denso + FTS5 esparso) + images/"
    - "O denso (sqlite-vec vec0, 1024-dim) e o esparso (FTS5/BM25) coexistem no MESMO arquivo .db"
    - "A indexacao e deterministica e testavel via fixtures sem baixar bge-m3 (model load lazy)"
    - "enable_load_extension e ligado so para carregar sqlite-vec e desligado logo apos (Security V12)"
  artifacts:
    - path: "scripts/converte_livro.py"
      provides: "build_index(): bge-m3 encode + DDL vec0/FTS5 + grava .index/"
      contains: "def build_index"
    - path: "tests/test_converte_livro.py"
      provides: "Testes de DDL/corpus.jsonl shape (sem modelo) + indexacao mockada"
      exports: ["test_index_corpus_shape", "test_index_ddl"]
  key_links:
    - from: "build_index"
      to: ".projetos/<slug>/livro/.index/vectors.db"
      via: "vec0 (dense) + fts5 (sparse) no mesmo .db"
      pattern: "vec0|fts5"
    - from: "build_index"
      to: "bge-m3 (FlagEmbedding)"
      via: "model.encode(return_dense=True, return_sparse=True), use_fp16=False"
      pattern: "use_fp16=False"
---

<objective>
Wave 2: transformar a lista de chunks (de `build_chunks`, plano 02) num INDICE LOCAL consultavel.
Vetoriza com bge-m3 (D-06, CPU `use_fp16=False`), grava o denso no sqlite-vec `vec0` e o esparso
no FTS5/BM25 no MESMO `vectors.db` (D-07), e persiste `corpus.jsonl` + `images/` em
`.projetos/<slug>/livro/.index/` (D-08). Artefato ADICIONAL dentro de `livro/` — NAO substitui os
`*.md` legiveis (contrato Phase 7).

Esta wave fecha o caminho de INGESTAO. O `consulta_livro.py` (plano 04) consome este `.index/`.

Purpose: sem indice nao ha busca semantica; o chunking do plano 02 vira algo que o tutor consulta.
Output: `build_index()` em `converte_livro.py` + testes de DDL/shape (deterministicos, sem modelo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid/07.1-CONTEXT.md
@.planning/phases/07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid/07.1-RESEARCH.md
@.planning/phases/07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid/07.1-PATTERNS.md
@scripts/converte_livro.py
@tests/test_converte_livro.py

<interfaces>
<!-- DDL CONTRATO (RESEARCH Pattern 4) — o plano 04 (consulta) le este MESMO schema -->
```sql
CREATE VIRTUAL TABLE vec_chunks USING vec0(embedding float[1024]);
CREATE VIRTUAL TABLE fts_chunks USING fts5(text, content='');
-- rowid de ambas as tabelas == chunk rowid (linha do corpus.jsonl, 1-indexada ou id estavel)
```

<!-- Carga sqlite-vec (RESEARCH Pattern 4; Security V12: desligar load_extension apos) -->
```python
import sqlite3, sqlite_vec
db = sqlite3.connect(str(index_dir / "vectors.db"))
db.enable_load_extension(True); sqlite_vec.load(db); db.enable_load_extension(False)
```

<!-- bge-m3 (RESEARCH Stack; Pattern 4) -->
```python
from FlagEmbedding import BGEM3FlagModel
model = BGEM3FlagModel("BAAI/bge-m3", use_fp16=False)   # CPU: fp16 OFF
out = model.encode(texts, return_dense=True, return_sparse=True)  # dense_vecs (1024) + lexical_weights
```

<!-- Schema do chunk (de build_chunks, plano 02) -->
{"id","text","page","chapter","concept_hint","latex"[],"page_image_path","parent_id"}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build_index() — corpus.jsonl + DDL vec0/FTS5 (estrutura, testavel sem modelo)</name>
  <read_first>
    - scripts/converte_livro.py (sanitize_slug 50-62; estrutura de main 291-337; padrao de write em out_dir)
    - tests/test_converte_livro.py (estilo de teste de logica pura + monkeypatch de deps)
    - .planning/phases/07.1-.../07.1-RESEARCH.md (Pattern 4 e 5; Pitfall 5; Security V12)
    - .planning/phases/07.1-.../07.1-PATTERNS.md (secao converte_livro: "D-08 geracao do .index/"; SQL bind params)
  </read_first>
  <files>scripts/converte_livro.py, tests/test_converte_livro.py</files>
  <behavior>
    - write_corpus(chunks, index_dir): grava 1 linha JSON por chunk em `.index/corpus.jsonl` (provenance completo preservado).
    - open_index_db(index_dir): conecta vectors.db, carrega sqlite-vec, cria as 2 virtual tables (DDL acima), desliga load_extension apos carregar.
    - O caminho do indice e sempre `.projetos/<slug>/livro/.index/` (via sanitize_slug).
    - Toda escrita SQL usa bind params `?` (nunca f-string com texto do usuario).
  </behavior>
  <action>
    1. Adicionar `index_dir_for(slug, out_dir=None)` que resolve
       `Path(out_dir or (".projetos"/slug/"livro")) / ".index"` e cria com `mkdir(parents=True, exist_ok=True)`.
    2. Adicionar `write_corpus(chunks, index_dir)`: abre `index_dir/"corpus.jsonl"` e grava
       `json.dumps(chunk, ensure_ascii=False)` por linha (preserva acentos do conteudo).
    3. Adicionar `open_index_db(index_dir)`: imports LAZY `import sqlite3, sqlite_vec`; conecta
       `vectors.db`; `enable_load_extension(True); sqlite_vec.load(db); enable_load_extension(False)`;
       executa a DDL do interface-block (vec0 float[1024] + fts5). Retorna a conexao.
    4. Inserts com bind params: dense em vec_chunks, `text` em fts_chunks; `rowid` = id estavel do
       chunk (mapeado em corpus.jsonl). NAO interpolar texto via f-string (Security/Injection).
    5. Testes (tornar verdes/novos): `test_index_corpus_shape` — `write_corpus` sobre chunks
       sinteticos gera jsonl 1-linha-por-chunk com as chaves de provenance; `test_index_ddl` —
       `open_index_db` cria `vec_chunks`/`fts_chunks` (consultar `sqlite_master`); pode rodar so
       no venv 3.12 (marcar skip gracioso se sqlite_vec ausente).
  </action>
  <verify>
    <automated>.venv-pdf\Scripts\python -m pytest tests/test_converte_livro.py -q -k "index_corpus_shape or index_ddl" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def write_corpus" scripts/converte_livro.py` == 1
    - `grep -c "def open_index_db" scripts/converte_livro.py` == 1
    - `grep -c "vec0(embedding float\[1024\])" scripts/converte_livro.py` == 1
    - `grep -c "fts5" scripts/converte_livro.py` >= 1
    - `grep -c "enable_load_extension(False)" scripts/converte_livro.py` >= 1 (desliga apos carregar)
    - nenhuma query SQL com f-string de texto do usuario (revisao: `grep -n "execute(f" scripts/converte_livro.py` => 0)
    - `pytest -k "index_corpus_shape or index_ddl"` PASS (ou skip gracioso sem sqlite_vec)
  </acceptance_criteria>
  <done>corpus.jsonl e a DDL vec0+FTS5 num unico .db existem; load_extension desligado apos carga; escrita por bind params.</done>
</task>

<task type="auto">
  <name>Task 2: Embeddings bge-m3 + wiring do build_index no pipeline (D-06) + pre-aviso D-15</name>
  <read_first>
    - scripts/converte_livro.py (main 291-337; print de progresso "lote N/M")
    - .planning/phases/07.1-.../07.1-RESEARCH.md (Stack bge-m3; Pitfall 4 download multi-GB; Pitfall 8 lentidao CPU)
  </read_first>
  <files>scripts/converte_livro.py</files>
  <action>
    1. `embed_chunks(chunks)`: import LAZY `from FlagEmbedding import BGEM3FlagModel`; carregar
       `BGEM3FlagModel("BAAI/bge-m3", use_fp16=False)` (CPU — NUNCA fp16=True, Pitfall); retornar
       `dense_vecs` (1024) e `lexical_weights`/`texts` para o FTS5. Expor progresso ("vetorizando
       chunk N de M; roda local; nao consome token") — molde do print de lote ja existente.
    2. `build_index(chunks, slug, out_dir=None)`: orquestra `index_dir_for` -> `write_corpus` ->
       `open_index_db` -> `embed_chunks` -> inserts dense (vec_chunks) + text (fts_chunks).
       Persistir as imagens-de-pagina em `.index/images/` (ja escritas por extract_mineru; aqui
       so referenciar pelo `page_image_path` do chunk).
    3. Wire em `main()`: apos `build_chunks`, se `--engine mineru` (default), chamar `build_index`.
       Manter os `*.md` legiveis (contrato Phase 7) — o `.index/` e ADICIONAL.
    4. **Pre-aviso D-15** (print antes de baixar/vetorizar): "1a execucao baixa modelos
       (MinerU ~1-2 GB + bge-m3 ~2.3 GB = ~3.5-6 GB); roda LOCAL, nao consome token; progresso por
       lote; modelos ficam em cache compartilhado (HF_HOME / MINERU_MODEL_SOURCE) e sao reusados
       entre projetos". Documentar tambem no docstring `Uso:`.
  </action>
  <verify>
    <automated>.venv-pdf\Scripts\python -c "import scripts.converte_livro as cl; assert hasattr(cl,'build_index') and hasattr(cl,'embed_chunks'); print('index wired')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def build_index" scripts/converte_livro.py` == 1
    - `grep -c "def embed_chunks" scripts/converte_livro.py` == 1
    - `grep -c "use_fp16=False" scripts/converte_livro.py` == 1
    - `grep -c "BAAI/bge-m3" scripts/converte_livro.py` == 1
    - pre-aviso D-15 presente: `grep -ci "nao consome token" scripts/converte_livro.py` >= 1 e mencao a "modelos" / GB
    - o modulo segue importavel sem FlagEmbedding (import lazy): `python -c "import scripts.converte_livro"` na suite stdlib OK
    - `main` chama `build_index` apos `build_chunks` (revisao do fluxo)
  </acceptance_criteria>
  <done>build_index grava o indice hibrido completo; bge-m3 em CPU fp16=OFF; pre-aviso D-15 emitido; *.md preservados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chunks -> sqlite (vec0/FTS5) | texto indexado em SQL (risco de injection se interpolado) |
| build_index -> filesystem | `.index/` gravado sob `.projetos/<slug>/livro/` |
| HF download -> cache | bge-m3 baixado de HuggingFace (integridade/origem) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-08 | Injection | inserts no vec0/FTS5 | mitigate | bind params `?` em todo execute; nunca `execute(f"...{texto}...")` |
| T-07.1-09 | Tampering | `enable_load_extension` ligado | mitigate | ligar so para carregar sqlite-vec; `enable_load_extension(False)` logo apos (Pattern 4) |
| T-07.1-10 | Tampering | path do `.index/` (slug) | mitigate | `sanitize_slug` antes de resolver index_dir; gravar so dentro de `.projetos/<slug>/livro/.index/` |
| T-07.1-11 | Tampering | download bge-m3 (HF) | accept | fonte oficial HuggingFace HTTPS; cache compartilhado; uso local single-user |
</threat_model>

<verification>
- `pytest tests/test_converte_livro.py -k "index_corpus_shape or index_ddl"` GREEN (ou skip gracioso sem sqlite_vec).
- `build_index`/`embed_chunks` existem; bge-m3 `use_fp16=False`; DDL vec0+FTS5 no mesmo .db.
- modulo importavel sem FlagEmbedding/sqlite_vec (imports lazy).
- pre-aviso D-15 presente.
</verification>

<success_criteria>
- `.index/` com corpus.jsonl + vectors.db (vec0 denso + FTS5 esparso) + images/ (D-07/D-08).
- bge-m3 em CPU fp16=OFF (D-06); load_extension desligado apos carga (Security).
- *.md legiveis preservados; pre-aviso de download multi-GB emitido (D-15).
</success_criteria>

<output>
Apos completar, crie `.planning/phases/07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid/07.1-03-SUMMARY.md`.
</output>
