---
phase: 07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - mentor/tutor.md
  - mentor/novo-projeto.md
  - mentor/reference.md
  - .claude/skills/converte-livro/SKILL.md
  - scripts/converte-livro.ps1
  - tests/test_golden_set.py
autonomous: false
requirements: [D-11, D-12, D-13, D-15, D-16, D-17, D-18]

must_haves:
  truths:
    - "O tutor consulta o indice (consulta_livro.py) em vez de ler livro/*.md cru, mantendo o protocolo de divergencia (APRENDIZADO.md)"
    - "O bootstrap (Passo 3b) tambem CONSTROI o indice apos converter; o pre-aviso inclui o download de modelos multi-GB na 1a execucao"
    - "reference.md documenta livro/.index/ no layout de .projetos/<slug>/"
    - "O venv e provisionado com py -3.12 (skill + ps1 refletem o pin)"
    - "O corpus mat-financeira e re-ingerido (778 cacos -> novo pipeline) com confirmacao de overwrite; a pagina 47 passa a conter a formula"
    - "O golden set (D-17) passa o limiar: hit@3 pagina >=80% (>=12/15), hit@1 >=60%, formula presente-e-legivel 100% dos conceitos-com-formula"
  artifacts:
    - path: "mentor/tutor.md"
      provides: "Passo de consulta ao indice + protocolo de divergencia preservado"
      contains: "consulta_livro"
    - path: "mentor/novo-projeto.md"
      provides: "Passo 3b constroi o indice + pre-aviso D-15 estendido"
      contains: ".index"
    - path: "mentor/reference.md"
      provides: "livro/.index/ no layout de .projetos/<slug>/"
      contains: ".index"
    - path: "scripts/converte-livro.ps1"
      provides: "Provisionamento py -3.12 + pre-aviso multi-GB"
      contains: "3.12"
    - path: "tests/test_golden_set.py"
      provides: "Gate executavel (hit@1/hit@3 + formula-presente) sobre o .index/ real"
      contains: "hit"
  key_links:
    - from: "mentor/tutor.md"
      to: "scripts/consulta_livro.py"
      via: "passo de consulta ao indice no Passo 0/3"
      pattern: "consulta_livro"
    - from: "tests/test_golden_set.py"
      to: ".projetos/mat-financeira/livro/.index/"
      via: "consulta real sobre o indice re-ingerido"
      pattern: "mat-financeira"
---

<objective>
Wave 4: COSTURA no metodo (fonte-unica em `mentor/`) + GATE da phase. Tece a consulta ao indice
no tutor (D-11), faz o bootstrap construir o indice com pre-aviso estendido (D-12/D-15), documenta
`livro/.index/` no layout (D-13), e alinha skill/ps1 ao pin Python 3.12. Depois RE-INGERE o corpus
mat-financeira (D-18, 778 cacos -> novo pipeline, com confirmacao de overwrite) e roda o GOLDEN SET
(D-17) como gate da phase — incluindo a pagina 47 auto-validante (`I=ln(1+i)`).

Edicoes de PROSA em PT SEM acentos (convencao `mentor/`/repo). Se o golden set REPROVAR, avaliar a
Fatia 4 (ColPali, DEFERIDA) antes de declarar a phase fechada — NAO planejar Fatia 4 aqui.

Purpose: o "done" da phase (CONTEXT D-16) e o tutor, numa sessao real, citar pagina+formula
corretas com o golden set acima do limiar. Output: mentor costurado + skill/ps1 + gate verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid/07.1-CONTEXT.md
@.planning/phases/07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid/07.1-RESEARCH.md
@.planning/phases/07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid/07.1-PATTERNS.md
@.planning/phases/07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid/07.1-VALIDATION.md
@mentor/tutor.md
@mentor/novo-projeto.md
@mentor/reference.md
@.claude/skills/converte-livro/SKILL.md
@scripts/converte-livro.ps1
@scripts/consulta_livro.py
@tests/test_golden_set.py
@tests/fixtures/golden_set.json
@.projetos/mat-financeira/CAMINHO.md

<interfaces>
<!-- Ancoras de edicao (PATTERNS.md "Costura no metodo") -->
mentor/tutor.md: Passo 0 (linhas 29-31, entrada livro/*.md); Passo 3 (linhas 79-88, ancora de
  pagina + protocolo de divergencia APRENDIZADO.md); Roteamento (125-127).
mentor/novo-projeto.md: Passo 3b (linhas 52-92); pre-aviso (67-90); nota de saida (86-88).
mentor/reference.md: layout `.projetos/<slug>/` (228-242, ja lista livro/ e livro-fonte/);
  "Idioma e acentuacao" herda regra de idioma para o .index/.
.claude/skills/converte-livro/SKILL.md: requisitos NAO-NEGOCIAVEIS (13-24).
scripts/converte-livro.ps1: provisionamento do venv (84-89, hoje `python -m venv`); pre-aviso (91-97, "~2GB").

<!-- CLI de consulta (do plano 04) que o tutor invoca -->
python scripts/consulta_livro.py "<query>" --slug <slug> [-k N]  -> JSON por linha
  {"trecho","pagina","latex","page_image_path"}

<!-- Limiar D-17 (CONTEXT) -->
hit@3 pagina >= 80% (>=12/15); hit@1 pagina >= 60%; formula presente-e-legivel 100% dos
conceitos-com-formula (LaTeX correto OU imagem-de-pagina legivel).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Costura no metodo (tutor/novo-projeto/reference) + skill + ps1</name>
  <read_first>
    - mentor/tutor.md (Passo 0 linhas 29-31; Passo 3 linhas 79-88; Roteamento 125-127)
    - mentor/novo-projeto.md (Passo 3b linhas 52-92; pre-aviso 67-90)
    - mentor/reference.md (layout linhas 228-242)
    - .claude/skills/converte-livro/SKILL.md (linhas 13-28)
    - scripts/converte-livro.ps1 (linhas 81-101)
    - .planning/phases/07.1-.../07.1-PATTERNS.md (secao "Costura no metodo" — ancoras exatas)
  </read_first>
  <files>mentor/tutor.md, mentor/novo-projeto.md, mentor/reference.md, .claude/skills/converte-livro/SKILL.md, scripts/converte-livro.ps1</files>
  <action>
    Todas as edicoes em PT SEM acentos (regra do `mentor/`/repo). PRESERVAR o protocolo de
    divergencia e a estrutura existente — ESTENDER, nao reescrever.
    1. **mentor/tutor.md (D-11):** no Passo 0 (entrada `livro/*.md`, linhas 29-31), trocar a
       leitura crua por: quando existir `livro/.index/`, CONSULTAR o indice via
       `python scripts/consulta_livro.py "<conceito>" --slug <slug>` (roda local, 0 token pago) e
       usar os top-k (trecho+pagina+latex+page_image_path) como baseline. No Passo 3 (79-88),
       MANTER o protocolo de divergencia (APRENDIZADO.md datado) e a citacao de pagina — agora o
       mentor LE a formula (latex OU a page_image quando o latex for duvidoso) para comparar com a
       pratica. Alinhar o roteamento (125-127).
    2. **mentor/novo-projeto.md (D-12/D-15):** no Passo 3b, apos converter o PDF, o fluxo TAMBEM
       constroi o `.index/` (o `.ps1`/`converte_livro.py` ja o faz). Estender o pre-aviso (67-90)
       para incluir: "a 1a execucao baixa modelos (~3.5-6 GB: MinerU + bge-m3), roda LOCAL, nao
       consome token, mostra progresso por lote; os modelos ficam em cache e sao reusados entre
       projetos". A nota de saida (86-88) ganha `livro/.index/` alem de `livro/*.md`.
    3. **mentor/reference.md (D-13):** na arvore do layout (228-242), adicionar sob `livro/`:
       ```text
       |   `-- .index/            # indice de busca (corpus.jsonl + vectors.db + images/) — gerado, gitignored
       ```
       Confirmar (sem nova regra) que o `.index/` herda "preserva idioma da fonte".
    4. **SKILL.md + converte-livro.ps1:** refletir o pin **py -3.12** no provisionamento do venv
       (ps1 linhas 84-89: trocar `python -m venv` por `py -3.12 -m venv`; mensagem "(Python 3.12)"
       em vez de "3.10+"); atualizar o pre-aviso de tamanho (ps1 91-97 e SKILL item 2) de "~2GB"
       para "~3.5-6 GB (modelos MinerU + bge-m3) e construcao do indice". Mencionar que o fluxo
       agora gera o `.index/` alem dos `*.md`.
  </action>
  <verify>
    <automated>sh scripts/check-consistencia.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "consulta_livro" mentor/tutor.md` >= 1 (passo de consulta tecido)
    - `grep -ci "protocolo de divergencia\|APRENDIZADO" mentor/tutor.md` >= 1 (protocolo preservado)
    - `grep -c "\.index" mentor/novo-projeto.md` >= 1 e `grep -ci "modelos" mentor/novo-projeto.md` >= 1 (pre-aviso estendido)
    - `grep -c "\.index" mentor/reference.md` >= 1 (layout documentado)
    - `grep -c "3.12" scripts/converte-livro.ps1` >= 1 e `grep -c "3.12" .claude/skills/converte-livro/SKILL.md` >= 1
    - `mentor/*.md` sem acentos: `rg -c "[a-zA-Z]" --pcre2 "[\xC0-\xFF]" mentor/tutor.md mentor/novo-projeto.md mentor/reference.md` => 0 (zero acentos)
    - `sh scripts/check-consistencia.sh` exit 0 (harness de consistencia verde)
  </acceptance_criteria>
  <done>tutor consulta o indice (protocolo preservado); bootstrap constroi o indice com pre-aviso multi-GB; reference documenta .index/; skill+ps1 com py -3.12; harness verde; zero acentos.</done>
</task>

<task type="auto">
  <name>Task 2: Re-ingestao do corpus mat-financeira (D-18) + golden set executavel (D-17)</name>
  <read_first>
    - scripts/converte_livro.py (pipeline completo dos planos 02/03)
    - scripts/consulta_livro.py (CLI do plano 04)
    - tests/test_golden_set.py (esqueleto RED do plano 01)
    - tests/fixtures/golden_set.json (~15 conceitos)
    - .planning/phases/07.1-.../07.1-RESEARCH.md (Validation Architecture: gate D-17)
  </read_first>
  <files>tests/test_golden_set.py</files>
  <action>
    1. **Re-ingestao (D-18):** rodar o pipeline novo sobre o PDF de
       `.projetos/mat-financeira/livro-fonte/` regenerando `.projetos/mat-financeira/livro/`
       (778 cacos -> novo) e construindo `.projetos/mat-financeira/livro/.index/`:
       ```powershell
       .\.venv-pdf\Scripts\python scripts\converte_livro.py <pdf-em-livro-fonte> --slug mat-financeira
       ```
       Na 1a re-rodada, CONFIRMAR o overwrite do dump quebrado (o `.ps1`/script deve avisar antes
       de sobrescrever; se ainda nao avisar, o executor confirma manualmente antes de apagar).
    2. **Golden set executavel (D-17):** completar `tests/test_golden_set.py` (esqueleto do plano
       01) para: carregar `golden_set.json`, para cada item rodar
       `consulta_livro.search(index_dir, query, k=3)` sobre o `.index/` de mat-financeira, e medir:
       - `hit@1` = `pagina_esperada` esta no TOP-1 dos resultados;
       - `hit@3` = `pagina_esperada` esta no top-3;
       - `formula_presente` = para itens com `formula_esperada=true`, ha LaTeX nao-vazio OU
         `page_image_path` existente no resultado (a imagem-rede D-02 garante presenca).
       Assertar o limiar D-17: `hit@3 >= 0.80` (>=12/15), `hit@1 >= 0.60`, `formula_presente == 1.0`
       sobre os conceitos-com-formula. Marcar `@pytest.mark.slow` e `pytest.skip` gracioso se o
       `.index/` de mat-financeira nao existir (gate so onde o corpus foi re-ingerido).
    3. Caso especifico auto-validante: assertar que a consulta "taxa continua" tem `pagina_esperada
       47` em hit@3 e formula presente (corrige o "tem-se:" vazio do dump antigo).
  </action>
  <verify>
    <automated>.venv-pdf\Scripts\python -m pytest tests/test_golden_set.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `.projetos/mat-financeira/livro/.index/vectors.db` existe e `.index/corpus.jsonl` tem N linhas > 0
    - o novo `.projetos/mat-financeira/livro/` tem MUITO menos arquivos que 778 (corte por fronteira real)
    - `grep -c "hit" tests/test_golden_set.py` >= 1 e `grep -c "0.8\|0.80" tests/test_golden_set.py` >= 1 (limiar hit@3)
    - `pytest tests/test_golden_set.py -v` => PASS (limiar D-17 satisfeito) ou, se reprovar, o
      executor REGISTRA o resultado e sinaliza avaliacao da Fatia 4 (D-16) — NAO mascara o gate
    - a consulta "taxa continua" retorna pagina 47 em hit@3 com formula presente (pagina auto-validante)
  </acceptance_criteria>
  <done>corpus mat-financeira re-ingerido (overwrite confirmado); golden set mede hit@1/hit@3 + formula-presente e satisfaz o limiar D-17; pagina 47 corrigida.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Sanidade manual do tutor (gate da phase D-16/D-17)</name>
  <what-built>
    O pipeline completo (extracao com formula -> indice hibrido -> consulta) e o golden set
    automatizado (Task 2) ja rodaram VERDES sobre o corpus mat-financeira re-ingerido. Esta
    verificacao e a sanidade SEMANTICA/VISUAL que nao se automatiza (VALIDATION "Manual-Only").
  </what-built>
  <action>
    Checkpoint humano (nao-automatizavel): apresentar ao usuario a amostra de consultas e a
    evidencia (pagina+formula) descrita em how-to-verify e aguardar o sinal de aprovacao antes de
    declarar a phase fechada. O agente NAO implementa nada nesta task -- so coleta a evidencia e
    pausa para o julgamento semantico/visual.
  </action>
  <verify>
    <automated>MISSING -- verificacao manual (sanidade semantica/visual nao automatizavel, VALIDATION Manual-Only); o gate automatizado e tests/test_golden_set.py na Task 2.</automated>
  </verify>
  <how-to-verify>
    Numa sessao real, com `.projetos/mat-financeira/livro/.index/` construido:
    1. Rodar 2-3 consultas do golden set, ex.:
       `.\.venv-pdf\Scripts\python scripts\consulta_livro.py "taxa continua" --slug mat-financeira -k 3`
       e conferir que a `pagina` retornada (47) REALMENTE contem `I=ln(1+i)` no PDF/markdown.
    2. Para um conceito-com-formula cujo LaTeX pareca duvidoso, ABRIR o `page_image_path` retornado
       e confirmar que a formula esta LEGIVEL na imagem-rede (D-02).
    3. Confirmar que o tutor, lendo esses DADOS, cita "ve a pagina N + formula" corretamente.
    Se algum conceito reprovar de forma sistematica, registrar e avaliar a Fatia 4 (ColPali,
    DEFERIDA) antes de fechar a phase — NAO implementar aqui.
  </how-to-verify>
  <resume-signal>Digite "gate aprovado" se pagina+formula conferem na amostra, ou descreva o que reprovou (e se cabe avaliar a Fatia 4).</resume-signal>
  <acceptance_criteria>
    - amostra de 2-3 conceitos: a pagina citada contem o conteudo (sanidade semantica)
    - ao menos 1 conceito-com-formula confirmado legivel (LaTeX OU imagem-rede)
    - decisao explicita: phase fechada OU avaliar Fatia 4 (sem implementa-la)
  </acceptance_criteria>
  <done>Sanidade manual confirma pagina+formula corretas numa amostra real; decisao de fechamento (ou escalonamento p/ Fatia 4) registrada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| re-ingestao -> filesystem | sobrescreve `.projetos/mat-financeira/livro/` (perda de dado se nao confirmado) |
| edicoes mentor -> metodo (fonte-unica) | mudanca de prosa que o tutor segue |
| consulta no tutor -> subprocess python | invocacao de `consulta_livro.py` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-16 | Tampering/Loss | overwrite do dump 778 (D-18) | mitigate | confirmar overwrite na 1a re-rodada (avisar antes de apagar); `.projetos/` e repo proprio do aluno (gitignored) |
| T-07.1-17 | Tampering | `--slug mat-financeira` no re-ingest | mitigate | `sanitize_slug` ja confina a `.projetos/<slug>/livro/` (herdado dos planos 02/04) |
| T-07.1-18 | Repudiation | divergencia livro x pratica sem registro | mitigate | protocolo de divergencia preservado: toda escolha vira entrada DATADA em APRENDIZADO.md (D-11) |
| T-07.1-19 | Information Disclosure | consulta vaza conteudo do livro | accept | conteudo e do proprio aluno, local, gitignored; saida por dados consumida so pelo tutor |
</threat_model>

<verification>
- `sh scripts/check-consistencia.sh` exit 0; `mentor/*.md` zero acentos (Task 1).
- `.projetos/mat-financeira/livro/.index/` construido; novos `*.md` << 778 (Task 2).
- `pytest tests/test_golden_set.py -v` satisfaz o limiar D-17 (Task 2).
- Sanidade manual confirma pagina+formula na amostra real (Task 3).
</verification>

<success_criteria>
- Metodo costurado: tutor consulta o indice (protocolo de divergencia preservado), bootstrap
  constroi o indice com pre-aviso multi-GB, reference documenta `.index/`, skill/ps1 com py -3.12.
- Corpus mat-financeira re-ingerido (D-18, overwrite confirmado); pagina 47 corrigida.
- Golden set (D-17) acima do limiar (hit@3 >=80%, hit@1 >=60%, formula presente 100%) + sanidade
  manual — o "done" da phase (D-16). Reprovou -> avaliar Fatia 4 antes de fechar (sem implementar).
</success_criteria>

<output>
Apos completar, crie `.planning/phases/07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid/07.1-05-SUMMARY.md`.
</output>
