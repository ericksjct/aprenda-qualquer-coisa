---
phase: 07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - requirements-pdf.txt
  - tests/conftest.py
  - tests/fixtures/content_list_sample.json
  - tests/test_converte_livro.py
  - tests/test_consulta_livro.py
  - tests/test_golden_set.py
  - tests/fixtures/golden_set.json
autonomous: false
requirements: [D-01, D-03, D-04, D-05, D-07, D-09, D-14, D-15, D-17]
user_setup:
  - service: python-3.12-venv
    why: "MinerU so suporta Python 3.10-3.12 no Windows (dep ray); o .venv-pdf atual roda 3.14 e pip install mineru FALHA (reproduzido). BLOQUEANTE."
    env_vars: []
    dashboard_config:
      - task: "Garantir que `py -3.12` esta disponivel (instalar Python 3.12 se ausente)"
        location: "python.org/downloads (3.12.x) ou Microsoft Store"

must_haves:
  truths:
    - "O .venv-pdf roda Python 3.12 e `pip install -r requirements-pdf.txt` instala mineru/FlagEmbedding/sqlite-vec sem erro"
    - "Existe um content_list.json sintetico (text/equation/image com page_idx) que permite testar renderer/chunking/provenance SEM rodar MinerU"
    - "A logica deterministica (renderer-formula, boundary, provenance, RRF, sqlite-preflight, CLI-shape) tem testes que FALHAM agora (RED) por ausencia da funcao-alvo"
    - "golden_set.json existe com ~15 conceitos derivados de .projetos/mat-financeira/CAMINHO.md (query, pagina_esperada, formula_esperada)"
  artifacts:
    - path: "requirements-pdf.txt"
      provides: "Deps RAG opt-in + pin documentado Python 3.12"
      contains: "mineru[pipeline]"
    - path: "tests/fixtures/content_list_sample.json"
      provides: "Fixture do schema MinerU para testes sem modelo"
      contains: "equation"
    - path: "tests/fixtures/golden_set.json"
      provides: "Gate D-17: ~15 conceitos do CAMINHO.md"
      min_lines: 15
    - path: "tests/test_consulta_livro.py"
      provides: "Testes RED de RRF/preflight/CLI-shape"
      exports: ["test_rrf", "test_sqlite_preflight", "test_cli_shape"]
    - path: "tests/test_golden_set.py"
      provides: "Gate de eval (hit@1/hit@3 + formula-presente), marcado slow/skip gracioso"
      contains: "importorskip"
  key_links:
    - from: "requirements-pdf.txt"
      to: ".venv-pdf (Python 3.12)"
      via: "pip install -r"
      pattern: "mineru\\[pipeline\\]"
    - from: "tests/test_converte_livro.py"
      to: "tests/fixtures/content_list_sample.json"
      via: "carga do fixture nos testes de renderer/chunking"
      pattern: "content_list_sample"
---

<objective>
Wave 0: fundacao do RAG. Recriar o `.venv-pdf` com Python 3.12 (BLOQUEANTE — MinerU nao
instala em 3.14), acrescentar as deps RAG opt-in, e criar TODO o andaime de teste (fixtures +
golden set + testes RED) para a logica deterministica das proximas waves.

Os testes desta wave FALHAM de proposito (RED) porque as funcoes-alvo (`render_block`,
`is_chapter_boundary`/boundary novo, `build_chunks`, `rrf`, `sqlite_preflight`, CLI de consulta)
ainda nao existem. Os planos 02/03/04 as criam (GREEN). Isso firma o contrato de dados ANTES da
implementacao (Nyquist: o teste vem primeiro).

Purpose: sem o venv 3.12 NENHUMA task que importa MinerU/bge-m3 roda; sem o fixture/golden set
nao ha como provar deterministicamente o renderer/chunking/fusao nem o gate da phase (D-17).
Output: `.venv-pdf` 3.12 funcional, `requirements-pdf.txt` estendido, fixtures sinteticos,
`golden_set.json`, e suites RED em `tests/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid/07.1-CONTEXT.md
@.planning/phases/07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid/07.1-RESEARCH.md
@.planning/phases/07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid/07.1-PATTERNS.md
@.planning/phases/07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid/07.1-VALIDATION.md
@requirements-pdf.txt
@tests/conftest.py
@tests/test_converte_livro.py
@.projetos/mat-financeira/CAMINHO.md

<interfaces>
<!-- Contratos de dados que as proximas waves implementam. Definidos AQUI nos fixtures/testes. -->

Schema do bloco do MinerU content_list.json (RESEARCH Code Examples; PATTERNS fixture):
```json
[
  {"type": "text",     "text": "A taxa continua e dada por:", "page_idx": 46},
  {"type": "equation", "text": "I = \\ln(1+i)",               "page_idx": 46},
  {"type": "image",    "img_path": "images/pg47_eq.jpg",      "page_idx": 46}
]
```

Schema do chunk no corpus.jsonl (RESEARCH Pattern 3 — contrato que 02/03/04 honram):
```json
{"id": "cap-3", "text": "...", "page": 47, "chapter": "Taxa continua",
 "concept_hint": "Taxa continua", "latex": ["I = \\ln(1+i)"],
 "page_image_path": "images/pg47.jpg", "parent_id": "cap"}
```

Assinatura RRF (RESEARCH Pattern 4 — implementada em consulta_livro no plano 04):
```python
def rrf(dense_ranked: list[int], sparse_ranked: list[int], k: int = 60) -> list[int]: ...
```

Schema do golden_set.json (D-17):
```json
[{"query": "taxa continua", "pagina_esperada": 47, "formula_esperada": true}]
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Recriar .venv-pdf com Python 3.12 (BLOQUEANTE)</name>
  <read_first>
    - .planning/phases/07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid/07.1-RESEARCH.md (Pitfall 1; Installation; Environment Availability)
    - requirements-pdf.txt (estado atual: docling/pypdf/pytest)
  </read_first>
  <what-built>
    O executor PRIMEIRO estende `requirements-pdf.txt` (Task 2, autonoma). Esta task confirma a
    recriacao do venv porque depende de `py -3.12` estar instalado na maquina (Claude nao pode
    instalar o runtime Python por conta propria).
  </what-built>
  <action>
    Sequencia EXATA (PowerShell, na raiz do repo). Confirmar `py -3.12 --version` primeiro; se
    ausente, instalar Python 3.12 antes de prosseguir.
    ```powershell
    py -3.12 --version                       # deve imprimir 3.12.x
    Remove-Item -Recurse -Force .venv-pdf -ErrorAction SilentlyContinue
    py -3.12 -m venv .venv-pdf
    .\.venv-pdf\Scripts\python -m pip install --upgrade pip
    .\.venv-pdf\Scripts\python -m pip install -r requirements-pdf.txt
    ```
    NOTA D-15: o `pip install` baixa torch + transitivos (centenas de MB a GBs); os MODELOS
    (MinerU ~1-2 GB, bge-m3 ~2.3 GB) so baixam na 1a EXTRACAO/EMBEDDING (plano 02/03), nao aqui.
  </action>
  <how-to-verify>
    1. `.\.venv-pdf\Scripts\python --version` imprime `Python 3.12.x`.
    2. `.\.venv-pdf\Scripts\python -c "import mineru, FlagEmbedding, sqlite_vec; print('ok')"`
       imprime `ok` sem ImportError.
    3. `.\.venv-pdf\Scripts\python -c "import sqlite3,sqlite_vec; d=sqlite3.connect(':memory:'); d.enable_load_extension(True); sqlite_vec.load(d); d.execute('CREATE VIRTUAL TABLE t USING fts5(x)'); print('vec+fts5 ok')"`
       imprime `vec+fts5 ok` (preflight D-07/Pitfall 5).
  </how-to-verify>
  <verify>
    <automated>.venv-pdf\Scripts\python -c "import sys,mineru,FlagEmbedding,sqlite_vec; assert sys.version_info[:2]==(3,12); print('venv ok')"</automated>
  </verify>
  <resume-signal>Digite "venv ok" quando o venv 3.12 estiver criado e as 3 deps importarem, ou descreva o erro.</resume-signal>
  <acceptance_criteria>
    - `.\.venv-pdf\Scripts\python --version` => `3.12`
    - import de `mineru`, `FlagEmbedding`, `sqlite_vec` sem erro
    - preflight `enable_load_extension` + `CREATE VIRTUAL TABLE ... fts5` sem erro
  </acceptance_criteria>
  <done>O venv roda 3.12; as 3 deps importam; sqlite-vec + FTS5 carregam.</done>
</task>

<task type="auto">
  <name>Task 2: Estender requirements-pdf.txt + fixtures sinteticos + golden_set.json</name>
  <read_first>
    - requirements-pdf.txt (cabecalho de aviso "venv dedicado" a preservar)
    - tests/conftest.py (fixtures `make_item`, `synthetic_doc`, marker `slow` — molde)
    - .projetos/mat-financeira/CAMINHO.md (fonte dos ~15 conceitos do golden set, D-17)
    - .planning/phases/07.1-.../07.1-RESEARCH.md (Code Examples: schema content_list.json; Pattern 4: rrf)
  </read_first>
  <files>requirements-pdf.txt, tests/fixtures/content_list_sample.json, tests/fixtures/golden_set.json, tests/conftest.py</files>
  <action>
    1. **requirements-pdf.txt** — preservar o cabecalho de aviso; trocar o exemplo
       `python -m venv` por `py -3.12 -m venv .venv-pdf` (Pitfall 1); acrescentar exatamente:
       ```text
       mineru[pipeline]>=3.4.0
       FlagEmbedding>=1.4.0
       sqlite-vec>=0.1.9
       ```
       Adicionar uma linha de comentario: `# Python 3.12 OBRIGATORIO (MinerU nao suporta 3.13/3.14 no Windows; dep ray).`
    2. **tests/fixtures/content_list_sample.json** — fixture do schema MinerU (RESEARCH Code
       Examples). DEVE incluir a pagina 47 auto-validante: ao menos 1 bloco `text` "tem-se:",
       1 bloco `equation` com `"text": "I = \\ln(1+i)"` e `"page_idx": 46`, e 1 bloco `image`.
       Incluir tambem >=2 blocos `title`/heading em paginas distintas para exercitar o boundary
       (cortar << 778). Use chaves `type`, `text`, `page_idx`, `img_path` exatas.
    3. **tests/fixtures/golden_set.json** — ~15 entradas derivadas de CAMINHO.md, formato
       `[{"query": str, "pagina_esperada": int, "formula_esperada": bool}]`. Incluir os
       conceitos do handoff sec.8 (taxa/montante M=C(1+i); juros simples x composto; taxa
       equivalente (1+i)^n-1; base 252/360; nominal x efetiva; taxa continua I=ln(1+i) pag.47;
       desconto racional pag.55; desconto bancario; spread; margem; PMT; Price x SAC; IOF
       gross-up). A entrada da taxa continua DEVE ter `pagina_esperada: 47, formula_esperada: true`.
    4. **tests/conftest.py** — adicionar fixture `content_list_sample` que carrega o JSON acima
       (`json.loads(Path(__file__).parent / "fixtures" / "content_list_sample.json")...read_text`)
       e (se preciso) registrar marker `golden` no `pytest_configure` ja existente. NAO remover
       o marker `slow` nem `synthetic_doc`.
  </action>
  <verify>
    <automated>.venv-pdf\Scripts\python -m pytest tests/conftest.py -q --collect-only 2>&1 | tail -3 && .venv-pdf\Scripts\python -c "import json,pathlib; g=json.loads(pathlib.Path('tests/fixtures/golden_set.json').read_text(encoding='utf-8')); assert len(g)>=12; assert any(x['pagina_esperada']==47 and x['formula_esperada'] for x in g); c=json.loads(pathlib.Path('tests/fixtures/content_list_sample.json').read_text(encoding='utf-8')); assert any(b['type']=='equation' and 'ln' in b['text'] for b in c); print('fixtures ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mineru\[pipeline\]" requirements-pdf.txt` == 1 e `grep -c "py -3.12" requirements-pdf.txt` >= 1
    - `golden_set.json` tem >= 12 itens e contem o item taxa-continua (pag 47, formula true)
    - `content_list_sample.json` contem ao menos 1 bloco `equation` com LaTeX de `ln`
    - `pytest --collect-only` nao quebra na conftest
  </acceptance_criteria>
  <done>requirements estendido com pin 3.12 documentado; fixtures e golden_set carregam e validam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Testes RED da logica deterministica (renderer/boundary/provenance/RRF/preflight/CLI)</name>
  <read_first>
    - tests/test_converte_livro.py (estilo: imports, `from scripts import converte_livro as cl`, monkeypatch)
    - tests/conftest.py (fixtures disponiveis)
    - .planning/phases/07.1-.../07.1-RESEARCH.md (Validation Architecture: Req->Test Map; Pattern 3 e 4)
    - .planning/phases/07.1-.../07.1-PATTERNS.md (assinaturas-alvo: render_block, is_chapter_boundary, build_chunks, rrf)
  </read_first>
  <files>tests/test_converte_livro.py, tests/test_consulta_livro.py, tests/test_golden_set.py</files>
  <behavior>
    - test_render_formula (D-03): `cl.render_block({"type":"equation","text":"I = \\ln(1+i)"})` == `"$$\nI = \\ln(1+i)\n$$"`; bloco `text` retorna o texto; bloco `image` retorna `![](path)`.
    - test_chapter_boundary (D-04): sobre `content_list_sample`, o numero de capitulos cortados e << numero de blocos (prova que abandonou "1 arquivo por heading").
    - test_chunk_provenance (D-05): cada chunk de `cl.build_chunks(content_list_sample, ...)` tem as chaves `page`, `chapter`, `latex`, `page_image_path`; o chunk da pagina 47 traz `"I = \\ln(1+i)"` em `latex`.
    - test_rrf (D-07): `rrf([1,2,3],[3,1,2])` produz ranking deterministico esperado (id 1 e 3 acima de 2).
    - test_sqlite_preflight (D-07): `sqlite_preflight()` retorna True nesta maquina (ou lanca msg instruindo pysqlite3-binary).
    - test_cli_shape (D-09): parser de `consulta_livro` aceita `query --slug s -k 3`; `sanitize_slug` rejeita `..`; saida e JSON por linha (mockear o modelo).
    - test_golden_set (D-17): carrega golden_set.json; com `.index/` ausente faz skip gracioso; estrutura do teste mede hit@1/hit@3 e formula-presente.
  </behavior>
  <action>
    Escrever os testes ACIMA. Eles devem FALHAR agora (RED) porque `cl.render_block`,
    `cl.build_chunks`, o novo boundary, e o modulo `scripts.consulta_livro` (com `rrf`,
    `sqlite_preflight`, `main`/parser) ainda nao existem. Espelhar o estilo de
    `test_converte_livro.py` (imports, `sys.path`, monkeypatch para deps pesadas via
    `monkeypatch.setitem(sys.modules, ...)`). Os testes de consulta importam
    `from scripts import consulta_livro as ql` dentro do teste com `pytest.importorskip` apenas
    onde precisarem do modelo; a logica pura (rrf, sanitize, parser) NAO precisa de modelo.
    `test_golden_set.py` marca `@pytest.mark.slow` e faz `pytest.skip` se o `.index/` de
    mat-financeira nao existir (gate roda no plano 05).
    Comprovar o RED: rodar a suite e confirmar que os novos testes falham por
    `AttributeError`/`ModuleNotFoundError`/`ImportError` (NAO por erro de sintaxe do teste).
  </action>
  <verify>
    <automated>.venv-pdf\Scripts\python -m pytest tests/ -q -k "render_formula or chapter_boundary or chunk_provenance or rrf or sqlite_preflight or cli_shape" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - A suite COLETA sem erro de sintaxe (`pytest --collect-only tests/test_consulta_livro.py` ok)
    - Os testes novos FALHAM (RED) por funcao/modulo ausente, nao por bug no proprio teste
    - `grep -c "def test_render_formula" tests/test_converte_livro.py` == 1
    - `grep -c "def test_rrf" tests/test_consulta_livro.py` == 1
    - `grep -c "importorskip" tests/test_golden_set.py` >= 1
  </acceptance_criteria>
  <done>Suites RED commitadas; cada Req D-03/D-04/D-05/D-07/D-09/D-17 tem teste-alvo falhando por ausencia da implementacao.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip/HF download -> maquina local | torch/mineru/bge-m3 baixados de PyPI/HuggingFace (integridade/origem) |
| dev -> filesystem | venv recriado; fixtures gravados em tests/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-01 | Tampering | download de pesos/deps (pip + HF cache) | accept | Fontes oficiais (PyPI, HuggingFace) por HTTPS; pin de versao em requirements-pdf.txt (`>=3.4.0` etc.); uso local single-user, sem cadeia de supply critica |
| T-07.1-02 | Tampering | venv recriado em path do repo | mitigate | Operacao em `.venv-pdf` (gitignored, dentro do repo); comando fixo, sem interpolacao de input externo |
| T-07.1-03 | Information Disclosure | golden_set.json / fixtures | accept | Conteudo derivado de CAMINHO.md (proprio do aluno, gitignored em `.projetos/`); fixtures em tests/ nao contem segredo |
</threat_model>

<verification>
- venv 3.12 importa mineru/FlagEmbedding/sqlite-vec; sqlite-vec+FTS5 carregam (Task 1).
- `requirements-pdf.txt` contem `mineru[pipeline]` e o pin 3.12 documentado (Task 2).
- fixtures e golden_set carregam e validam estrutura (Task 2).
- Suites RED falham por ausencia de implementacao, nao por bug de teste (Task 3).
</verification>

<success_criteria>
- `.venv-pdf` roda Python 3.12 com as 3 deps RAG instaladas.
- Fixtures (`content_list_sample.json`, `golden_set.json`) e suites RED commitados.
- A logica deterministica das proximas waves tem teste-gate ANTES da implementacao.
</success_criteria>

<output>
Apos completar, crie `.planning/phases/07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid/07.1-01-SUMMARY.md`.
</output>
