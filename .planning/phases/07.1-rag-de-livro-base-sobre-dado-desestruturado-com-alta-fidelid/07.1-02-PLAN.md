---
phase: 07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - scripts/converte_livro.py
  - tests/test_converte_livro.py
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-14]

must_haves:
  truths:
    - "O renderer emite blocos `equation` como `$$LaTeX$$` no markdown legivel (pagina 47 deixa de ser 'tem-se:' vazio)"
    - "A extracao MinerU roda em CPU, em lotes com gc.collect(), preservando a blindagem de memoria (threads=1)"
    - "O capitulo e cortado so em fronteira real (title/heading grande), NAO 1 arquivo por heading"
    - "Cada chunk de retrieval carrega provenance: page, chapter, concept_hint, latex[], page_image_path"
    - "A imagem-de-pagina e persistida como rede de seguranca de fidelidade (D-02)"
  artifacts:
    - path: "scripts/converte_livro.py"
      provides: "Extracao MinerU + renderer-formula + boundary-real + build_chunks deterministico"
      contains: "def render_block"
    - path: "tests/test_converte_livro.py"
      provides: "Testes GREEN de render_formula/chapter_boundary/chunk_provenance"
      exports: ["test_render_formula", "test_chapter_boundary", "test_chunk_provenance"]
  key_links:
    - from: "scripts/converte_livro.py (extract_mineru)"
      to: "content_list.json (blocos text/equation/image)"
      via: "doc_analyze_streaming(formula_enable=True) OU CLI subprocess fallback"
      pattern: "formula_enable"
    - from: "build_chunks"
      to: "provenance page = page_idx + 1"
      via: "ancora <!-- page: N --> vira campo"
      pattern: "page_idx"
---

<objective>
Wave 1: estender `scripts/converte_livro.py` (NAO reescrever) com o nucleo de EXTRACAO e
TRANSFORMACAO do novo pipeline: extracao via MinerU (CPU, em lotes) que emite LaTeX, renderer
que emite `equation` como `$$...$$` (corrige a causa-raiz das "0 formulas" — D-03), corte de
capitulo em fronteira real (D-04) e chunking parent-child com provenance completo (D-05),
incluindo a imagem-de-pagina como rede de fidelidade (D-02).

Esta wave torna VERDES os testes RED de render/boundary/provenance do plano 01. NAO grava ainda
o `.index/` (vetores) — isso e o plano 03; aqui produzimos o markdown legivel COM formula e a
lista de chunks com provenance.

Purpose: a pagina 47 (`I=ln(1+i)`) passa a conter a formula; o livro deixa de virar 778 cacos.
Output: `converte_livro.py` estendido + testes GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid/07.1-CONTEXT.md
@.planning/phases/07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid/07.1-RESEARCH.md
@.planning/phases/07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid/07.1-PATTERNS.md
@scripts/converte_livro.py
@tests/test_converte_livro.py
@tests/conftest.py

<interfaces>
<!-- Contrato a PRESERVAR (PATTERNS.md, linhas citadas no proprio script) -->
Preservar identicos: blindagem de memoria (linhas 41-45), `sanitize_slug` (50-62),
`_page_batches` (171-191), `_ChainedDoc` (194-209), `_run_markdownlint` (272-288),
`heading_histogram` (155-165). Imports de MinerU/torch DEVEM ser LAZY (dentro das funcoes).

<!-- Schema do bloco MinerU (do fixture do plano 01) -->
{"type": "text"|"equation"|"image", "text": str, "page_idx": int, "img_path": str}

<!-- Renderer-alvo (RESEARCH Code Example 2; PATTERNS D-03) -->
def render_block(b):
    if b["type"] == "equation": return f"$$\n{b['text']}\n$$"
    if b["type"] == "text":     return b["text"]
    if b["type"] == "image":    return f"![]({b['img_path']})"
    return ""

<!-- Chunk-alvo (RESEARCH Pattern 3) -->
chunk = {"id","text","page"(=page_idx+1),"chapter","concept_hint","latex"[],"page_image_path","parent_id"}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extracao MinerU encapsulada (CPU, lotes, blindagem) + fallback CLI</name>
  <read_first>
    - scripts/converte_livro.py (convert_pdf linhas 212-269; env guards 41-45; _page_batches 171-191)
    - .planning/phases/07.1-.../07.1-RESEARCH.md (Pattern 1; Pitfall 1/3/7; Code Examples)
    - .planning/phases/07.1-.../07.1-PATTERNS.md (secao converte_livro.py: "O QUE PRESERVAR" e "Encapsular a extracao")
  </read_first>
  <files>scripts/converte_livro.py</files>
  <behavior>
    - extract_mineru(pdf_path, index_dir, lang, batch_size) retorna `content_list` (lista de
      blocos com type/text/page_idx/img_path) consolidado dos lotes; persiste images/ em index_dir.
    - Em lotes com `gc.collect()` entre eles (preserva blindagem anti std::bad_alloc).
    - Imports de mineru sao LAZY (dentro da funcao) — o modulo segue importavel sem mineru.
    - Tem fallback: se a API Python interna falhar (ImportError/assinatura), cai para subprocess
      `mineru -p <pdf> -o <out> -b pipeline -f true` e LE `*_content_list.json` (interface estavel).
  </behavior>
  <action>
    1. No topo, junto da blindagem (linhas 41-45), acrescentar
       `os.environ.setdefault("MINERU_MODEL_SOURCE", "huggingface")` (Pattern 1).
    2. Criar `extract_mineru(pdf_path, index_dir, lang="pt", batch_size=DEFAULT_BATCH_SIZE)`:
       - imports LAZY: `from mineru.cli.common import read_fn`,
         `from mineru.backend.pipeline.pipeline_analyze import doc_analyze_streaming`,
         `from mineru.backend.pipeline.pipeline_middle_json_mkcontent import union_make`,
         `from mineru.data.data_reader_writer import FileBasedDataWriter`,
         `from mineru.utils.enum_class import MakeMode` (RESEARCH Pattern 1).
       - rodar em LOTES por `page_range`/batch (reusar `_page_batches` + contagem por PdfReader),
         `gc.collect()` entre lotes; callback `on_doc_ready` captura `middle_json` -> `content_list`.
       - chamar `doc_analyze_streaming(..., formula_enable=True, table_enable=True, lang_list=[lang], parse_method="auto")`.
       - persistir images via `FileBasedDataWriter(str(index_dir / "images"))`.
       - **fallback** (Pitfall 7): envolver o caminho API em try/except (ImportError, TypeError,
         AttributeError); no except, rodar subprocess `["mineru","-p",str(pdf),"-o",str(out),"-b","pipeline","-f","true","-l",lang]`
         (lista fixa, sem shell-string com input do usuario — Security) e ler `*_content_list.json`.
    3. NAO remover `convert_pdf` (docling) — ele permanece como caminho legado para PDF nativo
       limpo (RESEARCH: Opcao A, sem regressao Phase 7). Um flag `--engine {mineru,docling}`
       (default `mineru`) seleciona; documentar no docstring `Uso:`.
    Validacao da assinatura: o executor DEVE rodar
    `.venv-pdf\Scripts\python -c "from mineru.backend.pipeline.pipeline_analyze import doc_analyze_streaming; print('api ok')"`;
    se falhar, implementar PRIMEIRO o caminho subprocess como principal e a API como tentativa.
  </action>
  <verify>
    <automated>.venv-pdf\Scripts\python -c "import scripts.converte_livro as cl; assert hasattr(cl,'extract_mineru'); print('extract ok')" && .venv-pdf\Scripts\python -m pytest tests/test_converte_livro.py -q -k "env_guards or batches" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def extract_mineru" scripts/converte_livro.py` == 1
    - `grep -c "formula_enable" scripts/converte_livro.py` >= 1
    - `grep -c "gc.collect" scripts/converte_livro.py` >= 1 (blindagem preservada)
    - `grep -c "MINERU_MODEL_SOURCE" scripts/converte_livro.py` == 1
    - O modulo importa SEM mineru instalado (imports lazy): `python -c "import scripts.converte_livro"` na suite stdlib
    - existe fallback subprocess (`grep -c "subprocess" scripts/converte_livro.py` >= 2: lint + mineru-cli)
  </acceptance_criteria>
  <done>extract_mineru roda em lotes/CPU com formula ligada, blindagem preservada, e tem fallback CLI->artefatos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Renderer-formula (D-03) + boundary real (D-04) + build_chunks com provenance (D-05)</name>
  <read_first>
    - scripts/converte_livro.py (render_chapter 120-140; _is_boundary 79-87; split_into_chapters 89-117)
    - tests/test_converte_livro.py (testes RED render_formula/chapter_boundary/chunk_provenance do plano 01)
    - tests/fixtures/content_list_sample.json (fixture do plano 01)
    - .planning/phases/07.1-.../07.1-RESEARCH.md (Code Example 2; Pattern 2 is_chapter_boundary; Pattern 3 build_chunks)
  </read_first>
  <files>scripts/converte_livro.py, tests/test_converte_livro.py</files>
  <behavior>
    - render_block(b): `equation` -> `$$\n{text}\n$$`; `text` -> texto; `image` -> `![](img_path)`; outro -> "".
    - O renderer de capitulo concatena render_block sobre os blocos do content_list (nao mais so `item.text`).
    - is_chapter_boundary(block, font_cut): True se `type=="title"` OU `text_level==1` OU altura-de-fonte >= corte; NAO corta a cada heading.
    - build_chunks(content_list, ...): parent-child; cada chunk com `page=page_idx+1`, `chapter`, `concept_hint`, `latex[]` (dos blocos equation), `page_image_path`, `parent_id`.
  </behavior>
  <action>
    1. Adicionar `render_block(b)` exatamente como o interface-block acima (RESEARCH Code Ex.2).
       Adaptar `render_chapter` para iterar blocos do content_list e usar `render_block`,
       preservando a ancora `<!-- page: N -->` no topo (de `start_page`) e o `# {title}`.
    2. Adicionar `is_chapter_boundary(block, font_cut_height)` (RESEARCH Pattern 2): corta so em
       `type in {"title"}` OU `text_level==1` OU `bbox_height(block) >= font_cut_height`. Evoluir
       `split_into_chapters` para operar sobre content_list usando esse boundary (ABANDONAR "1
       arquivo por heading"). Manter `heading_histogram` como base do `font_cut_height`.
    3. Adicionar `build_chunks(content_list, chapter_of, target_tokens=768, overlap=128)`
       (RESEARCH Pattern 3): agrupa por secao-pai, janela de ~768 tokens com overlap 128, e
       monta cada chunk com o provenance completo. `latex` = textos dos blocos `equation` da
       janela; `page = page_idx + 1`; `page_image_path` = caminho relativo ao `.index/`.
    4. Tornar VERDES os testes do plano 01 (render_formula/chapter_boundary/chunk_provenance);
       ajustar SO o necessario no teste se a assinatura final divergir levemente (sem afrouxar a
       asserção da pagina 47).
  </action>
  <verify>
    <automated>.venv-pdf\Scripts\python -m pytest tests/test_converte_livro.py -q -k "render_formula or chapter_boundary or chunk_provenance" 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -k "render_formula or chapter_boundary or chunk_provenance"` => todos PASS (GREEN)
    - `grep -c "def render_block" scripts/converte_livro.py` == 1
    - `grep -c "def build_chunks" scripts/converte_livro.py` == 1
    - `grep -c "def is_chapter_boundary" scripts/converte_livro.py` == 1
    - `python -c "import scripts.converte_livro as cl; print(cl.render_block({'type':'equation','text':'I = \\\\ln(1+i)'}))"` imprime bloco `$$...$$` com `ln`
    - render produz N capitulos << numero de blocos no fixture (prova D-04)
  </acceptance_criteria>
  <done>render_block emite `$$...$$`; boundary corta so em fronteira real; build_chunks carrega provenance completo; testes GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PDF do aluno -> parser MinerU | input nao confiavel (PDF possivelmente malicioso/enorme) |
| extracao -> filesystem | images/ e markdown gravados sob `.projetos/<slug>/livro/` |
| script -> subprocess (mineru CLI fallback) | execucao de processo externo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-04 | DoS | parse de PDF grande/malicioso | mitigate | threads=1 + lotes (`page_range`) + `gc.collect()` (blindagem preservada); progresso por lote |
| T-07.1-05 | Tampering | path de saida (slug) | mitigate | `sanitize_slug` ANTES de qualquer write; gravar so dentro de `.projetos/<slug>/livro/` |
| T-07.1-06 | Injection | subprocess do `mineru` CLI (fallback) | mitigate | lista de args FIXA (sem shell-string interpolada com input do usuario); paths via Path controlado |
| T-07.1-07 | Tampering | LaTeX duvidoso de scan ruim | accept (mitigado por D-02) | imagem-de-pagina persistida como rede de fidelidade; gate via golden set (plano 05) |
</threat_model>

<verification>
- `pytest tests/test_converte_livro.py -k "render_formula or chapter_boundary or chunk_provenance"` GREEN.
- modulo importa sem mineru (imports lazy); blindagem (gc.collect/threads=1) intacta.
- `extract_mineru` existe com `formula_enable` e fallback subprocess.
</verification>

<success_criteria>
- Renderer emite `$$LaTeX$$` (pagina 47 deixa de ser vazia).
- Corte so em fronteira real (D-04); chunks com provenance completo (D-05).
- Blindagem de memoria e `markdownlint --fix` preservados (D-14).
</success_criteria>

<output>
Apos completar, crie `.planning/phases/07.1-rag-de-livro-base-sobre-dado-desestruturado-com-alta-fidelid/07.1-02-SUMMARY.md`.
</output>
