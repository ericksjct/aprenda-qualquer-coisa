---
phase: 06-auditoria-de-consistencia
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - mentor/debug.md
  - mentor/fundamentos.md
autonomous: true
requirements: [CONS-02, CONS-03]
must_haves:
  truths:
    - "debug.md nao tem nenhuma letra acentuada (Peca/peca corrigidos em :31-32) -- V-16 fica verde"
    - "Nenhuma das 10 linhas de fundamentos.md ainda carrega a marca `pendente` na coluna Aplicado em -- todas viradas para status aplicado (D-04)"
    - "Rodar `sh scripts/check-consistencia.sh` agora sai com exit 0 (suite 100% verde, V-01..V-16)"
    - "Nenhum flip de status precedeu a confirmacao mecanica do deep grep (a suite verde do Plano 01 ja provou cada anchor antes do flip)"
  artifacts:
    - path: "mentor/debug.md"
      provides: "Protocolo de forense sem acentos (IN-01 fechado, D-07)"
      contains: "Peca a mensagem"
    - path: "mentor/fundamentos.md"
      provides: "Coluna Aplicado em com 10 status aplicados (D-04), zero `pendente`"
  key_links:
    - from: "mentor/fundamentos.md status aplicado"
      to: "scripts/check-consistencia.sh deep grep verde"
      via: "so vira aplicado o que V-01..V-11 confirmaram"
      pattern: "aplicado"
---

<objective>
Aterrissar os DOIS fixes triviais (D-05: trivial = corrige inline) que mudam os arquivos
que o script audita, agora que o Plano 01 ja escreveu o harness e provou (V-01..V-11) que
cada teoria de fato aterrissa:

1. IN-01 / D-07: remover os acentos `Peça`/`peça` em `debug.md:31-32` (conserto one-off;
   NAO virar linter permanente -- Risco #5 fora de escopo). Isto vira V-16 de red->green.
2. D-04: virar as 10 marcas `(... pendente)` da coluna "Aplicado em (doc + status)" de
   `fundamentos.md` para um status aplicado. So vira "aplicado" porque a verificacao
   profunda (V-01..V-11 do Plano 01) JA confirmou cada anchor.

Purpose: fechar CONS-02 (toda teoria com aterrissagem confirmada e marcada como aplicada)
e a divida IN-01 herdada da Fase 5, deixando a suite 100% verde.

Output: `debug.md` sem acentos, `fundamentos.md` sem nenhuma marca `pendente`, e
`sh scripts/check-consistencia.sh` => exit 0.

ORDERING (inegociavel): este plano roda DEPOIS do Plano 01. NAO flipar status antes do
deep grep confirmar -- o Plano 01 ja fez essa confirmacao mecanica (suite verde exceto V-16).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-auditoria-de-consistencia/06-CONTEXT.md
@.planning/phases/06-auditoria-de-consistencia/06-RESEARCH.md
@.planning/phases/06-auditoria-de-consistencia/06-VALIDATION.md
@scripts/check-consistencia.sh
@mentor/debug.md
@mentor/fundamentos.md

<conventions>
- Portugues SEM acentos; paths em backticks. Os fixes deste plano DEFENDEM essa convencao.
- Edicoes minimas e cirurgicas -- nao reescrever prosa, so trocar os tokens exatos.
</conventions>

<in01_exact_fix>
Os UNICOS 2 tokens acentuados de todo `mentor/` (VERIFICADO via cat -A: `PeM-CM-'a` = `Peça`):
- `mentor/debug.md:31`: `- Peça a mensagem de erro completa (copiar/colar).`
  -> `- Peca a mensagem de erro completa (copiar/colar).`
- `mentor/debug.md:32`: `... peça para descrever o comportamento observado ...`
  -> `... peca para descrever o comportamento observado ...`
Trocar `Peça`->`Peca` e `peça`->`peca`. Nenhuma outra mudanca em debug.md.
</in01_exact_fix>

<d04_exact_flips>
As 10 linhas de `fundamentos.md` com marca `pendente` na coluna "Aplicado em (doc + status)"
(strings VERIFICADAS no arquivo atual). Trocar APENAS o trecho de status entre parenteses;
manter os paths/docs citados intactos. Texto de substituicao a DISCRICAO do executor, mas
DEVE: (a) nao conter mais a palavra `pendente`; (b) ser consistente nas 10 linhas; (c) seguir
convencao sem acentos. Sugestao de status aplicado: `(aplicado)` ou `(aplicado, auditado Fase 6)`.

| Linha | Pratica | String de status ATUAL (trocar) |
|-------|---------|----------------------------------|
| :24 | Backward Design | `(Fase 2/3, pendente)` |
| :25 | Constructive Alignment | `(Fase 2, pendente)` |
| :26 | CLT / Mayer | `(ja presente; Mayer/CARGA-03 Fase 2, pendente)` -> preservar o `ja presente` da parte CLT; so o trecho `Mayer/CARGA-03 Fase 2, pendente` vira aplicado (ex: `ja presente; Mayer/CARGA-03 aplicado`) |
| :27 | Worked-Example | `(parcial hoje; Fase 2, pendente)` -> ex: `(aplicado)` (a aplicacao deixou de ser parcial; V-05 confirma) |
| :28 | Retrieval Practice | `(Fase 4, pendente)` |
| :45 | SDT | `(Fase 4, pendente)` |
| :47 | Mastery Learning | `(Fase 4, pendente)` |
| :48 | Avaliacao Formativa | `(Fase 5, pendente)` |
| :49 | Feedback Hattie | `(Fase 5, pendente)` |
| :50 | Spacing Effect | `(Fase 4, pendente)` |

As 3 linhas que ja sao `(ja presente)` (First Principles :23, CLT-core parte de :26, ZPD :46)
NAO precisam de flip -- ja estao aterrissadas.
</d04_exact_flips>

<escalation_policy>
D-05: se durante o flip o executor descobrir que algum anchor NAO confirma de fato (a RESEARCH
diz que TODOS os 11 passam, entao isto NAO e esperado) -- ESCALAR como achado, NAO virar para
"aplicado" essa linha nem inventar fix estrutural. Pendencias estruturais (teoria sem
aterrissagem, metodo duplicado num adaptador, divergencia real de conjunto) viram achado
apresentado ao usuario no relatorio (Plano 03), nunca fix silencioso. Como a suite do Plano 01
ficou verde em V-01..V-11, nenhuma escalacao e esperada.
</escalation_policy>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix IN-01 (acentos debug.md:31-32) + flip dos 10 status pendentes (D-04)</name>
  <files>mentor/debug.md, mentor/fundamentos.md</files>
  <read_first>
    - mentor/debug.md (linhas 29-34 -- os 2 tokens acentuados)
    - mentor/fundamentos.md (linhas 21-50 -- as 2 tabelas com a coluna "Aplicado em (doc + status)")
    - scripts/check-consistencia.sh (o harness do Plano 01 -- e o que prova a suite verde apos os fixes)
    - .planning/phases/06-auditoria-de-consistencia/06-RESEARCH.md (secao "IN-01 fix (D-07)" + "CONS-02 Anchor Map" -- confirma que as 10 linhas tem aterrissagem)
  </read_first>
  <action>
    Parte A (IN-01 / D-07): em `mentor/debug.md`, trocar `Peça`->`Peca` (linha 31) e `peça`->`peca`
    (linha 32), exatamente como em <in01_exact_fix>. Nenhuma outra edicao em debug.md.

    Parte B (D-04): em `mentor/fundamentos.md`, virar as 10 marcas `pendente` listadas em
    <d04_exact_flips> para um status aplicado consistente (texto a discricao; sugestao `(aplicado)`).
    Casos especiais: linha :26 preserva o `ja presente` da parte CLT e so vira o trecho Mayer;
    linha :27 deixa de ser `parcial hoje` e vira aplicado. NAO tocar nas 3 linhas que ja sao
    `(ja presente)`. NAO alterar paths/docs citados -- so o trecho de status entre parenteses.

    Ordem: aplique os dois fixes e entao rode o harness; a suite deve ficar 100% verde (V-16
    fechado pelo Parte A; CONS-02 ja estava verde). Se qualquer anchor falhar, NAO flipe essa
    linha -- siga <escalation_policy> e registre para o relatorio (Plano 03).
  </action>
  <verify>
    <automated>sh scripts/check-consistencia.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c '[áàâãéêíóôõúüçÁÀÂÃÉÊÍÓÔÕÚÜÇ]' mentor/debug.md` => 0 (zero letras acentuadas).
    - `rg -c '[áàâãéêíóôõúüçÁÀÂÃÉÊÍÓÔÕÚÜÇ]' mentor/` => 0 (todo o mentor/ limpo; V-16 verde).
    - `rg -q 'Peca a mensagem' mentor/debug.md` casa e `rg -q 'Peça' mentor/debug.md` NAO casa.
    - `rg -c 'pendente' mentor/fundamentos.md` => 0 (nenhuma marca pendente sobrou).
    - `rg -c '\(ja presente\)' mentor/fundamentos.md` >= 2 (as linhas ja-presentes intactas; nao foram mexidas).
    - As 10 linhas tabela (Backward, Alignment, CLT/Mayer, Worked-Example, Retrieval, SDT, Mastery, Formativa, Hattie, Spacing) ainda citam seus docs `mentor/<doc>.md` (paths preservados).
    - `sh scripts/check-consistencia.sh; echo $?` => exit `0` (suite 100% verde, V-01..V-16).
  </acceptance_criteria>
  <done>
    `debug.md` sem acentos, `fundamentos.md` com 10 status aplicados e zero `pendente`, e
    `sh scripts/check-consistencia.sh` sai 0. CONS-02 fechado mecanicamente; IN-01 quitado.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Nenhuma. Edicao de markdown (status flip + remocao de 2 acentos).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | (n/a) | `mentor/debug.md`, `mentor/fundamentos.md` | accept | No applicable attack surface -- doc audit phase. Edicoes de texto em arquivos trackeados; sem runtime, sem input externo. RESEARCH.md "## Security Domain": nenhuma categoria ASVS se aplica. |
</threat_model>

<verification>
- Depende do Plano 01 (script existe e provou os anchors verdes antes do flip).
- Pos-Plano-02: `sh scripts/check-consistencia.sh` => exit 0; `rg -c pendente mentor/fundamentos.md` => 0; `rg -c '[acentos]' mentor/` => 0.
- Ordering respeitado: o flip de status (D-04) so aconteceu apos a confirmacao mecanica do Plano 01.
</verification>

<success_criteria>
- IN-01 fechado: zero acentos em `mentor/` (V-16 verde).
- D-04 completo: zero `pendente`, 10 status aplicados, docs citados preservados.
- Suite `sh scripts/check-consistencia.sh` 100% verde (exit 0).
- Nenhuma escalacao estrutural disparada (esperado: nenhuma).
</success_criteria>

<output>
Apos completar, criar `.planning/phases/06-auditoria-de-consistencia/06-02-SUMMARY.md`.
</output>
