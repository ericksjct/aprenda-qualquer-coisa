---
phase: 06-auditoria-de-consistencia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/check-consistencia.sh
autonomous: true
requirements: [CONS-02, CONS-03]
must_haves:
  truths:
    - "Rodar `sh scripts/check-consistencia.sh` da raiz do repo imprime PASS/FAIL por criterio e sai com exit = numero de falhas"
    - "Os 11 anchors de CONS-02 (V-01..V-11) passam hoje (cada teoria de fundamentos.md aterrissa no doc citado)"
    - "Os checks de CONS-03 comando<->arquivo (V-12), backtick-paths (V-13), jargao-em-adaptadores (V-14) e set-equality (V-15) passam hoje"
    - "O check de acentos (V-16) FALHA hoje com got=2 (os dois cedilhas em debug.md:31-32) -- red esperado ate o fix do Plano 02 entrar"
  artifacts:
    - path: "scripts/check-consistencia.sh"
      provides: "Harness permanente PASS/FAIL codificando V-01..V-16; primeiro tool de verificacao commitado do toolkit (D-01 artefato a)"
      contains: "exit $fails"
  key_links:
    - from: "scripts/check-consistencia.sh"
      to: "mentor/fundamentos.md anchors nos docs citados"
      via: "check_has com os termos-ancora da CONS-02 Anchor Map"
      pattern: "check_has"
    - from: "scripts/check-consistencia.sh"
      to: "AGENTS.md + .claude/"
      via: "check_zero sobre o LEAK_PAT (anti-drift)"
      pattern: "check_zero"
---

<objective>
Escrever o harness permanente `scripts/check-consistencia.sh` -- o PRIMEIRO tool de
verificacao commitado do toolkit (D-01, artefato a). Ele codifica as 16 verificacoes
mecanicas (V-01..V-16) do contrato Nyquist: 11 anchors anti-cargo-cult (CONS-02),
3 checks anti-drift (CONS-03), 1 check de set-equality (CONS-03/D-06) e 1 guard de
acentos (IN-01/D-07).

Purpose: trocar a re-auditoria futura de "agente rele 8 docs" (caro em tokens, sem
artefato) por "rodar 1 comando ~0 tokens" (D-03 rationale). E o down-payment mecanico
parcial do Risco #1 (drift fonte<->adaptadores); a versao completa e o FUT-04 (v2).

Output: `scripts/check-consistencia.sh` commitado, executavel da raiz do repo. Ao rodar
hoje: tudo PASS exceto V-16 (acentos, got=2) -- red ESPERADO, fechado pelo Plano 02.
NAO aplica nenhuma correcao nem flip de status nesta fase -- so escreve e roda o script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-auditoria-de-consistencia/06-CONTEXT.md
@.planning/phases/06-auditoria-de-consistencia/06-RESEARCH.md
@.planning/phases/06-auditoria-de-consistencia/06-VALIDATION.md
@AGENTS.md

<conventions>
- Portugues SEM acentos em prosa/codigo; paths e identificadores em backticks.
- Shebang `#!/usr/bin/env sh` (POSIX, sem bashisms) -- padrao das Fases 2/4/5.
- Saida com `printf` alinhado (NUNCA `echo -e`); helpers `check_min`/`check_zero`/`check_has`;
  acumulador `fails`; `exit $fails` ao final.
- O script roda da RAIZ do repo e audita `mentor/`, `AGENTS.md`, `README.md`, `.claude/`.
</conventions>

<harness_skeleton>
A RESEARCH.md secao "## Code Examples > Full harness skeleton" tras o esqueleto COMPLETO
pronto (linhas ~325-376 de 06-RESEARCH.md). Use-o como base. Pontos verificados nele:
- Guard `command -v rg >/dev/null 2>&1 || { echo "...requer ripgrep (rg)"; exit 2; }` no topo (Pitfall 5 / O-1).
- 11 `check_has` para CONS-02 (V-01..V-11) com os termos-ancora exatos da Anchor Map.
- Loop closed-allowlist `for cmd in novo-projeto tutor fecha-marco debug spidr-split` -> `test -f mentor/$cmd.md` (V-12).
- Bloco de backtick-paths com a SKIP regex de not-in-repo -> `check_zero "backtick-paths"` (V-13).
- `check_zero "adapter-jargon"` sobre `rg -c "$LEAK" AGENTS.md .claude/` somado por awk (V-14).
- `check_zero "accents-mentor"` sobre a classe estreita de LETRAS acentuadas (V-16).
</harness_skeleton>

<anchor_map>
Os 11 anchors de CONS-02 (todos VERIFICADOS >=1 hit em sessao -- ver tabela "CONS-02 Anchor Map" na RESEARCH):

| Check ID | label sugerido | termo-ancora (rg pattern) | doc citado |
|----------|----------------|---------------------------|------------|
| V-01 | C2-backward-ref   | `backward design`                 | mentor/reference.md |
| V-02 | C2-backward-novo  | `backward design`                 | mentor/novo-projeto.md |
| V-03 | C2-align          | `Objetivo \(capacidade\)`         | mentor/reference.md |
| V-04 | C2-mayer          | `so 3 de 12 principios\|coerencia`| mentor/reference.md |
| V-05 | C2-expertrev      | `expertise-reversal`              | mentor/reference.md |
| V-06 | C2-retrieval      | `retrieval practice`              | mentor/tutor.md |
| V-07 | C2-sdt            | `proxima acao`                    | mentor/tutor.md |
| V-08 | C2-mastery        | `mastery learning`                | mentor/fecha-marco.md |
| V-09 | C2-formativa      | `auto-explicacao`                 | mentor/metodo.md |
| V-10 | C2-hattie         | `feed-forward`                    | mentor/debug.md |
| V-11 | C2-spacing        | `Agenda de retrieval`             | mentor/fecha-marco.md |

Nota discricionaria: V-03 (`Objetivo (capacidade)`) e V-04 (`so 3 de 12 principios|coerencia`)
sao os anchors marcados como discricao na RESEARCH (escolhidos por robustez); se um nao
casar exato no doc atual, prefira o anchor alternativo listado na coluna "Robust grep anchor"
da Anchor Map, mantendo >=1 hit.
</anchor_map>

<cons03_definitions>
- V-12 set canonico (D-06, fonte AGENTS.md): EXATAMENTE `novo-projeto tutor fecha-marco debug spidr-split`. NUNCA descobrir comandos por regex `/[a-z-]+` (gera falsos: `/config`, `/clear`, `/renderiza`, `/dados`...).
- V-13 SKIP regex (not-in-repo intencional): `(<slug>|<comando>|<conceito>|P0x|GEMINI\.md|CAMINHO\.md|PROGRESSO\.md|APRENDIZADO\.md|aulas/|exercicios/|projeto/|referencias/|^reference\.md$|^metodo\.md$)`. Runtime artifacts em `.projetos/<slug>/` gitignored, placeholders e link-relative forms sao SKIP, nao FAIL.
- V-14 LEAK_PAT (jargao que so pode viver em `mentor/`, nunca nos adaptadores): `SDT|mastery|spacing|backward design|Bloom|GRR|Mayer|retrieval practice|testing effect|feed-up|feed-back|feed-forward|Hattie|expertise-reversal|cognitive load|constructive alignment|formativ`. Roda WHOLE-FILE sobre `AGENTS.md` + `.claude/` apenas (NUNCA sobre `mentor/` -- la o jargao e legitimo). Baseline verificado: 0.
- V-15 set-equality (D-06, Criterio 3): cada um de `AGENTS.md`, `README.md`, `metodo.md` referencia os 5 comandos canonicos e nenhum fora do set. Implementar como: para cada um dos 3 arquivos, confirmar que os 5 nomes aparecem (`rg -q` por nome) -> 5 PASS/arquivo, ou um check agregado. Discricao na forma; objetivo e grep-avel.
- V-16 accent class (IN-01/D-07): `[áàâãéêíóôõúüçÁÀÂÃÉÊÍÓÔÕÚÜÇ]` -- LETRAS acentuadas APENAS. NUNCA `[^\x00-\x7F]` (em-dash `—` e middot `·` sao pontuacao intencional, gerariam centenas de falsos FAILs). Hoje da got=2 (debug.md:31-32) -> FAIL esperado.
</cons03_definitions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever scripts/check-consistencia.sh (harness permanente V-01..V-16)</name>
  <files>scripts/check-consistencia.sh</files>
  <read_first>
    - .planning/phases/06-auditoria-de-consistencia/06-RESEARCH.md (secao "## Code Examples > Full harness skeleton" -- esqueleto pronto; "CONS-02 Anchor Map"; "CONS-03 Three Checks"; Pitfalls 1-5)
    - .planning/phases/06-auditoria-de-consistencia/06-VALIDATION.md (mapa V-01..V-16)
    - AGENTS.md (fonte canonica do set de 5 comandos, tabela de roteamento :16-23)
    - mentor/ (confirmar que os termos-ancora existem antes de codifica-los: `rg -c '<anchor>' mentor/<doc>.md`)
  </read_first>
  <action>
    Crie `scripts/check-consistencia.sh` a partir do esqueleto da RESEARCH (secao "Full harness skeleton").
    Estrutura obrigatoria:
    1. Shebang `#!/usr/bin/env sh` + comentario de cabecalho de 2-3 linhas (proposito, "roda da raiz do repo", "exit = numero de falhas").
    2. Guard `command -v rg >/dev/null 2>&1 || { echo "check-consistencia.sh: requer ripgrep (rg)"; exit 2; }`.
    3. `fails=0`; helpers `check_min`, `check_zero`, `check_has` (clonados verbatim do skeleton).
    4. Bloco "CONS-02": os 11 `check_has` com os labels/anchors/docs da tabela <anchor_map> acima. ANTES de finalizar, rode cada `rg -c '<anchor>' mentor/<doc>.md` para confirmar >=1 hit; se V-03/V-04 nao casarem, troque pelo anchor alternativo da Anchor Map (discricao).
    5. Bloco "CONS-03 #1" (V-12): `for cmd in novo-projeto tutor fecha-marco debug spidr-split; do ... test -f mentor/$cmd.md ...`. SET FECHADO -- nao descobrir por regex.
    6. Bloco "CONS-03 #2" (V-13): extrair backtick-paths de `mentor/ AGENTS.md README.md`, aplicar a SKIP regex de <cons03_definitions>, `test -f` so os repo-resident, somar UNRESOLVED, `check_zero "backtick-paths"`.
    7. Bloco "CONS-03 #3" (V-14): `leak=$(rg -c "$LEAK" AGENTS.md .claude/ 2>/dev/null | awk -F: '{s+=$NF} END{print s+0}')`; `check_zero "adapter-jargon" "$leak"`. LEAK_PAT exato de <cons03_definitions>. NUNCA incluir `mentor/`.
    8. Bloco "CONS-03 set-equality" (V-15): para cada um de AGENTS.md, README.md, metodo.md confirmar via `rg -q` que os 5 comandos canonicos aparecem (e nenhum fora do set). Forma a discricao; mantenha grep-avel.
    9. Bloco "IN-01" (V-16): `acc=$(rg -c '[áàâãéêíóôõúüçÁÀÂÃÉÊÍÓÔÕÚÜÇ]' mentor/ 2>/dev/null | awk -F: '{s+=$NF} END{print s+0}')`; `check_zero "accents-mentor" "$acc"`. Classe ESTREITA de letras (nunca non-ASCII amplo).
    10. Rodape: `echo "== $fails fail(s) =="`; `exit $fails`.
    NAO corrija acentos nem flip de status aqui -- esses sao do Plano 02. Este plano so ENTREGA e RODA o script.
  </action>
  <verify>
    <automated>sh scripts/check-consistencia.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-consistencia.sh` existe e e um arquivo POSIX-sh (primeira linha = `#!/usr/bin/env sh`).
    - `rg -q 'exit \$fails' scripts/check-consistencia.sh` casa (acumulador + exit codificados).
    - `rg -c 'check_has' scripts/check-consistencia.sh` >= 11 (os 11 anchors CONS-02).
    - `rg -q 'for cmd in novo-projeto tutor fecha-marco debug spidr-split' scripts/check-consistencia.sh` casa (set fechado, V-12).
    - `rg -q 'AGENTS.md .claude' scripts/check-consistencia.sh` casa e `mentor/` NAO aparece no comando do leak-check (V-14 escopado aos adaptadores).
    - `sh scripts/check-consistencia.sh` imprime exatamente UM `FAIL` -- a linha `accents-mentor` com `got=2` (V-16 red esperado); todos os outros checks imprimem `PASS`.
    - `sh scripts/check-consistencia.sh; echo $?` => exit `1` (uma unica falha: V-16, ainda nao corrigido).
  </acceptance_criteria>
  <done>
    Script commitado em `scripts/check-consistencia.sh`, roda da raiz, V-01..V-15 verdes e V-16 vermelho (got=2) -- exatamente o estado pre-fix descrito no contrato Nyquist (06-VALIDATION.md).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Nenhuma. Esta e uma fase de auditoria de docs/script.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | (n/a) | `scripts/check-consistencia.sh` | accept | No applicable attack surface -- doc/script audit phase. O script e read-only (`rg`/`test -f` sobre arquivos trackeados), sem `eval`, sem input nao-confiavel, sem rede. RESEARCH.md "## Security Domain": nenhuma categoria ASVS se aplica. |
</threat_model>

<verification>
- `sh scripts/check-consistencia.sh` roda em ~2s da raiz do repo.
- Estado esperado pos-Plano-01: V-01..V-15 PASS, V-16 FAIL (got=2), exit 1.
- O fix do V-16 e o flip de status (D-04) sao do Plano 02 -- NAO antecipar aqui.
</verification>

<success_criteria>
- `scripts/check-consistencia.sh` existe, e POSIX-sh, roda da raiz e codifica V-01..V-16.
- Saida PASS/FAIL alinhada + `exit $fails`; guard de `rg` presente.
- Unico FAIL hoje = `accents-mentor` (V-16, got=2); demais verdes.
</success_criteria>

<output>
Apos completar, criar `.planning/phases/06-auditoria-de-consistencia/06-01-SUMMARY.md`.
</output>
