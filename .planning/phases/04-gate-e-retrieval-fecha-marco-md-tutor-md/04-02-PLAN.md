---
phase: 04-gate-e-retrieval-fecha-marco-md-tutor-md
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - mentor/reference.md
autonomous: true
requirements: [AVAL-02]
nyquist_compliant: true

must_haves:
  truths:
    - "O template `PROGRESSO.md` em `reference.md` tem uma secao `## Agenda de retrieval` distinta de `## Dividas de aprendizado`"
    - "A entrada da agenda usa o formato de contrato `- <conceito> -- revisitar na abertura do marco <NN>`, em linguagem sem jargao (artefato do aluno)"
    - "Nenhum jargao de framework (SDT/retrieval/mastery/spacing/...) vaza para dentro do bloco cercado do template (CONS-01)"
  artifacts:
    - path: "mentor/reference.md"
      provides: "Secao `## Agenda de retrieval` no template PROGRESSO.md -- dona do contrato cross-file (nome da secao + formato da entrada)"
      contains: "## Agenda de retrieval"
  key_links:
    - from: "mentor/reference.md (## Agenda de retrieval)"
      to: "mentor/fecha-marco.md (writer, plano 03) e mentor/tutor.md (reader, plano 04)"
      via: "string literal byte-identica do nome da secao e do formato da entrada"
      pattern: "Agenda de retrieval"
---

<objective>
Definir, no dono do template (`mentor/reference.md`), a nova secao `## Agenda de retrieval`
dentro do bloco cercado `PROGRESSO.md` (D-09). Esta secao e a FONTE DO CONTRATO cross-file:
`fecha-marco.md` (plano 03) escreve nela e `tutor.md` (plano 04) le dela. O nome da secao e o
formato da entrada definidos AQUI sao a verdade byte-identica que os outros dois planos copiam.

Purpose: AVAL-02 exige que `PROGRESSO.md` ganhe um campo de agenda de retrieval/dividas para
registrar a revisao espacada. D-09 manda que seja uma secao SEPARADA de `## Dividas de
aprendizado` (que sao dividas de curadoria, coisa diferente) -- D-02 do tutor precisa de uma
fonte limpa e inequivoca. Como o conteudo vive DENTRO de um bloco ` ```markdown `, e artefato
visivel pelo aluno -> tem de ser livre de jargao (CONS-01).

Output: `mentor/reference.md` com a secao `## Agenda de retrieval` no template PROGRESSO.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/04-RESEARCH.md
@.planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/04-CONTEXT.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Estrutura ATUAL do template PROGRESSO.md em reference.md (linhas ~198-250, dentro de um -->
<!-- bloco ```markdown). Inserir a nova secao ENTRE `## Dividas de aprendizado` (~L243) e -->
<!-- `## Log` (~L247). Releia o arquivo para os numeros de linha atuais antes de editar. -->

Ordem atual das secoes no template (dentro do fence):
  ## Substrato por assunto
  ## Objetivo
  ## Output final
  ## Definition of Done
  ## Marcos        (cada marco tem **User Story:**, **Capacidade:**, **Passos do caminho:**, **Entregavel:**)
  ## Dividas de aprendizado     <-- ~L243
  ## Log                        <-- ~L247

CONTRATO a fixar aqui (copiado por fecha-marco.md e tutor.md verbatim):
  - Nome da secao : ## Agenda de retrieval
  - Formato da entrada : - <conceito> -- revisitar na abertura do marco <NN>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inserir a secao `## Agenda de retrieval` no template PROGRESSO.md</name>
  <read_first>
    - mentor/reference.md (LER o bloco do template PROGRESSO.md ~L198-250 para confirmar os numeros de linha e a indentacao/estilo atuais ANTES de editar)
    - .planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/04-RESEARCH.md (edit map de reference.md + Code Examples "agenda entry template form")
    - .planning/codebase/CONVENTIONS.md (portugues SEM acentos; paths em backticks)
  </read_first>
  <files>mentor/reference.md</files>
  <behavior>
    - V-08: `rg -n "## Agenda de retrieval" mentor/reference.md` retorna 1 linha.
    - V-09: `## Agenda de retrieval` E `## Dividas de aprendizado` existem em linhas distintas (duas secoes separadas, nao fundidas).
    - V-15 (anti-leak): `sh scripts/extract-fenced.sh mentor/reference.md | rg -c "SDT|retrieval|mastery|spacing|backward design|Bloom|GRR|Mayer"` == 0. ATENCAO: o nome da secao e `Agenda de retrieval` -- a palavra "retrieval" no padrao anti-leak casaria! Confirme com o executor: o check V-15 da Fase 4 mira jargao de FRAMEWORK dentro do fence; o NOME da secao `Agenda de retrieval` e parte do contrato e e linguagem do produto, nao jargao teorico. Resolva escrevendo o nome da secao SEM a palavra ser tratada como leak: o padrao de V-15 em check-phase4.sh NAO deve incluir "retrieval" isolado se o nome da secao o usa -- ver nota abaixo.
  </behavior>
  <action>
    No bloco ` ```markdown ` do template `PROGRESSO.md` (em `mentor/reference.md`), insira uma
    nova secao ENTRE `## Dividas de aprendizado` e `## Log`. Texto EXATO a inserir (portugues
    SEM acentos, sem jargao de framework -- e artefato do aluno):

    ```
    ## Agenda de retrieval

    - <conceito> -- revisitar na abertura do marco <NN>
    ```

    Regras:
    - O nome da secao e EXATAMENTE `## Agenda de retrieval` (contrato; D-09). Distinta de
      `## Dividas de aprendizado` -- NAO funda as duas.
    - O formato da entrada e EXATAMENTE `- <conceito> -- revisitar na abertura do marco <NN>`
      (contrato; copiado verbatim por `fecha-marco.md` no plano 03 e lido por `tutor.md` no
      plano 04).
    - `<conceito>` e linguagem PLANA derivada da frase de `**Capacidade:**` -- nunca o nome de
      um framework. Zero jargao dentro do fence.

    NOTA ANTI-LEAK (resolver o conflito "retrieval"): a palavra "retrieval" aparece no NOME da
    secao por design de contrato (D-09). Para nao gerar falso-positivo no check V-15, ajuste o
    padrao do anti-leak em `check-phase4.sh` (plano 01, ja autorado) para mirar so jargao
    teorico de framework -- isto e, REMOVA o token isolado `retrieval` do padrao V-15
    (mantenha `SDT|mastery|spacing|backward design|Bloom|GRR|Mayer`) OU torne o match
    word-boundary contra "retrieval practice"/"testing effect". O objetivo de CONS-01 e barrar
    DEFINICAO/jargao teorico vazando pro aluno; o nome de secao `Agenda de retrieval` e rotulo
    de produto, nao teoria. Documente esse ajuste no SUMMARY.
  </action>
  <verify>
    <automated>rg -n "^## Agenda de retrieval" mentor/reference.md && rg -n "^## Dividas de aprendizado" mentor/reference.md && rg -n "revisitar na abertura do marco" mentor/reference.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "^## Agenda de retrieval" mentor/reference.md` retorna exatamente 1 linha (V-08)
    - `rg -n "^## Dividas de aprendizado" mentor/reference.md` retorna 1 linha em numero DIFERENTE da Agenda (V-09 -- duas secoes distintas)
    - `rg -n "revisitar na abertura do marco" mentor/reference.md` retorna 1 linha com o formato `- <conceito> -- revisitar na abertura do marco <NN>`
    - `sh .planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/scripts/extract-fenced.sh mentor/reference.md | rg -c "SDT|mastery|spacing|backward design|Bloom|GRR|Mayer"` == 0 (V-15 anti-leak, com o padrao ajustado conforme a NOTA)
    - `rg -c "ã|ç|é|á|í|ó|ú|â|ê|õ" mentor/reference.md` -- a secao inserida nao introduz acentos (verificar o diff)
  </acceptance_criteria>
  <done>A secao `## Agenda de retrieval` existe no template, distinta de `## Dividas de aprendizado`, com o formato de entrada de contrato, livre de jargao e de acentos.</done>
</task>

</tasks>

<threat_model>
## N/A -- justificativa

Fase de edicao de documentacao markdown. Sem auth, rede, parsing de input, cripto ou dados.
Nenhuma categoria ASVS aplica (04-RESEARCH.md "Security Domain"). Unica preocupacao de
integridade e editorial: anti-leak (V-15) garante que jargao teorico nao vaze para o artefato
do aluno dentro do fence; o contrato cross-file (V-13) e verificado no plano 04. Nada inventado.

| Trust Boundary | Description |
|----------------|-------------|
| N/A | Documento markdown lido por agente/aluno; sem boundary de runtime |
</threat_model>

<verification>
- V-08, V-09 verdes apos a edicao (`check-phase4.sh`).
- V-15 anti-leak == 0 (padrao ajustado para nao casar o rotulo de contrato "retrieval").
- Sem acentos introduzidos.
</verification>

<success_criteria>
- `## Agenda de retrieval` presente, distinta de `## Dividas de aprendizado`, com o formato de entrada de contrato.
- Contrato (nome da secao + formato da entrada) fixado para os planos 03 e 04 copiarem.
- CONS-01 preservado dentro do fence.
</success_criteria>

<output>
After completion, create `.planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/04-02-SUMMARY.md`
</output>
