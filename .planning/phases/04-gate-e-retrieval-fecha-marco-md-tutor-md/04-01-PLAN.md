---
phase: 04-gate-e-retrieval-fecha-marco-md-tutor-md
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/scripts/extract-fenced.sh
  - .planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/scripts/check-phase4.sh
autonomous: true
requirements: [ENG-01, AVAL-01, AVAL-02, AVAL-03, AVAL-04]
nyquist_compliant: true

must_haves:
  truths:
    - "Um script estatico `check-phase4.sh` roda os 15 checks V-01..V-15 e retorna exit code != 0 se qualquer check positivo falhar"
    - "O extrator de blocos cercados `extract-fenced.sh` esta presente no scripts dir da Fase 4 (clone verbatim da Fase 2)"
    - "No baseline (antes das edicoes dos planos 02/03/04) os checks positivos FALHAM e o ANTI-LEAK (V-15) PASSA -- mesma convencao da Fase 2"
  artifacts:
    - path: ".planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/scripts/extract-fenced.sh"
      provides: "Isolador de conteudo de bloco cercado para o grep anti-leak (CONS-01)"
      contains: "inside = !inside"
    - path: ".planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/scripts/check-phase4.sh"
      provides: "Gate estatico PASS/FAIL para V-01..V-15"
      contains: "check_min"
  key_links:
    - from: "scripts/check-phase4.sh"
      to: "scripts/extract-fenced.sh"
      via: "sh \"$DIR/extract-fenced.sh\" para o check V-15 anti-leak"
      pattern: "extract-fenced\\.sh"
    - from: "scripts/check-phase4.sh"
      to: "mentor/fundamentos.md"
      via: "ANCHOR-RESOLVE grep das 3 headings (V-14)"
      pattern: "Frameworks (foundational|supporting)"
---

<objective>
Construir o harness de verificacao estatica da Fase 4: clonar `extract-fenced.sh` verbatim da
Fase 2 e autorar `check-phase4.sh` codificando os 15 checks V-01..V-15 de `04-VALIDATION.md`.
Este e o Wave 0 -- precisa existir antes de qualquer edicao em `mentor/` para que cada plano
de edicao tenha um gate grep-avel.

Purpose: Esta fase NAO tem runtime nem framework de teste. "Verificacao" e grep estatico
(presenca/ausencia + resolucao de ancora + igualdade de contrato cross-file), modelado
verbatim no harness provado da Fase 2 (`check-phase2.sh` + `extract-fenced.sh`). Sem este
gate, os planos 02/03/04 nao teriam como provar conclusao.

Output: dois scripts em `.planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/scripts/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/04-VALIDATION.md
@.planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/04-RESEARCH.md
@.planning/phases/02-templates-e-sintaxe-reference-md/scripts/check-phase2.sh
@.planning/phases/02-templates-e-sintaxe-reference-md/scripts/extract-fenced.sh

<interfaces>
<!-- O gate da Fase 2 a CLONAR. Padrao de funcoes (copie verbatim): -->
<!-- count(pat,file): rg -c, fallback echo 0 -->
<!-- check_min(label,val,min): PASS quando val >= min (checks positivos) -->
<!-- check_zero(label,val): PASS quando val == 0 (so o anti-leak V-15) -->
<!-- check_has(label,pat,file): PASS quando rg -q acha o padrao (ANCHOR-RESOLVE) -->

extract-fenced.sh (clonar verbatim, sem alteracao de logica):
```sh
FILE="${1:-mentor/reference.md}"
awk '
  /^[[:space:]]*```/ { inside = !inside; next }
  inside { print }
' "$FILE"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clonar extract-fenced.sh verbatim para o scripts dir da Fase 4</name>
  <read_first>
    - .planning/phases/02-templates-e-sintaxe-reference-md/scripts/extract-fenced.sh (o original a clonar)
    - .planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/04-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <files>.planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/scripts/extract-fenced.sh</files>
  <action>
    Crie o diretorio `.planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/scripts/`
    e copie `extract-fenced.sh` da Fase 2 VERBATIM (logica byte-identica). O script isola SO o
    conteudo dentro de blocos cercados (` ``` `) usando o toggle awk `inside = !inside`, para
    que o grep anti-leak (V-15/CONS-01) rode SO contra o texto que vira artefato do aluno e nao
    de falso-positivo na prosa legitima. Nao altere a logica; o default de FILE pode permanecer
    `mentor/reference.md`. Mantenha o cabecalho de comentario explicando o porque.
  </action>
  <verify>
    <automated>sh .planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/scripts/extract-fenced.sh mentor/reference.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/scripts/extract-fenced.sh` existe e contem `inside = !inside`
    - `rg -c "inside = !inside" .planning/phases/04-.../scripts/extract-fenced.sh` >= 1
    - Rodar `sh extract-fenced.sh mentor/reference.md` imprime conteudo de bloco cercado (nao vazio, sem as cercas ```)
  </acceptance_criteria>
  <done>O extrator esta presente no scripts dir da Fase 4, logica identica ao da Fase 2, e roda sem erro.</done>
</task>

<task type="auto">
  <name>Task 2: Autorar check-phase4.sh codificando V-01..V-15</name>
  <read_first>
    - .planning/phases/02-templates-e-sintaxe-reference-md/scripts/check-phase2.sh (o template a clonar: count/check_min/check_zero/check_has/ANCHOR-RESOLVE)
    - .planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/04-VALIDATION.md (a tabela V-01..V-15 com os comandos rg exatos)
    - mentor/fundamentos.md (confirmar headings literais para o ANCHOR-RESOLVE V-14)
  </read_first>
  <files>.planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/scripts/check-phase4.sh</files>
  <action>
    Clone `check-phase2.sh` como `check-phase4.sh`, reusando as funcoes `count`/`check_min`/
    `check_zero`/`check_has` VERBATIM. Defina `TUT="mentor/tutor.md"`, `FCH="mentor/fecha-marco.md"`,
    `REF="mentor/reference.md"`, `FUN="mentor/fundamentos.md"`, `DIR=$(dirname "$0")`. Codifique
    estes 15 checks (use os comandos rg exatos de 04-VALIDATION.md):

    - V-01 (check_min, AVAL-01): `count '[Rr]ecuperacao ativa' "$TUT"` >= 1 -- presenca do heading de recuperacao no tutor.
    - V-02 (check_min, AVAL-01/D-02): `count 'Agenda de retrieval' "$TUT"` >= 1 -- tutor referencia a secao.
    - V-03 (check_min, AVAL-01/D-04): `count 'agenda.*vazia|nenhum conceito|pule|sem entrada' "$TUT"` >= 1 -- regra skip-on-empty.
    - V-04 (check_min, AVAL-03): `count 'mastery gate|gate de maestria|gate de marco' "$FCH"` >= 1.
    - V-05 (check_min, AVAL-03/D-06): `count 'Capacidade' "$FCH"` >= 1 -- gate le o campo.
    - V-06 (check_min, AVAL-04/D-07): `count 'como voce mudaria|estender|extensao|transferencia' "$FCH"` >= 1.
    - V-07 (check_min, AVAL-04/D-08): `count 'NAO .*fecha|volte ao ciclo' "$FCH"` >= 1 -- blocking-formative.
    - V-08 (check_min, AVAL-02): `count '## Agenda de retrieval' "$REF"` >= 1.
    - V-09 (check_min, AVAL-02/D-09): `count '## Dividas de aprendizado' "$REF"` >= 1 (distinta; V-08 ja cobre Agenda). Ambas >= 1 prova que as duas secoes coexistem.
    - V-10 (check_min, AVAL-02): `count 'Agenda de retrieval' "$FCH"` >= 1 -- fecha-marco escreve a entrada.
    - V-11 (check_min, ENG-01): `count 'exatamente 1|uma .*proxima acao|1 proxima acao' "$TUT"` >= 1 E o mesmo padrao em `"$FCH"` >= 1 (faca 2 check_min, V-11a tutor e V-11b fecha-marco).
    - V-12 (check_min, ENG-01/D-13): `count 'fundamentos\.md#frameworks-supporting-ancoram-um-doc' "$TUT"` + idem `"$FCH"` >= 1 cada, E `count 'fundamentos\.md#sdt-relatedness-em-solo-ia'` somando os dois arquivos >= 1.
    - V-13 (CONTRACT cross-file): a literal `Agenda de retrieval` aparece em TODOS os 3 (`$REF`, `$FCH`, `$TUT`) -- faca um check_has por arquivo OU um bloco que falha se qualquer um dos tres count for 0.
    - V-14 (ANCHOR-RESOLVE, bloco if igual ao da Fase 2 L84-90): PASS so se `rg -q '^## Frameworks supporting \(ancoram um doc\)' "$FUN"` E `rg -q '^### SDT relatedness em solo' "$FUN"` E `rg -q '^## Frameworks foundational \(load-bearing\)' "$FUN"` todos derem exit 0.
    - V-15 (check_zero, CONS-01 anti-leak): `leak=$(sh "$DIR/extract-fenced.sh" "$REF" | rg -c "SDT|retrieval|mastery|spacing|backward design|Bloom|GRR|Mayer" 2>/dev/null || echo 0)` -> check_zero "ANTI-LEAK" "$leak".

    Termine com `echo "== $fails fail(s) =="` e `exit $fails`, igual a Fase 2. Adicione no
    cabecalho a nota de baseline: "antes das edicoes dos planos 02/03/04 os checks POSITIVOS
    vao FALHAR -- esperado; so V-15 (ANTI-LEAK) passa no baseline".
  </action>
  <verify>
    <automated>sh .planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/scripts/check-phase4.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `check-phase4.sh` existe e contem `check_min`, `check_zero`, e a chamada `extract-fenced.sh` (V-15)
    - O script imprime uma linha PASS/FAIL por check e termina com `== N fail(s) ==`
    - No BASELINE (antes das edicoes): V-15 ANTI-LEAK = PASS (got=0) e V-14 ANCHOR-RESOLVE = PASS (as 3 headings ja existem em fundamentos.md); os demais checks positivos FALHAM (esperado, igual Fase 2)
    - `rg -q '^## Frameworks supporting \(ancoram um doc\)' mentor/fundamentos.md` retorna exit 0 (confirma que o ANCHOR-RESOLVE V-14 do script casa um heading real)
  </acceptance_criteria>
  <done>check-phase4.sh roda, codifica V-01..V-15, e no baseline so V-14 e V-15 passam (os positivos das edicoes ainda nao foram aplicados).</done>
</task>

</tasks>

<threat_model>
## N/A -- justificativa

Esta e uma fase de edicao de documentacao markdown (e autoria de dois scripts shell de
verificacao estatica que rodam SO localmente sobre arquivos do repo). Nao ha autenticacao,
rede, parsing de input nao-confiavel, cripto, nem manuseio de dados. Nenhuma categoria ASVS
se aplica (confirmado em 04-RESEARCH.md "Security Domain"). A unica preocupacao de integridade
e editorial (anti-drift / anti-leak), ja coberta pela Validation Architecture (V-15 anti-leak,
V-13 contrato cross-file, V-14 anchor-resolve). Nenhuma ameaca inventada.

| Trust Boundary | Description |
|----------------|-------------|
| N/A | Sem boundary de runtime -- scripts shell locais sobre arquivos versionados |
</threat_model>

<verification>
- `sh scripts/check-phase4.sh` roda em ~2s e produz uma linha PASS/FAIL por check V-01..V-15.
- Baseline: V-14 (ANCHOR-RESOLVE) e V-15 (ANTI-LEAK) PASS; positivos FALHAM (esperado).
- `extract-fenced.sh` isola conteudo cercado de `mentor/reference.md`.
</verification>

<success_criteria>
- Dois scripts presentes no scripts dir da Fase 4.
- `check-phase4.sh` codifica todos os 15 checks com os comandos rg de 04-VALIDATION.md.
- Convencao de baseline da Fase 2 preservada (positivos falham ate as edicoes; anti-leak passa).
</success_criteria>

<output>
After completion, create `.planning/phases/04-gate-e-retrieval-fecha-marco-md-tutor-md/04-01-SUMMARY.md`
</output>
